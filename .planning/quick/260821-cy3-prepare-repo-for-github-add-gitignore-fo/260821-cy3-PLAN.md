---
phase: 260821-cy3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - ecosystem.config.js
  - ecosystem.config.js.example
  - scripts/backup_data.sh
  - requirements.txt
  - DEPLOY.md
  - SECURITY-NOTES.md
  - README.md
autonomous: true
requirements:
  - GH-IGNORE
  - GH-UNTRACK
  - GH-SECRET
  - GH-BACKUP
  - GH-DEPS
  - GH-DEPLOY
user_setup:
  - service: github
    why: "Первый push в новый публичный/приватный репозиторий — требует аккаунта и созданного пустого репозитория"
    dashboard_config:
      - task: "Создать пустой репозиторий (без README/license/.gitignore)"
        location: "github.com -> New repository"

must_haves:
  truths:
    - "Свежий git clone этого репозитория не содержит базы данных, фотографий лиц, xlsx-выгрузок, venv, .planning/ и реального SECRET_KEY"
    - ".planning/ и ecosystem.config.js перестают отслеживаться git, но остаются на диске и продолжают работать локально"
    - "scripts/backup_data.sh одной командой создаёт timestamped-архив с data/app.db, data/encodings.json, data/faces/ и data_backup_pre_sqlite/"
    - "requirements.txt содержит все сторонние пакеты, которые импортируют app.py / models.py / migrate*.py, с версиями из текущего venv"
    - "DEPLOY.md на русском описывает путь от пустого сервера Ubuntu до работающего PM2-процесса, включая создание репозитория и первый push"
    - "SECURITY-NOTES.md описывает ручную чистку истории через git filter-repo и НЕ содержит утёкшего секрета в тексте"
    - "Executor не создал remote, не сделал push и не переписал историю — коммит e7d5621 всё ещё существует"
  artifacts:
    - path: ".gitignore"
      provides: "исключение данных, PII, секретов и dev-артефактов"
      contains: "data_backup_pre_sqlite/"
    - path: "ecosystem.config.js.example"
      provides: "коммитабельный шаблон PM2-конфига без реального секрета"
      contains: "REPLACE_WITH_GENERATED_SECRET"
    - path: "scripts/backup_data.sh"
      provides: "перенос данных на новый сервер одним архивом"
      contains: "tar -czf"
    - path: "requirements.txt"
      provides: "воспроизводимая установка зависимостей на чистом сервере"
      contains: "opencv-contrib-python=="
    - path: "DEPLOY.md"
      provides: "русская инструкция: GitHub + развёртывание на Ubuntu"
      min_lines: 120
    - path: "SECURITY-NOTES.md"
      provides: "ручная процедура ротации секрета и чистки git-истории"
      contains: "git filter-repo"
  key_links:
    - from: ".gitignore"
      to: "ecosystem.config.js"
      via: "ignore rule + committed .example"
      pattern: "^ecosystem\\.config\\.js$"
    - from: "DEPLOY.md"
      to: "scripts/backup_data.sh"
      via: "шаг переноса данных"
      pattern: "scripts/backup_data\\.sh"
    - from: "DEPLOY.md"
      to: "SECURITY-NOTES.md"
      via: "обязательный шаг перед первым push"
      pattern: "SECURITY-NOTES\\.md"
---

<objective>
Подготовить репозиторий к публикации на GitHub: убрать мусор и PII из-под контроля версий, перестать отслеживать реальный секрет, дать пользователю один скрипт для переноса данных и полную русскую инструкцию по развёртыванию на новом сервере Ubuntu.

Purpose: Сейчас репозиторий нельзя пушить — в истории лежит рабочий `SECRET_KEY`, а в рабочей директории untracked-мусор (`data_backup_pre_sqlite/`, xlsx с ПДн сотрудников), который легко закоммитить случайно. Плюс `requirements.txt` неполный, поэтому на чистом сервере проект не поднимется.
Output: обновлённый `.gitignore`, `ecosystem.config.js.example`, `scripts/backup_data.sh`, полный `requirements.txt`, `DEPLOY.md`, `SECURITY-NOTES.md`.
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.gitignore
@ecosystem.config.js
@requirements.txt
@README.md

**Установленные факты (проверены на диске — НЕ перепроверять):**

- Ветка: `master`. Remote не настроен (`git remote -v` пусто) — ничего никогда не пушилось.
- Хранилище — **SQLite**, не JSON. Реальные данные: `data/app.db` (135 KB), `data/encodings.json` (147 KB, 128-d эмбеддинги лиц), `data/faces/` (40 подпапок, фото сотрудников = ПДн). Итого `data/` = 1.8 MB. `CLAUDE.md` в части «JSON files, no DB» устарел — в этом плане его не трогаем.
- `.gitignore` уже исключает: `data/app.db`, `data/encodings.json`, `data/faces/`, `venv/`, `__pycache__/`, `.pytest_cache/`, `*.egg-info/`, `dist/`, `build/`, `.DS_Store`, `*.swp`. Эти строки НЕ дублировать.
- Отслеживается git 243 файла, из них **196 — это `.planning/`**. Остальное: `.gitignore`, `CLAUDE.md`, `app.py`, `ecosystem.config.js`, `migrate.py`, `migrate_to_sqlite.py`, `models.py`, `pytest.ini`, `requirements.txt`, `templates/*` (19 файлов), `tests/*` (18 файлов).
- `README.md` существует (3.4 KB), но **не отслеживается** git.
- Untracked мусор в корне: `data_backup_pre_sqlite/` (432 KB — старые `app.db`, `employees.json`, `users.json` с правами 600, `faces/`), `employee-medical-education-report-1781259653386.xlsx` (выгрузка с ПДн), `instance/` (пустая папка Flask), `.claude/`.
- Ни одного `*.xlsx` под контролем версий нет — правило `/*.xlsx` безопасно.
- `ecosystem.config.js` **отслеживается** и содержит реальный секрет вместо плейсхолдера `REPLACE_WITH_GENERATED_SECRET`, о котором говорят его же комментарии. Секрет — 64 hex-символа, встречается ровно в 1 отслеживаемом файле. Запускает `venv/bin/gunicorn -w 1 -b 127.0.0.1:5051 app:app`, имя процесса PM2 — `face-recognition`, cwd — `/var/www/sites/face-almgp33`.
- Python в venv: **3.14.4**. Полный `pip freeze` уже снят, версии перечислены в Задаче 2 — повторно `pip freeze` можно вызвать для сверки, но список пакетов уже готов.
- Импорты сторонних пакетов (`app.py`, `models.py`, `migrate*.py`): `flask`, `flask_limiter`, `flask_sqlalchemy`, `flask_wtf.csrf`, `itsdangerous`, `openpyxl`, `sqlalchemy`, `werkzeug.middleware.proxy_fix`, `bcrypt`, `cv2`, `face_recognition`, `numpy`. Тесты дополнительно используют `pytest`.
- `scripts/` в репозитории пока нет — это новая директория.

**Решения пользователя (не пересматривать):**

- Историю git (389 коммитов) **сохраняем**, squash не делаем.
- `.planning/` исключаем из публичного репозитория — это внутренние заметки процесса разработки, не часть продукта.
- Инструкции пишем **на русском** — пользователь общается на русском.
</context>

<constraints>
**Executor НЕ ДЕЛАЕТ (это ручные шаги пользователя, только документируются):**

- НЕ выполняет `git remote add`, `git push`, не создаёт репозиторий на GitHub.
- НЕ выполняет `git filter-repo`, `git filter-branch`, `git rebase`, `git commit --amend` и любую другую перезапись истории.
- НЕ выполняет `pm2 restart` / `pm2 start` — на машине работает живой сервис, ротация секрета разлогинит пользователей; момент перезапуска выбирает владелец.
- НЕ удаляет `data_backup_pre_sqlite/` — сначала архивируем, удаление остаётся на усмотрение пользователя.

**Ожидаемый побочный эффект:** после Задачи 1 `.planning/` попадает под ignore, поэтому финальный коммит GSD-воркфлоу не сможет добавить `.planning/quick/.../PLAN.md` и `SUMMARY.md` (`git add` вернёт «ignored by .gitignore»). Это **штатно и ожидаемо** — файлы остаются на диске. Не использовать `git add -f` для `.planning/`, не откатывать ignore-правило.
</constraints>

<tasks>

<task type="auto">
  <name>Задача 1: Закрыть утечки — .gitignore, снятие с отслеживания, ротация секрета</name>
  <files>.gitignore, ecosystem.config.js.example, ecosystem.config.js</files>
  <action>
Шаг 1 — дополнить `.gitignore`. Правила, которые там уже есть (`data/app.db`, `data/encodings.json`, `data/faces/`, `venv/`, `__pycache__/`, `.pytest_cache/`, `.DS_Store` и прочие), НЕ дублировать. Добавить в конец файла новый блок с комментариями на русском:

- `.planning/` — внутренние заметки GSD, не часть продукта (GH-IGNORE).
- `.claude/` — конфигурация агента, не часть продукта.
- `data_backup_pre_sqlite/` — дамп до миграции на SQLite, содержит ПДн и старые хеши паролей.
- `backups/` — сюда пишет архивы скрипт из Задачи 2.
- `instance/` — instance-папка Flask.
- `/*.xlsx`, `/*.xls`, `/*.csv` — выгрузки отчётов в корне содержат ПДн сотрудников. Обязательно с ведущим слешем: якорь на корень, чтобы не задеть возможные фикстуры внутри `tests/`.
- `ecosystem.config.js` — хранит боевой `SECRET_KEY`; в репозиторий идёт только `.example`.

Шаг 2 — снять с отслеживания, оставив файлы на диске (GH-UNTRACK):
`git rm -r --cached .planning -q` и `git rm --cached ecosystem.config.js -q`.
Флаг `--cached` обязателен: файлы должны остаться на диске, PM2 продолжает читать локальный `ecosystem.config.js`. Удаления остаются в индексе и войдут в коммит Задачи 3.

Шаг 3 — создать `ecosystem.config.js.example` (GH-SECRET). За основу взять текущий `ecosystem.config.js`, но:
- в `SECRET_KEY` поставить строку `REPLACE_WITH_GENERATED_SECRET` — реального значения в файле быть не должно;
- заменить абсолютные пути `/var/www/sites/face-almgp33` на `/var/www/face-almgp33` — это целевая директория из `DEPLOY.md` (Задача 3), пути в обоих файлах обязаны совпадать;
- сохранить `name: "face-recognition"`, `-w 1`, `exec_mode: "fork"`, `interpreter: "none"`, порт `127.0.0.1:5051`;
- сохранить комментарий-предупреждение о том, что `-w 1` обязателен (advisory-блокировки `fcntl.flock` не работают между воркерами);
- добавить в шапку комментарий на русском: файл — шаблон, скопировать в `ecosystem.config.js` (он в .gitignore), подставить свой секрет и путь.

Шаг 4 — ротация секрета в ЛОКАЛЬНОМ `ecosystem.config.js` (файл теперь untracked). Сгенерировать новое значение командой `/var/www/sites/face-almgp33/venv/bin/python -c "import secrets; print(secrets.token_hex(32))"` и заменить им старое 64-символьное hex-значение. Пути в локальном файле НЕ менять — там реальный путь этой машины. `pm2 restart` не выполнять: новый секрет вступит в силу вручную (шаг описан в SECURITY-NOTES.md), потому что рестарт разлогинит всех активных пользователей.

Шаг 5 — разведка для документации: выполнить `grep -n "itsdangerous" app.py` и посмотреть, для чего используется подписывание. Если секретом подписываются долгоживущие ссылки (например токены регистрации или сброса пароля), а не только cookie сессии — зафиксировать это, в Задаче 3 в SECURITY-NOTES.md нужно предупредить, что после ротации такие ссылки станут недействительными. Если используется только для сессий — написать, что последствие ротации ограничено разлогином.
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && OLD=caf3e39dc0b37fca957176a65ef933de2b6929b0ef864910d7902e76ec661d8d && git check-ignore -q .planning && git check-ignore -q data_backup_pre_sqlite && git check-ignore -q backups && git check-ignore -q instance && git check-ignore -q ecosystem.config.js && git check-ignore -q employee-medical-education-report-1781259653386.xlsx && echo "ignore-rules OK" && [ "$(git ls-files .planning | wc -l)" -eq 0 ] && [ "$(git ls-files ecosystem.config.js | wc -l)" -eq 0 ] && echo "untracked OK" && [ -d .planning ] && [ -f ecosystem.config.js ] && [ -f ecosystem.config.js.example ] && echo "files-on-disk OK" && grep -q REPLACE_WITH_GENERATED_SECRET ecosystem.config.js.example && ! grep -q "$OLD" ecosystem.config.js.example && ! grep -q "$OLD" ecosystem.config.js && grep -qE 'SECRET_KEY: "[0-9a-f]{64}"' ecosystem.config.js && echo "secret-rotated OK" && [ "$(git ls-files tests | wc -l)" -eq 18 ] && echo "tests-still-tracked OK"</automated>
  </verify>
  <done>`.planning/`, `data_backup_pre_sqlite/`, `backups/`, `instance/`, корневые xlsx и `ecosystem.config.js` игнорируются; `.planning/` и `ecosystem.config.js` больше не отслеживаются, но лежат на диске; `ecosystem.config.js.example` закоммичен-готов с плейсхолдером; в локальном `ecosystem.config.js` новый 64-hex секрет, старого значения нет ни в одном файле рабочей директории; тесты и шаблоны остались под контролем версий.</done>
</task>

<task type="auto">
  <name>Задача 2: Скрипт бэкапа данных и полный requirements.txt</name>
  <files>scripts/backup_data.sh, requirements.txt</files>
  <action>
Шаг 1 — создать `scripts/backup_data.sh` (GH-BACKUP), исполняемый (`chmod +x`). Требования к скрипту:

- shebang `#!/usr/bin/env bash`, `set -euo pipefail`;
- определить корень проекта относительно расположения самого скрипта (`dirname "${BASH_SOURCE[0]}"/..`), а не через `pwd` — скрипт должен работать при вызове из любой директории;
- каталог назначения: `${BACKUP_DIR:-$ROOT/backups}`, создать через `mkdir -p` (`backups/` уже в .gitignore после Задачи 1);
- имя архива: `face-data-<ГГГГММДД-ЧЧММСС>.tar.gz`;
- собирать пути `data/app.db`, `data/encodings.json`, `data/faces`, `data_backup_pre_sqlite` — каждый включать только если он существует (проверка `[ -e ... ]`), чтобы скрипт не падал на сервере, где старого дампа нет; если не набралось ни одного пути — выйти с ненулевым кодом и сообщением в stderr;
- упаковать `tar -czf` относительными путями от корня проекта, чтобы распаковка `tar -xzf ... -C /var/www/face-almgp33` восстанавливала структуру один-в-один;
- рядом положить контрольную сумму: `sha256sum "$ARCHIVE" > "$ARCHIVE.sha256"`;
- последней строкой напечатать полный путь к архиву — чтобы значение можно было подхватить в пайплайне;
- в шапке комментарий на русском: что попадает в архив и как восстанавливать.

Шаг 2 — выполнить скрипт один раз и убедиться, что архив создан (данных всего ~2.2 MB, отработает мгновенно).

Шаг 3 — переписать `requirements.txt` целиком (GH-DEPS). Сейчас там 6 строк, отсутствуют Flask, numpy, opencv, bcrypt, openpyxl, gunicorn и прочее — на чистом сервере проект не запустится. Записать полный runtime-набор с версиями, установленными в venv (значения уже сверены с `pip freeze`, повторная сверка допустима, но список финальный):

`Flask==3.1.3`, `Werkzeug==3.1.8`, `Jinja2==3.1.6`, `MarkupSafe==3.0.3`, `itsdangerous==2.2.0`, `click==8.4.1`, `blinker==1.9.0`, `Flask-SQLAlchemy==3.1.1`, `SQLAlchemy==2.0.50`, `greenlet==3.5.1`, `typing_extensions==4.15.0`, `Flask-Limiter==4.1.1`, `limits==5.8.0`, `Deprecated==1.3.1`, `wrapt==2.2.2`, `ordered-set==4.1.0`, `packaging==26.2`, `Flask-WTF==1.3.0`, `WTForms==3.2.2`, `bcrypt==5.0.0`, `opencv-contrib-python==4.13.0.92`, `numpy==2.4.6`, `dlib==20.0.1`, `face-recognition==1.3.0`, `face_recognition_models==0.3.0`, `pillow==12.2.0`, `openpyxl==3.1.5`, `et_xmlfile==2.0.0`, `gunicorn==26.0.0`.

Сгруппировать по назначению с русскими комментариями (`# Веб-фреймворк`, `# База данных`, `# Распознавание лиц`, `# Экспорт Excel`, `# Продакшн-сервер`). Тестовые зависимости (`pytest==9.0.3`) сюда НЕ включать — прод остаётся тонким, установка pytest будет отдельной строкой в DEPLOY.md. `cmake` в файл не добавлять: он нужен только на этапе сборки dlib и ставится системным пакетом до `pip install` — порядок установки внутри requirements.txt не гарантируется, поэтому опираться на него нельзя (это описывается в DEPLOY.md).
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && [ -x scripts/backup_data.sh ] && ARCHIVE=$(bash scripts/backup_data.sh | tail -1) && [ -f "$ARCHIVE" ] && [ -f "$ARCHIVE.sha256" ] && tar -tzf "$ARCHIVE" > /tmp/arc.txt && grep -q '^data/app.db$' /tmp/arc.txt && grep -q '^data/encodings.json$' /tmp/arc.txt && grep -q '^data/faces/' /tmp/arc.txt && grep -q '^data_backup_pre_sqlite/' /tmp/arc.txt && echo "backup OK" && git check-ignore -q backups && FREEZE=$(/var/www/sites/face-almgp33/venv/bin/pip freeze | tr 'A-Z_' 'a-z-') && MISSING=0 && while read -r line; do case "$line" in ''|'#'*) continue;; esac; norm=$(printf '%s' "$line" | tr 'A-Z_' 'a-z-'); echo "$FREEZE" | grep -qx "$norm" || { echo "NOT INSTALLED: $line"; MISSING=1; }; done < requirements.txt && [ "$MISSING" -eq 0 ] && echo "requirements pinned-to-venv OK" && for p in Flask opencv-contrib-python numpy bcrypt openpyxl gunicorn face-recognition Flask-SQLAlchemy itsdangerous; do grep -qi "^$p==" requirements.txt || { echo "MISSING DEP: $p"; exit 1; }; done && echo "core-deps OK"</automated>
  </verify>
  <done>`scripts/backup_data.sh` исполняем, запускается из любой директории, создаёт `backups/face-data-*.tar.gz` + `.sha256`, архив содержит `data/app.db`, `data/encodings.json`, `data/faces/` и `data_backup_pre_sqlite/`; `requirements.txt` содержит все runtime-зависимости, каждая версия совпадает с установленной в venv.</done>
</task>

<task type="auto">
  <name>Задача 3: DEPLOY.md, SECURITY-NOTES.md, README и коммит</name>
  <files>DEPLOY.md, SECURITY-NOTES.md, README.md</files>
  <action>
Шаг 1 — создать `SECURITY-NOTES.md` на русском (GH-SECRET). Содержание:

- **Проблема:** файл `ecosystem.config.js` отслеживался git и содержал боевой `SECRET_KEY` (64 hex) вместо плейсхолдера. Значение попало в историю репозитория. Пока push не сделан, утечка ограничена этой машиной.
- **Что уже сделано автоматически:** секрет заменён новым; `ecosystem.config.js` снят с отслеживания и добавлен в `.gitignore`; в репозиторий теперь идёт только `ecosystem.config.js.example` с плейсхолдером.
- **Шаг вручную №1 — применить новый секрет:** `pm2 restart face-recognition --update-env`, затем `pm2 describe face-recognition`. Явно предупредить: все активные сессии будут разлогинены. Дополнить последствиями, найденными при разведке `itsdangerous` в Задаче 1 (если секретом подписываются долгоживущие ссылки — перечислить, какие перестанут работать).
- **Шаг вручную №2 — вычистить секрет из истории ДО первого push.** Обязательно пометить как разрушительную операцию: меняются все SHA коммитов, откатить нельзя.
  - Сначала резервная копия всего репозитория: `cp -a /var/www/sites/face-almgp33 /var/www/sites/face-almgp33.bak-$(date +%F)`.
  - Установка: `pip install git-filter-repo` (в venv или глобально) либо `sudo apt install git-filter-repo` — сейчас инструмент не установлен.
  - Собрать список утёкших значений прямо из истории, не выписывая их в документ:
    `git log --all -p -- ecosystem.config.js | grep -oE '[0-9a-f]{64}' | sort -u > /tmp/leaked.txt`
    `sed 's/$/==>REDACTED/' /tmp/leaked.txt > /tmp/replacements.txt`
    `git filter-repo --replace-text /tmp/replacements.txt --force`
    Пояснить, зачем `--force`: git-filter-repo по умолчанию отказывается работать в репозитории, который не является свежим клоном.
  - Проверка результата: `git log --all -p | grep -oE '[0-9a-f]{64}' | sort -u` — должен вернуть пусто (или ничего похожего на секрет).
  - Предупредить: после filter-repo remote удаляется, `git remote add origin` нужно выполнить заново; `/tmp/leaked.txt` и `/tmp/replacements.txt` удалить.
- **Важно:** `.planning/` остаётся в старой истории (пользователь решил историю сохранить); новые коммиты его не содержат. Если позже потребуется убрать и его — тот же `git filter-repo --path .planning --invert-paths`.
- **В сам документ реальное значение секрета не вписывать** — иначе он утечёт заново уже через этот файл.

Шаг 2 — создать `DEPLOY.md` на русском (GH-DEPLOY). Разделы строго в этом порядке:

1. **Что НЕ хранится в Git** — `data/app.db`, `data/encodings.json`, `data/faces/`, `ecosystem.config.js`, `backups/`, `.planning/`, `venv/`; переносятся архивом из `scripts/backup_data.sh`.
2. **ШАГ 0 — обязательно перед первым push** — короткий блок со ссылкой на `SECURITY-NOTES.md`: пока история не вычищена, репозиторий публиковать нельзя.
3. **Создание репозитория и первый push (вручную)** — создать на github.com пустой репозиторий: без README, без .gitignore, без лицензии. Затем:
   `git remote add origin git@github.com:USER/REPO.git` (или https-вариант),
   `git push -u origin master`.
   Отметить, что текущая ветка называется `master`; если на GitHub нужен `main` — `git branch -M main && git push -u origin main`.
   Явно указать: эти команды executor не выполнял, их запускает владелец.
4. **Требования сервера** — Ubuntu 22.04/24.04, минимум 2 GB RAM (сборка dlib прожорлива; при 1 GB добавить swap — привести команды `fallocate`/`mkswap`/`swapon`). Python 3.14 отсутствует в стандартных репозиториях: `sudo add-apt-repository ppa:deadsnakes/ppa`, затем `python3.14 python3.14-venv python3.14-dev`. Отметить, что 3.12/3.13 тоже подходят.
5. **Системные пакеты:** `sudo apt update && sudo apt install -y git build-essential cmake pkg-config libopenblas-dev liblapack-dev libx11-dev libgtk-3-dev libjpeg-dev libpng-dev libgl1 libglib2.0-0`. Пояснить: `cmake` + `build-essential` нужны для сборки `dlib` из исходников, `libgl1`/`libglib2.0-0` — для колёс `opencv-contrib-python` на headless-сервере.
6. **Клонирование и venv:** директория `/var/www/face-almgp33` (тот же путь, что в `ecosystem.config.js.example`), `git clone`, `python3.14 -m venv venv`, `./venv/bin/pip install --upgrade pip wheel setuptools`, `./venv/bin/pip install -r requirements.txt`. Предупредить, что сборка dlib занимает несколько минут. Отдельной строкой: `./venv/bin/pip install pytest==9.0.3` — если нужно прогнать тесты (`./venv/bin/pytest`).
7. **Перенос данных:** на старом сервере `bash scripts/backup_data.sh`, скопировать архив (`scp`), на новом — проверить `sha256sum -c`, распаковать `tar -xzf face-data-*.tar.gz -C /var/www/face-almgp33`, проверить, что появились `data/app.db`, `data/encodings.json`, `data/faces/`. Отметить, что `data_backup_pre_sqlite/` в архиве — это старый дамп до миграции, для работы он не нужен и на новом сервере его можно удалить.
8. **Секрет и конфиг PM2:** `cp ecosystem.config.js.example ecosystem.config.js`, сгенерировать `./venv/bin/python -c "import secrets; print(secrets.token_hex(32))"`, вставить в `SECRET_KEY`, сверить пути внутри файла с реальными. Напомнить: файл в `.gitignore`, в репозиторий не попадёт.
9. **Запуск через PM2:** установка (`sudo apt install -y nodejs npm && sudo npm install -g pm2`), `pm2 start /var/www/face-almgp33/ecosystem.config.js`, `pm2 save`, `pm2 startup` (выполнить выданную команду), проверка `pm2 status` / `pm2 logs face-recognition`. Обновление после `git pull`: `pm2 restart face-recognition`. Отдельным предупреждением — `-w 1` менять нельзя: запись защищена advisory-блокировками `fcntl.flock`, которые не работают между несколькими воркерами gunicorn.
10. **Nginx + HTTPS:** конфиг реверс-прокси на `127.0.0.1:5051` с `proxy_set_header Host`, `X-Real-IP`, `X-Forwarded-For`, `X-Forwarded-Proto` (приложение использует `ProxyFix`), `client_max_body_size 20M` для загрузки фото. Отдельным блоком-предупреждением: киоск и страница регистрации используют камеру через `getUserMedia`, а браузеры дают доступ к камере только в защищённом контексте — **без HTTPS камера не заработает**; привести `sudo apt install certbot python3-certbot-nginx && sudo certbot --nginx -d example.com`.
11. **Проверка и типовые проблемы:** сервис отвечает (`curl -I http://127.0.0.1:5051/`); «камера не открывается» -> нет HTTPS; «ImportError: libGL.so.1» -> не установлен `libgl1`; «dlib падает при сборке» -> нет `cmake` или не хватает памяти; «сотрудники не распознаются» -> не восстановлен `data/encodings.json`.

Шаг 3 — актуализировать `README.md` (он существует, но не отслеживается — этим коммитом попадёт в репозиторий). Минимальная правка: исправить описание хранилища на SQLite (`data/app.db` + `data/encodings.json` + `data/faces/`) вместо JSON-файлов и заменить существующий раздел про запуск/деплой ссылкой на `DEPLOY.md` и `SECURITY-NOTES.md`. Остальной текст не переписывать.

Шаг 4 — коммит. Проиндексировать точечно: `git add .gitignore ecosystem.config.js.example scripts/backup_data.sh requirements.txt DEPLOY.md SECURITY-NOTES.md README.md`. Снятия с отслеживания из Задачи 1 уже находятся в индексе и войдут в этот же коммит. Коммитить без `-a`, чтобы не захватить постороннюю правку `templates/register_token.html` — она к этой задаче не относится. Сообщение: `chore: prepare repo for GitHub (gitignore, secret rotation, data backup, deploy docs)` с трейлером `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`. Push не делать. Если финальный шаг воркфлоу попытается добавить файлы из `.planning/` и получит «ignored by .gitignore» — это ожидаемо, продолжать без `-f`.
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && OLD=caf3e39dc0b37fca957176a65ef933de2b6929b0ef864910d7902e76ec661d8d && [ -f DEPLOY.md ] && [ -f SECURITY-NOTES.md ] && ! grep -q "$OLD" DEPLOY.md SECURITY-NOTES.md README.md && echo "no-secret-in-docs OK" && for t in "git filter-repo" "git-filter-repo" "pm2 restart face-recognition --update-env" "REDACTED"; do grep -qF "$t" SECURITY-NOTES.md || { echo "SECURITY-NOTES missing: $t"; exit 1; }; done && for t in "git remote add origin" "git push -u origin" "add-apt-repository ppa:deadsnakes/ppa" "build-essential" "cmake" "libgl1" "pip install -r requirements.txt" "scripts/backup_data.sh" "tar -xzf" "ecosystem.config.js.example" "token_hex(32)" "pm2 save" "pm2 startup" "pm2 restart face-recognition" "127.0.0.1:5051" "X-Forwarded-Proto" "certbot" "SECURITY-NOTES.md"; do grep -qF "$t" DEPLOY.md || { echo "DEPLOY.md missing: $t"; exit 1; }; done && echo "deploy-sections OK" && [ "$(grep -vc '^$' DEPLOY.md)" -ge 100 ] && grep -qiE 'https|getUserMedia|камер' DEPLOY.md && echo "https-camera-warning OK" && [ -z "$(git remote -v)" ] && git cat-file -e e7d5621^{commit} && git cat-file -e c329f93^{commit} && echo "history-intact + no-remote OK" && git ls-files --error-unmatch DEPLOY.md SECURITY-NOTES.md README.md requirements.txt scripts/backup_data.sh ecosystem.config.js.example > /dev/null && [ "$(git ls-files .planning | wc -l)" -eq 0 ] && [ "$(git ls-files ecosystem.config.js | wc -l)" -eq 0 ] && echo "commit-contents OK" && [ -n "$(git status --porcelain templates/register_token.html)" ] && echo "unrelated-change-untouched OK"</automated>
  </verify>
  <done>`DEPLOY.md` и `SECURITY-NOTES.md` написаны на русском, содержат все обязательные разделы и точные копипастабельные команды, но не содержат утёкшего секрета; README актуализирован и добавлен в репозиторий; всё закоммичено одним коммитом вместе со снятием `.planning/` и `ecosystem.config.js` с отслеживания; remote не создан, push не сделан, коммиты `e7d5621` и `c329f93` на месте — история не переписана; посторонняя правка `templates/register_token.html` осталась незакоммиченной.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> публичный GitHub | всё, что попадает в коммит или историю, становится доступно третьим лицам |
| git working tree -> git index | untracked-мусор с ПДн может быть случайно захвачен командой `git add -A` |
| архив данных -> канал передачи (scp) | архив содержит БД, фото лиц и хеши паролей |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-cy3-01 | Information Disclosure | `ecosystem.config.js` в истории git | mitigate | снять с отслеживания + `.gitignore` + ротация секрета (Задача 1); чистка истории через `git filter-repo` документирована как ручной шаг в SECURITY-NOTES.md |
| T-cy3-02 | Information Disclosure | `data/faces/`, `data/app.db`, `data_backup_pre_sqlite/`, корневые `*.xlsx` (ПДн сотрудников) | mitigate | правила `.gitignore` + автоматическая проверка `git check-ignore` в Задаче 1 |
| T-cy3-03 | Information Disclosure | текст SECURITY-NOTES.md | mitigate | секрет извлекается из истории командой на месте, в документ не вписывается; проверка `! grep -q "$OLD"` в Задаче 3 |
| T-cy3-04 | Elevation of Privilege | `data_backup_pre_sqlite/users.json` (права 600, хеши паролей) | mitigate | каталог в `.gitignore`, попадает только в локальный архив, который тоже игнорируется |
| T-cy3-05 | Denial of Service | необратимая перезапись истории (`git filter-repo`) | mitigate | executor не выполняет; документируется как ручной шаг с обязательной резервной копией репозитория |
| T-cy3-06 | Tampering | утечка нового секрета через `ecosystem.config.js` в будущем | mitigate | файл в `.gitignore`, в репозитории только `.example` с плейсхолдером |
| T-cy3-07 | Repudiation | внезапный `pm2 restart` разлогинивает активных пользователей | accept | рестарт вынесен в ручной шаг, момент выбирает владелец системы |
| T-cy3-SC | Tampering | новых пакетов не устанавливается | accept | план не добавляет зависимостей; `requirements.txt` только фиксирует версии, уже установленные и работающие в venv |
</threat_model>

<verification>
```bash
cd /var/www/sites/face-almgp33

# 1. Симуляция того, что увидит GitHub: ни ПДн, ни секретов, ни мусора
git ls-files | grep -E '^(data/|backups/|\.planning/|\.claude/|instance/|venv/)|\.xlsx$|^ecosystem\.config\.js$' && echo "ПРОВАЛ: мусор всё ещё отслеживается" || echo "OK: индекс чист"

# 2. Секрет отсутствует во всех отслеживаемых файлах
git grep -lI 'caf3e39dc0b37fca957176a65ef933de2b6929b0ef864910d7902e76ec661d8d' -- . && echo "ПРОВАЛ" || echo "OK: секрета в рабочем дереве нет"

# 3. Локальный запуск не сломан
[ -f ecosystem.config.js ] && [ -d .planning ] && [ -f data/app.db ] && echo "OK: локальное окружение цело"

# 4. Тесты по-прежнему проходят (структура репозитория не тронута)
./venv/bin/pytest -q 2>&1 | tail -3

# 5. История не переписана, push не сделан
git remote -v | wc -l   # ожидается 0
git rev-list --count HEAD  # ожидается 390 (389 + 1 новый коммит)
```
</verification>

<success_criteria>
- `git ls-files` не содержит ни одного файла из `data/`, `backups/`, `.planning/`, `.claude/`, `instance/`, ни одного `*.xlsx`, ни `ecosystem.config.js`
- Утёкшее значение секрета не встречается ни в одном отслеживаемом файле рабочего дерева
- `ecosystem.config.js` и `.planning/` остались на диске; PM2-процесс продолжает работать без изменений
- `bash scripts/backup_data.sh` создаёт проверяемый архив со всеми рантайм-данными
- `./venv/bin/pip install -r requirements.txt` устанавливает полный набор зависимостей приложения (версии совпадают с текущим venv)
- `DEPLOY.md` даёт русскую пошаговую инструкцию от пустого сервера Ubuntu до работающего сервиса за nginx с HTTPS
- `SECURITY-NOTES.md` даёт точные команды ротации секрета и чистки истории, не раскрывая сам секрет
- Remote не настроен, push не выполнен, коммиты `e7d5621` и `c329f93` существуют — история не тронута
- `./venv/bin/pytest` проходит так же, как до изменений
</success_criteria>

<output>
Create `.planning/quick/260821-cy3-prepare-repo-for-github-add-gitignore-fo/260821-cy3-SUMMARY.md` when done.

Примечание: после Задачи 1 каталог `.planning/` игнорируется git, поэтому SUMMARY останется на диске, но не будет закоммичен — это ожидаемое поведение, а не ошибка.
</output>
