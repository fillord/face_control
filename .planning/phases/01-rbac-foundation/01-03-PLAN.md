---
phase: 01-rbac-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - app.py
  - templates/admin.html
autonomous: true
requirements: [AUTH-05, DASH-03]
must_haves:
  truths:
    - "Every previously-unprotected non-kiosk route now requires an authenticated, active, authorized session (AUTH-05)"
    - "Kiosk routes GET /, POST /api/recognize, POST /api/detect remain undecorated and public (AUTH-05)"
    - "The legacy @login_required decorator is retired — no route uses it (D-04)"
    - "admin.html nav links are role-gated via Jinja2 {% if session.role in [...] %} and the user badge shows the real username (DASH-03)"
  artifacts:
    - path: "app.py"
      provides: "All page+API routes decorated with @require_role; login_required removed"
      contains: "@require_role"
    - path: "templates/admin.html"
      provides: "Role-gated navigation and dynamic user badge"
      contains: "session.role"
  key_links:
    - from: "app.py protected routes"
      to: "require_role decorator"
      via: "decorator applied above each non-kiosk handler"
      pattern: "@require_role\\("
    - from: "templates/admin.html nav"
      to: "session.role"
      via: "Jinja2 conditional rendering"
      pattern: "session.role in"
---

<objective>
Lock down the currently-unauthenticated API surface and retire the legacy decorator. This is the security-hardening slice: after this plan, an unauthenticated `curl` to any employee/attendance/stats endpoint redirects to `/login`, while the kiosk remains fully public.

Purpose: RESEARCH Pitfall 1 confirmed 8 API routes (`GET/POST /api/employees`, `DELETE /api/employees/<id>`, `POST /api/employees/<id>/reset`, `POST /api/register_face`, `GET /api/attendance`, `GET /api/attendance/dates`, `GET /api/stats`) plus `/register` and `/admin` page routes currently have no auth at all. Per D-04 all are upgraded to `@require_role(...)` and `@login_required` is removed entirely. The kiosk trio stays undecorated. Nav links in admin.html are role-gated (DASH-03 defense-in-depth).
Output: All non-kiosk routes decorated; `login_required` deleted from app.py; admin.html nav role-gated with dynamic user badge.
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rbac-foundation/01-CONTEXT.md
@.planning/phases/01-rbac-foundation/01-RESEARCH.md
@.planning/phases/01-rbac-foundation/01-PATTERNS.md
@.planning/phases/01-rbac-foundation/01-UI-SPEC.md
@app.py
@templates/admin.html
</context>

## Artifacts this phase produces

See `01-01-PLAN.md` "Artifacts this phase produces". This plan creates NO new symbols — it applies the `@require_role` decorator (from 01-02) to existing routes, removes `login_required`, and edits `admin.html` nav. Removed symbol: `login_required` decorator and the `session["logged_in"]`/`session["username"]` writes in login_page (already replaced in 01-02).

<tasks>

<task type="auto">
  <name>Task 1: Apply @require_role to all non-kiosk routes and retire @login_required</name>
  <files>app.py</files>
  <read_first>
    - app.py (FULL file — must enumerate every @app.route to classify each as protected vs public; route inventory below)
    - app.py lines 42-48 (login_required decorator to delete)
    - app.py lines 159-167 (register_page, admin_page — currently @login_required)
    - app.py lines 169-217 (employee API routes), lines 219-241 (register_face), lines 329-414 (attendance/stats routes) — currently NO decorator
    - app.py lines 131-134 (GET / kiosk), 243-260 (POST /api/detect), 264-327 (POST /api/recognize) — MUST stay public
    - .planning/phases/01-rbac-foundation/01-PATTERNS.md ("existing @login_required routes upgraded to @require_role" section — exact role tuples)
    - .planning/phases/01-rbac-foundation/01-CONTEXT.md (D-04 retire login_required; D-05 decorator behavior)
    - .planning/phases/01-rbac-foundation/01-RESEARCH.md (Pitfall 1 list of 8 unprotected routes; Anti-Patterns: never leave API routes unauthenticated)
  </read_first>
  <action>
    Decorate the following routes with `@require_role("superadmin", "org_admin", "dept_admin")` (the admin-tier roles — viewer/employee have no access to employee/attendance management in Phase 1):
      - `/register` (register_page) — replace existing `@login_required`
      - `/admin` (admin_page) — replace existing `@login_required`
      - `GET /api/employees` (get_employees) — add (currently none)
      - `POST /api/employees` (add_employee) — add
      - `DELETE /api/employees/<emp_id>` (delete_employee) — add
      - `POST /api/employees/<emp_id>/reset` (reset_employee_face) — add
      - `POST /api/register_face` (register_face) — add
      - `GET /api/attendance` (get_attendance) — add
      - `GET /api/attendance/dates` (get_dates) — add
      - `GET /api/stats` (get_stats) — add
    The decorator goes BETWEEN the `@app.route(...)` line and the `def` line (same placement pattern the old `@login_required` used at lines 160/165).
    Leave `/dashboard` and `/profile` as-is (dashboard added in 01-02 with `@require_role()`; profile added in 01-04).
    Do NOT decorate the kiosk routes: `GET /` (kiosk, line 131), `POST /api/recognize` (recognize, line 264), `POST /api/detect` (detect_face_only, line 243). They remain permanently public per AUTH-05. Also leave `/login` and `/logout` undecorated.
    DELETE the `login_required` function definition entirely (app.py lines 42-48) since no route references it anymore (D-04: decorator retired). After deletion, grep must find zero occurrences of `login_required` in app.py.
    No type hints; preserve existing handler bodies unchanged — this task only adds/removes decorators and deletes one function.
  </action>
  <verify>
    <automated>/var/www/sites/face-almgp33/venv/bin/pytest tests/test_rbac.py::test_unauthenticated_redirect tests/test_rbac.py::test_public_routes -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "login_required" app.py` returns 0 (legacy decorator and all references removed)
    - `grep -c "@require_role" app.py` returns >= 10 (8 API routes + /register + /admin; /dashboard from 01-02 also counts)
    - `pytest tests/test_rbac.py::test_unauthenticated_redirect -x` PASSES (GET /admin without session → 302 /login)
    - `pytest tests/test_rbac.py::test_public_routes -x` PASSES (GET /, /api/recognize, /api/detect never redirect to /login)
    - Decorator placement check: the lines immediately following `@app.route("/api/employees", methods=["GET"])`, `@app.route("/api/stats", methods=["GET"])`, and `@app.route("/api/register_face", methods=["POST"])` each contain `@require_role(`
    - The three kiosk handlers (`def kiosk`, `def recognize`, `def detect_face_only`) have NO `@require_role` on the line above their `def`
    - `pytest tests/ -q` exits 0 (full suite still collects and the public/redirect tests are green; remaining xfails unchanged)
  </acceptance_criteria>
  <done>All 10 non-kiosk routes are role-protected; kiosk trio stays public; login_required is fully removed; unauthenticated-redirect and public-route tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Role-gate admin.html navigation and show real username</name>
  <files>templates/admin.html</files>
  <read_first>
    - templates/admin.html lines 1-120 (header block ~66-79 with logo + user badge "Администратор" + logout; nav-tabs ~76-79 with Журнал/Статистика tabs; tab CSS classes ~19-21)
    - .planning/phases/01-rbac-foundation/01-PATTERNS.md (admin.html header and nav upgrade section — exact lines and replacements)
    - .planning/phases/01-rbac-foundation/01-UI-SPEC.md (Role-scoped navigation table: which tabs visible to which roles; Role Display Names; user badge)
    - .planning/phases/01-rbac-foundation/01-CONTEXT.md (Claude's Discretion: nav visibility via {% if session.role in [...] %})
  </read_first>
  <action>
    In `templates/admin.html` header (~line 68), replace the static `<span class="user-badge">Администратор</span>` text with the real username. Since the session no longer stores `username` (replaced in 01-02), pass a `username` (and optional `role_label`) variable from `admin_page()` in app.py — update `admin_page()` to look up `load_users().get(session["user_id"], {}).get("username", "")` and pass it to `render_template("admin.html", username=...)`. Render the badge as `{{ username }}`. (This is the one app.py touch in this task; keep it minimal.)
    Wrap the existing nav tabs (`Журнал посещаемости`, `Статистика за месяц`) and add a `Пользователи` tab placeholder, all gated by `{% if session.role in ['superadmin', 'org_admin', 'dept_admin'] %}` per the UI-SPEC Role-scoped navigation table. The "Пользователи" tab itself (`<span class="tab" id="tabUsers" onclick="switchTab('users')">Пользователи</span>`) is added here but its panel content is built in plan 01-04 — add only the tab trigger now, gated by the same role condition.
    Keep the existing tab CSS classes (`.tab`, `.tab.active`, `.tab:hover:not(.active)`) — no new CSS. The "← Киоск" and "Выйти" links stay visible to all (no gating).
    Do not alter the existing JS `switchTab` behavior; the users panel wiring is plan 01-04's job. Russian copy exactly per the Copywriting Contract.
  </action>
  <verify>
    <automated>grep -c "session.role in" templates/admin.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "session.role in \['superadmin', 'org_admin', 'dept_admin'\]" templates/admin.html` >= 1
    - `grep -c "Администратор</span>" templates/admin.html` returns 0 (static badge text replaced)
    - `grep -c "{{ username }}" templates/admin.html` >= 1
    - `grep -c "tabUsers" templates/admin.html` >= 1 (Пользователи tab trigger present)
    - `grep -n "render_template(\"admin.html\", username=" app.py` matches (admin_page passes username)
    - `grep -c "Журнал посещаемости" templates/admin.html` >= 1 (existing tab preserved)
    - `pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>admin.html nav tabs are role-gated, the user badge shows the logged-in username, and the Пользователи tab trigger is present (panel built in 01-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external client (curl) → /api/* | Previously unauthenticated API surface; primary attack target |
| browser → /admin nav | Client-side nav hiding is defense-in-depth only; server decorator is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-API | Spoofing | 8 unprotected /api/* routes | mitigate | Apply `@require_role("superadmin","org_admin","dept_admin")` to every employee/attendance/stats route; verified by test_unauthenticated_redirect |
| T-01-03-LR | Elevation of Privilege | leftover @login_required path | mitigate | Delete login_required entirely so no route silently keeps the weaker check (D-04); grep gate enforces 0 occurrences |
| T-01-03-NAV | Information Disclosure | client-only nav gating | accept | Jinja nav gating is supplemental; server-side `@require_role` is the authoritative control — nav hiding alone is never relied upon |
| T-01-03-KIOSK | Denial of Service | accidentally decorating kiosk | mitigate | Explicit acceptance check asserts kiosk handlers have NO decorator; kiosk must stay public for attendance to function |
</threat_model>

<verification>
- `/var/www/sites/face-almgp33/venv/bin/pytest tests/test_rbac.py -q` green for redirect + public-route tests
- Manual: `curl -s -o /dev/null -w "%{http_code} %{redirect_url}" http://localhost:5051/api/employees` returns 302 → /login (after deploy); `curl http://localhost:5051/` returns 200
- `grep -c login_required app.py` == 0
</verification>

<success_criteria>
Every non-kiosk route is server-side protected by @require_role; the kiosk trio stays public; @login_required is gone; admin.html nav is role-gated and shows the real username. AUTH-05 is satisfied for the existing route surface.
</success_criteria>

<output>
Create `.planning/phases/01-rbac-foundation/01-03-SUMMARY.md` when done.
</output>
