---
phase: 10-superadmin-panel-extension
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - app.py
  - templates/superadmin.html
  - templates/base.html
  - tests/test_sadm06.py
autonomous: true
requirements: [SADM-06]
must_haves:
  truths:
    - "GET /api/superadmin/attendance_stats?days=30 returns one entry per day with date, total_employees, present_count, percent"
    - "The days parameter is clamped to a sane maximum (90)"
    - "percent equals present_count divided by total employees times 100, rounded"
    - "The Analytics tab renders a Chart.js line chart of percent attendance per day over the last 30 days"
    - "The chart re-renders cleanly when the tab is reopened (no duplicate canvas instances)"
    - "GET /api/superadmin/attendance_stats returns 403 for any non-superadmin role"
  artifacts:
    - path: "app.py"
      provides: "GET /api/superadmin/attendance_stats aggregation endpoint"
      contains: "attendance_stats"
    - path: "templates/superadmin.html"
      provides: "panelAnalytics tab, Chart.js CDN, line chart render"
      contains: "panelAnalytics"
    - path: "templates/base.html"
      provides: "Аналитика nav link"
      contains: "/superadmin/analytics"
  key_links:
    - from: "templates/superadmin.html loadAnalytics()"
      to: "GET /api/superadmin/attendance_stats"
      via: "fetch in switchTab lazy-load then Chart render"
      pattern: "attendance_stats"
    - from: "templates/superadmin.html"
      to: "Chart.js CDN"
      via: "script tag in head block"
      pattern: "chart.umd.min.js"
---

<objective>
Deliver SADM-06 (D-06): a system-wide attendance analytics chart. Add GET /api/superadmin/attendance_stats returning percent attendance per day for the last N days across all orgs, and a new Analytics tab that renders it as a Chart.js line chart.

Purpose: Superadmins need an at-a-glance trend of overall attendance health across the whole system.
Output: One aggregation endpoint, a panelAnalytics tab with a Chart.js line chart (CDN already used elsewhere), a base.html nav link, and a pytest file.
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-superadmin-panel-extension/10-CONTEXT.md
@.planning/phases/10-superadmin-panel-extension/10-RESEARCH.md
@.planning/phases/10-superadmin-panel-extension/10-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (new symbols introduced by this plan)

- superadmin_attendance_stats — Flask route handler for GET /api/superadmin/attendance_stats (app.py)
- panelAnalytics — superadmin.html tab div id
- analyticsChart — canvas element id (superadmin.html)
- loadAnalytics — superadmin.html JS function
- analyticsLoaded — superadmin.html JS state flag
- analyticsChartInst, analyticsCtx — Chart.js instance and context variables (uniquely named to avoid ctx collision)
- tests/test_sadm06.py — new pytest file
- Chart.js 4.4.0 CDN script tag added to superadmin.html head block
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Backend — attendance_stats aggregation endpoint + tests</name>
  <files>app.py, tests/test_sadm06.py</files>
  <read_first>
    - app.py the "API: Superadmin Extensions" section (from plan 10-01)
    - models.py AttendanceRecord (emp_id, date, check_in_time, check_out_time, event_type) and Employee
    - app.py top imports (confirm date, timedelta available; add if missing)
    - .planning/phases/10-superadmin-panel-extension/10-RESEARCH.md (SADM-06 section — aggregation approach, days clamp)
    - tests/conftest.py lines 194-245 (seed_employees, seed_attendance)
  </read_first>
  <action>
    Add superadmin_attendance_stats() at GET /api/superadmin/attendance_stats guarded by require_role superadmin. Read the days query param (default 30) and clamp it to a maximum of 90 and a minimum of 1. Compute total_employees as Employee.query.count(). For each of the last N days (oldest first, ending today) compute present_count as the count of AttendanceRecord rows for that ISO date where check_in_time is not null, and percent as present_count divided by total_employees times 100 rounded to one decimal (or 0 when there are no employees). Return a JSON list of objects with date, total_employees, present_count, percent (D-06).

    Create tests/test_sadm06.py with superadmin session injection and seeded employees/attendance: (a) GET /api/superadmin/attendance_stats?days=7 returns 200 and a list of length 7 whose items contain date, total_employees, present_count, percent; (b) on a day where a seeded employee has a check_in_time, percent is greater than 0; (c) days is clamped — GET with days=1000 returns at most 90 entries; (d) GET as org_admin returns 403.
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && python -m pytest tests/test_sadm06.py -x -q && python -c "import ast; ast.parse(open('app.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_sadm06.py passes all four tests
    - grep -q "def superadmin_attendance_stats" app.py succeeds
    - grep -q attendance_stats app.py succeeds
    - python ast.parse of app.py exits 0
  </acceptance_criteria>
  <done>The endpoint returns per-day attendance percentages across all orgs, clamps days to 90, and returns 403 for non-superadmins.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — Analytics tab, Chart.js line chart, nav link</name>
  <files>templates/superadmin.html, templates/base.html</files>
  <read_first>
    - templates/superadmin.html block head (line 4, currently empty), switchTab/init and state block (with prior plan additions)
    - templates/admin.html line 5 (Chart.js CDN tag) and lines 389-411 (destroy-then-create chart pattern)
    - .planning/phases/10-superadmin-panel-extension/10-PATTERNS.md (Chart.js analytics panel section — CDN tag, loadAnalytics with analyticsCtx/analyticsChartInst, canvas)
    - .planning/phases/10-superadmin-panel-extension/10-RESEARCH.md (Pitfall 5 — avoid ctx variable collision; recommendation to use a dedicated panelAnalytics tab)
    - templates/base.html superadmin nav block (with prior links)
  </read_first>
  <action>
    Add the Chart.js 4.4.0 CDN script tag (https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js) to the superadmin.html head block, matching the tag already used in admin.html. Add a panelAnalytics div with page-title "Аналитика" containing a canvas element id analyticsChart (height ~80). Add JS state analyticsLoaded false and analyticsChartInst null. Add loadAnalytics which fetches /api/superadmin/attendance_stats?days=30, maps the response into labels (date) and values (percent), destroys analyticsChartInst if it already exists, then creates a new Chart of type line on a uniquely named context analyticsCtx (from getElementById('analyticsChart').getContext('2d')), with a single dataset labeled "% присутствия", tension ~0.3, fill false, and y-axis scale fixed min 0 max 100 (D-06). Guard against empty data so no JS error is thrown when the array is empty (Claude's Discretion: empty-data handling). Extend switchTab to toggle panelAnalytics and lazy-load loadAnalytics guarded by analyticsLoaded. In templates/base.html add an Аналитика nav link (href /superadmin/analytics, icon 📈) after the Календарь link. Use analyticsCtx and analyticsChartInst names (not ctx) to avoid collision (Pitfall 5).
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && grep -q panelAnalytics templates/superadmin.html && grep -q analyticsChart templates/superadmin.html && grep -q chart.umd.min.js templates/superadmin.html && grep -q "/superadmin/analytics" templates/base.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/superadmin.html contains panelAnalytics and a canvas id analyticsChart
    - templates/superadmin.html head includes the chart.umd.min.js CDN tag
    - templates/superadmin.html defines function loadAnalytics using analyticsChartInst and analyticsCtx (no bare ctx collision)
    - switchTab toggles panelAnalytics with lazy-load guard
    - templates/base.html contains an anchor with href /superadmin/analytics
  </acceptance_criteria>
  <done>The Analytics tab renders a line chart of percent attendance per day for the last 30 days; reopening the tab re-renders without duplicate chart instances; nav link present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to /api/superadmin/attendance_stats | authenticated read of system-wide aggregated attendance |
| Chart.js CDN | third-party script loaded into the superadmin page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-15 | Information Disclosure / Elevation | attendance_stats endpoint | mitigate | require_role("superadmin") returns 403 for all other roles; verified by test (d) |
| T-10-16 | Denial of Service | days query param | mitigate | days clamped to max 90, min 1, preventing an unbounded day-by-day query loop |
| T-10-17 | Tampering | Chart.js from CDN | accept | Pinned to chart.umd.min.js@4.4.0 already used and trusted in admin.html across prior phases; same origin policy applies |
</threat_model>

<verification>
- Run python -m pytest tests/test_sadm06.py -x -q — all tests pass
- Run python -m pytest tests/ -q — no regressions
- grep confirms the endpoint, panelAnalytics, the Chart.js CDN tag, and the nav link
</verification>

<success_criteria>
- attendance_stats returns per-day system-wide percent attendance for the last N days, days clamped to 90 (SADM-06 / D-06)
- Analytics tab renders a Chart.js line chart that re-renders cleanly on reopen
- Endpoint returns 403 for non-superadmins
- No test regressions
</success_criteria>

<output>
Create .planning/phases/10-superadmin-panel-extension/10-04-SUMMARY.md when done.
</output>
