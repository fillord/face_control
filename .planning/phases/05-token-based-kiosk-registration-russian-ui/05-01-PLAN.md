---
phase: 05-token-based-kiosk-registration-russian-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrate.py
  - app.py
  - data/orgs.json
  - tests/test_migrate_tokens.py
  - tests/conftest.py
autonomous: false
requirements:
  - MIG-TOKEN-01
  - MIG-TOKEN-02
  - MIG-TOKEN-03
user_setup: []

must_haves:
  truths:
    - "Every org in orgs.json has org_token, reg_token, kiosk_pin (bcrypt), reg_pin (bcrypt), reg_token_expires, kiosk_display_name after migration"
    - "An existing plaintext kiosk_pin ('2386') is replaced by a bcrypt hash that verifies against '2386'"
    - "Re-running migrate.py does not change already-bcrypt PINs or already-set tokens (idempotent)"
    - "New orgs created via POST /api/orgs receive org_token, reg_token, and bcrypt default PINs automatically"
  artifacts:
    - path: "migrate.py"
      provides: "Phase 5 token+PIN migration extending Phase 2 migration"
      contains: "org_token"
    - path: "tests/test_migrate_tokens.py"
      provides: "Migration unit tests for token/PIN fields and idempotency"
      contains: "def test_migrate_adds_all_fields"
  key_links:
    - from: "migrate.py"
      to: "data/orgs.json"
      via: "_load_json / _save_json (existing Phase 2 helpers)"
      pattern: "_save_json\\(ORGS_FILE"
    - from: "app.py create_org"
      to: "org token/PIN fields"
      via: "secrets.token_hex + bcrypt.hashpw on org creation"
      pattern: "secrets\\.token_hex"
---

<objective>
Upgrade the organizations data model to carry token-based access fields and bcrypt-hashed PINs, write the Phase 5 migration that backfills and re-hashes existing org data, and update org creation in app.py so all new orgs are born with these fields. Also lay the Wave 0 test scaffold for migration.

Purpose: Every downstream route (token kiosk, token registration, org_admin settings) depends on these fields existing and PINs being bcrypt hashes. This plan is the foundation — nothing else in Phase 5 can run until orgs.json carries org_token/reg_token/kiosk_pin/reg_pin/reg_token_expires/kiosk_display_name.
Output: Extended migrate.py, a shared token/hash helper in app.py, updated create_org, and tests/test_migrate_tokens.py.
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-token-based-kiosk-registration-russian-ui/05-CONTEXT.md
@.planning/phases/05-token-based-kiosk-registration-russian-ui/05-RESEARCH.md
@CLAUDE.md
@migrate.py
@app.py
@data/orgs.json
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add token/PIN helpers and bootstrap new-org fields in app.py</name>
  <read_first>
    - app.py (lines 1-30 imports; load_orgs/save_orgs at 138-148; create_org at 495-511; bcrypt patterns at 43, 78, 406, 458)
    - .planning/phases/05-token-based-kiosk-registration-russian-ui/05-RESEARCH.md (Pattern 4 token generation; Code Examples bcrypt pattern)
    - CLAUDE.md (snake_case, 4-space indent, box-drawing section headers, no type hints)
  </read_first>
  <action>
    Add `import secrets` to the top-of-file import line in app.py (line 1 currently imports os, json, base64, time, shutil, uuid, tempfile, sys — append secrets). Do NOT add any third-party package; secrets is stdlib.

    Add two module-level helpers near the Orgs/Depts data helpers section (after save_orgs, around line 149), under a new box-drawing comment header `# ─── Org tokens / PIN helpers ───`:
    - `generate_unique_token(existing_tokens)` — loops `secrets.token_hex(4)` (produces 8 hex chars) until the result is not in the passed-in set, returns the token. Use per D-CONTEXT discretion: token_hex(4) per RESEARCH Pattern 4.
    - `hash_pin(pin)` — returns `bcrypt.hashpw(str(pin).encode(), bcrypt.gensalt()).decode()`.
    - `is_bcrypt_hash(value)` — returns `bool(value and str(value).startswith("$2b$"))` (RESEARCH Pitfall 1 detection pattern).

    Update `create_org` (line 495-511) so every newly created org dict includes, in addition to existing id/name/description/created_at:
    - `org_token`: generate_unique_token over the set of existing org_token values across all orgs
    - `reg_token`: generate_unique_token over the set of existing reg_token values (compute org_token first, add it to a local seen-set so org_token != reg_token within the same org)
    - `kiosk_pin`: hash_pin("0000")
    - `reg_pin`: hash_pin("1234")
    - `reg_token_expires`: None
    - `kiosk_display_name`: the org `name` value

    Keep existing fields untouched. Do NOT change save_orgs (it already uses fcntl.flock).
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && grep -v '^#' app.py | grep -c "secrets.token_hex" | grep -qv '^0$' && grep -v '^#' app.py | grep -c "def hash_pin" | grep -qv '^0$' && echo HELPERS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `import secrets` present in app.py imports.
    - Functions `generate_unique_token`, `hash_pin`, `is_bcrypt_hash` defined at module level.
    - `create_org` writes org_token, reg_token, kiosk_pin (bcrypt), reg_pin (bcrypt), reg_token_expires=None, kiosk_display_name into the new org dict.
    - `python -c "import app"` imports without error (run with venv python: /var/www/sites/face-almgp33/venv/bin/python).
  </acceptance_criteria>
  <done>app.py exposes token/hash helpers and create_org produces fully-populated orgs; module imports cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Phase 5 migrate.py token/PIN migration + test_migrate_tokens.py</name>
  <read_first>
    - migrate.py (full file — _load_json, _save_json, run_migration, ORGS_FILE constant)
    - app.py (the helpers added in Task 1 — but migrate.py must NOT import app; replicate secrets/bcrypt logic locally to keep migrate.py standalone, matching Phase 2 migrate.py which does not import app)
    - data/orgs.json (current data: one org kiosk_pin=null, one org kiosk_pin="2386" plaintext)
    - .planning/phases/05-token-based-kiosk-registration-russian-ui/05-RESEARCH.md (Pitfall 1 plaintext detection; Validation Architecture MIG-TOKEN tests)
    - tests/conftest.py (seed_orgs helper, tmp_data fixture, monkeypatched ORGS_FILE)
  </read_first>
  <behavior>
    - Test test_migrate_adds_all_fields: after migration, every org has keys org_token (8 hex chars), reg_token (8 hex chars), kiosk_pin, reg_pin, reg_token_expires, kiosk_display_name. org_token and reg_token differ within one org.
    - Test test_migrate_rehashes_plaintext: an org seeded with kiosk_pin="2386" ends with a kiosk_pin starting "$2b$" that bcrypt.checkpw verifies against "2386".
    - Test test_migrate_hashes_null_default: an org seeded with kiosk_pin=None ends with a bcrypt hash verifying against "0000".
    - Test test_migrate_idempotent: running migration twice does not change org_token, reg_token, or already-bcrypt kiosk_pin/reg_pin values (capture values after first run, assert equal after second run).
    - Test test_migrate_sets_display_name: kiosk_display_name defaults to the org name when absent.
  </behavior>
  <action>
    Extend migrate.py (do NOT create a separate file — Phase 5 extends the existing Phase 2 migrate.py per RESEARCH "Recommended Project Structure"). Add `import secrets` and `import bcrypt` to migrate.py imports. Add a function `migrate_org_tokens(orgs)` that mutates the orgs dict in place:
    - Build a set of all existing org_token and reg_token values across orgs (for uniqueness).
    - For each org: if it lacks `org_token`, generate a unique 8-hex token (secrets.token_hex(4)) not in the seen set, add to seen set. Same for `reg_token`.
    - For `kiosk_pin`: if value is None/missing → hash "0000"; else if NOT starting with "$2b$" (plaintext, e.g. "2386") → re-hash that plaintext value; else leave unchanged. Use a local `_is_bcrypt(v)` helper checking startswith("$2b$").
    - For `reg_pin`: if value is None/missing → hash "1234"; else if not bcrypt → re-hash; else leave.
    - For `reg_token_expires`: if missing → set None (do not overwrite existing).
    - For `kiosk_display_name`: if missing/empty → set to org.get("name").
    - Print a per-org summary line stating which fields were added or re-hashed.

    Wire migrate_org_tokens into run_migration: after the existing Phase 2 org/dept setup, load orgs via _load_json(ORGS_FILE), call migrate_org_tokens(orgs), _save_json(ORGS_FILE, orgs). Preserve all existing Phase 2 behavior — do not remove or reorder employee/dept migration.

    Create tests/test_migrate_tokens.py implementing the five behaviors above. Import migrate module and call migrate_org_tokens directly on a dict (unit-level), AND seed_orgs + run the file-level migration where the test needs ORGS_FILE. Use bcrypt.checkpw to assert PIN correctness. Do NOT touch tests/conftest.py unless a new seed helper is strictly required (seed_orgs already exists).
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && venv/bin/python -m pytest tests/test_migrate_tokens.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_migrate_tokens.py exists with tests: test_migrate_adds_all_fields, test_migrate_rehashes_plaintext, test_migrate_hashes_null_default, test_migrate_idempotent, test_migrate_sets_display_name.
    - `venv/bin/python -m pytest tests/test_migrate_tokens.py -q` passes (all green, 0 failures).
    - migrate.py contains migrate_org_tokens and references secrets.token_hex(4) and bcrypt.
    - Re-running the migration produces byte-identical org_token/reg_token values (idempotency test green).
  </acceptance_criteria>
  <done>migrate.py backfills/re-hashes all token and PIN fields idempotently; migration test suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>migrate.py now adds org_token/reg_token/kiosk_pin(bcrypt)/reg_pin(bcrypt)/reg_token_expires/kiosk_display_name to real data/orgs.json, re-hashing the existing plaintext "2386" PIN. This checkpoint runs the migration against PRODUCTION data and asks the operator to confirm the result before routes are built on top of it.</what-built>
  <how-to-verify>
    1. Back up: `cp data/orgs.json data/orgs.json.pre05.bak`
    2. Run: `venv/bin/python migrate.py` — confirm it prints a summary with no traceback.
    3. Inspect: `cat data/orgs.json` — confirm BOTH orgs now have org_token (8 hex), reg_token (8 hex), kiosk_pin starting "$2b$", reg_pin starting "$2b$", reg_token_expires (null is fine), kiosk_display_name.
    4. Confirm the NurLab org's kiosk_pin verifies against "2386": `venv/bin/python -c "import json,bcrypt; o=json.load(open('data/orgs.json')); print([bcrypt.checkpw(b'2386', v['kiosk_pin'].encode()) for v in o.values() if v.get('name')=='NurLab'])"` → prints `[True]`.
    5. Run migrate.py a SECOND time — confirm tokens did not change (idempotent).
  </how-to-verify>
  <resume-signal>Type "approved" once orgs.json carries all six fields and the "2386" PIN verifies, or describe what looks wrong.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
- `migrate.py` — extended with migrate_org_tokens (token generation, plaintext PIN re-hashing, default PIN hashing, display name backfill, idempotent).
- `app.py` — generate_unique_token / hash_pin / is_bcrypt_hash helpers; create_org emits all token/PIN fields.
- `data/orgs.json` — migrated in place (production) with all six new fields per org.
- `tests/test_migrate_tokens.py` — migration unit/integration tests (MIG-TOKEN-01..03).
</artifacts_this_phase_produces>

<verification>
- `venv/bin/python -m pytest tests/test_migrate_tokens.py -q` green.
- `venv/bin/python -m pytest tests/ -q` shows no NEW failures introduced by app.py changes (existing suite still green).
- `data/orgs.json` carries org_token, reg_token, bcrypt kiosk_pin, bcrypt reg_pin, reg_token_expires, kiosk_display_name on every org.
</verification>

<success_criteria>
- All orgs migrated idempotently with bcrypt PINs and unique tokens.
- New orgs auto-provision token/PIN fields via create_org.
- Migration test suite green; no regressions in existing tests.
</success_criteria>

<output>
Create `.planning/phases/05-token-based-kiosk-registration-russian-ui/05-01-SUMMARY.md` when done.
</output>
