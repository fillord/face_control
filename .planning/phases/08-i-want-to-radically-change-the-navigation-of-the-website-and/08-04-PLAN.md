---
phase: 08-i-want-to-radically-change-the-navigation-of-the-website-and
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - templates/admin.html
  - templates/employee.html
  - templates/dashboard.html
autonomous: true
requirements: [D-09, D-10, D-11, D-14]
must_haves:
  truths:
    - "admin.html, employee.html, and dashboard.html each extend base.html"
    - "admin.html keeps its Журнал/Статистика panel switching via switchTab()"
    - "employee.html and dashboard.html (no tabs) render their content directly in the content area"
    - "No #1565C0 or #0d47a1 blue remains in any of the three files"
  artifacts:
    - path: "templates/admin.html"
      provides: "hr_viewer journal page converted to base.html child"
      contains: "{% extends 'base.html' %}"
    - path: "templates/employee.html"
      provides: "Employee cabinet converted to base.html child"
      contains: "{% extends 'base.html' %}"
    - path: "templates/dashboard.html"
      provides: "Dashboard landing converted to base.html child"
      contains: "{% extends 'base.html' %}"
  key_links:
    - from: "templates/admin.html"
      to: "templates/base.html"
      via: "{% extends %} + {% block content %}"
      pattern: "extends 'base.html'"
    - from: "templates/employee.html"
      to: "templates/base.html"
      via: "{% extends %} + {% block content %}"
      pattern: "extends 'base.html'"
---

<objective>
Convert `templates/admin.html` (tabbed: Журнал/Статистика), `templates/employee.html` (single-page cabinet), and `templates/dashboard.html` (minimal landing) to extend `base.html`. admin.html follows the tabbed-page recipe (plan 02); employee.html and dashboard.html follow the simpler single-page recipe — no switchTab needed (D-09, D-10, D-11, D-14).

Purpose: Migrate the hr_viewer journal, the employee self-service cabinet, and the dashboard landing to the new shell.
Output: `templates/admin.html`, `templates/employee.html`, `templates/dashboard.html` (all modified).
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-PATTERNS.md
@templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert admin.html (hr_viewer journal) to extend base.html</name>
  <read_first>
    - templates/admin.html (the ENTIRE current file — head, style, header, nav-tabs: Журнал/Статистика + conditional Пользователи, panels, switchTab JS, and any {% include 'reports_partial.html' %})
    - templates/base.html (shared CSS classes that must NOT be redefined)
    - .planning/phases/08-.../08-PATTERNS.md (admin.html note on session.role conditional tab, lines 374–381 + switchTab adaptation)
  </read_first>
  <action>
Rewrite `templates/admin.html` to begin with `{% extends 'base.html' %}` and `{% block title %}Журнал — МедКонтроль{% endblock %}`.

Apply the tabbed-page recipe (same as pilot plan 02):
  1. Remove document shell, `<header>`, and `<div class="nav-tabs">` bar.
  2. Remove CSS reset + `body` + shared component CSS now in base.html (note: `.badge*` are now in base.html — remove the local copies).
  3. admin-specific CSS (journal-row styling, stats-panel layout) → `{% block head %}<style>...</style>{% endblock %}` (or empty block).
  4. Wrap markup in `{% block content %}`: first element `<h1 class="page-title">Журнал посещаемости</h1>` (D-05), then existing panels (Журнал, Статистика, and the `{% if session.role == 'superadmin' %}`-guarded Пользователи panel) with `.hidden` toggling, then existing `<script>` blocks. Preserve any `{% include 'reports_partial.html' %}` exactly (updated in plan 06).
  5. switchTab() adaptation: drop dead `.tab` `classList.toggle('active', ...)` lines; KEEP the `{% if session.role == 'superadmin' %}` guard on the Пользователи PANEL div (research: panel conditional stays — sidebar already handles role nav visibility); keep panel `.hidden` toggles and the function name/signature.
  6. Color replacements: `#1565C0` → `var(--accent)`, `#0d47a1` → `var(--accent-hover)`, `rgba(21,101,192,...)` → `rgba(13,148,136,...)`, `.stat-val.blue` → `.stat-val.teal`.
  </action>
  <verify>
    <automated>SECRET_KEY=testkey venv/bin/python -c "from app import app; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 templates/admin.html` is exactly `{% extends 'base.html' %}`.
    - `grep -c "<!DOCTYPE" templates/admin.html` returns 0, `grep -c "<header" templates/admin.html` returns 0, `grep -c "class=\"nav-tabs\"" templates/admin.html` returns 0.
    - `grep -c "{% block content %}" templates/admin.html` returns ≥1 and `grep -c "page-title" templates/admin.html` returns ≥1.
    - `grep -c "session.role == 'superadmin'" templates/admin.html` returns ≥1 (Пользователи panel guard preserved).
    - `grep -vE '^\s*/?\*|^\s*//' templates/admin.html | grep -c "#1565C0\|#0d47a1"` returns 0.
    - `SECRET_KEY=testkey venv/bin/python -c "from app import app"` prints OK.
  </acceptance_criteria>
  <done>admin.html extends base.html, no shell/header/nav-tabs, content in {% block content %} with h1.page-title, switchTab() + superadmin-guarded Пользователи panel preserved, reports_partial include intact, no old blue. App imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Convert employee.html and dashboard.html (single-page, no tabs) to extend base.html</name>
  <read_first>
    - templates/employee.html (the ENTIRE current file — head, style, header, content grid/stats, JS; note it does NOT use switchTab and the route does NOT pass `username`)
    - templates/dashboard.html (the ENTIRE current file — minimal landing page)
    - templates/base.html (shared CSS classes; note `.page` max-width:1200px wrapper exists)
    - .planning/phases/08-.../08-PATTERNS.md (employee.html MODIFY section lines 384–401, dashboard.html lines 469–473)
  </read_first>
  <action>
Convert BOTH single-page templates. Neither uses in-page tab switching, so no switchTab is involved.

employee.html: begin with `{% extends 'base.html' %}` and `{% block title %}Мой табель — МедКонтроль{% endblock %}`.
  1. Remove document shell, `<header>`, and any nav-tabs.
  2. Remove CSS reset + `body` + shared component CSS now in base.html.
  3. employee-specific CSS — the T-13 grid / stats-card / time-tooltip styles unique to the cabinet — goes in `{% block head %}<style>...</style>{% endblock %}`. The employee grid can be wide, so wrap its table region in `<div style="overflow-x:auto;">` rather than the max-width `.page` wrapper (research Pitfall 6 — employee.html uses unrestricted width for its grid).
  4. Wrap markup in `{% block content %}`: first element `<h1 class="page-title">Мой табель</h1>` (D-05), then the existing stats cards + grid + JS. Do NOT reference `{{ username }}` for any sidebar purpose — the sidebar already reads session (research: employee route does not pass username).
  5. Color replacements as in Task 1.

dashboard.html: begin with `{% extends 'base.html' %}` and `{% block title %}Панель — МедКонтроль{% endblock %}`.
  1. Remove document shell, `<header>`, nav-tabs, CSS reset + body + shared CSS.
  2. Wrap surviving markup in `{% block content %}` inside a `<div class="page">`: first element `<h1 class="page-title">Панель</h1>` (D-05), then the existing (minimal) content.
  3. Color replacements as in Task 1.
  </action>
  <verify>
    <automated>SECRET_KEY=testkey venv/bin/python -c "from app import app; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 templates/employee.html` and `head -1 templates/dashboard.html` are each exactly `{% extends 'base.html' %}`.
    - For both files: `grep -c "<!DOCTYPE"` returns 0, `grep -c "<header"` returns 0, `grep -c "{% block content %}"` returns ≥1, `grep -c "page-title"` returns ≥1.
    - `grep -vE '^\s*/?\*|^\s*//' templates/employee.html templates/dashboard.html | grep -c "#1565C0\|#0d47a1"` returns 0.
    - employee.html contains `overflow-x:auto` (or `overflow-x: auto`) around its grid region (wide-table handling).
    - `SECRET_KEY=testkey venv/bin/python -c "from app import app"` prints OK.
  </acceptance_criteria>
  <done>employee.html and dashboard.html each extend base.html with content in {% block content %} and an h1.page-title; employee grid wrapped for horizontal scroll; no shell/header/old-blue. App imports cleanly.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

Modified files: `templates/admin.html`, `templates/employee.html`, `templates/dashboard.html` — all `base.html` children.

Symbols consumed from base.html: `{% block title %}`, `{% block head %}`, `{% block content %}`, `.page-title`, `.page`, `.stat-card`, `.table-card`, `.badge*`, `.hidden`.
Symbols retained: admin.html `switchTab(tab)` (panel-toggle only) + superadmin-guarded Пользователи panel + reports_partial include; employee.html stats/grid JS + `overflow-x:auto` wrapper; dashboard.html minimal content.
</artifacts_produced>

<verification>
- `SECRET_KEY=testkey venv/bin/python -c "from app import app; print('OK')"` — all three parse.
- Each file first line `{% extends 'base.html' %}`; no DOCTYPE/header.
- `grep -vE '^\s*/?\*|^\s*//' templates/admin.html templates/employee.html templates/dashboard.html | grep -c "#1565C0\|#0d47a1"` → 0.
</verification>

<success_criteria>
- All three pages extend base.html (D-10, D-14).
- admin.html panels switch via sidebar switchTab() with superadmin Пользователи guard intact (D-09).
- employee/dashboard render directly with no tabs; employee grid scrolls horizontally.
- No old blue; app imports without error.
</success_criteria>

<output>
Create `.planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-04-SUMMARY.md` when done.
</output>
