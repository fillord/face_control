---
phase: 10-superadmin-panel-extension
plan: 06
type: execute
wave: 6
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05"]
files_modified: []
autonomous: false
requirements: [SADM-01, SADM-02, SADM-03, SADM-04, SADM-05, SADM-06, SADM-07]
must_haves:
  truths:
    - "The full test suite passes after all six feature waves"
    - "pm2 restart face-recognition succeeds with no import errors or startup exceptions"
    - "All seven superadmin features are reachable and visually correct in the running app"
  artifacts: []
  key_links: []
---

<objective>
Final integration gate for Phase 10. Run the full automated test suite, restart the live PM2 process, confirm a clean startup (auto-creating the holiday_calendar table), then human-verify all seven new superadmin capabilities in the browser. This satisfies success criteria 6 and 7 (no regressions; pm2 restart succeeds).

Purpose: The seven features were built in isolated waves; this plan proves they integrate, the app boots cleanly with the new DB table, and each feature works end-to-end for a real superadmin.
Output: A passing test run, a clean pm2 restart, and a human-confirmed visual smoke of every feature.
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-superadmin-panel-extension/10-CONTEXT.md
</context>

<artifacts_produced>
## Artifacts this phase produces (new symbols introduced by this plan)

- None — this is a verification and checkpoint plan; it creates no new code symbols.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Full suite + clean startup verification</name>
  <files></files>
  <read_first>
    - .planning/phases/10-superadmin-panel-extension/10-RESEARCH.md (Validation Architecture — Req-to-Test map; Runtime State Inventory — db.create_all() creates holiday_calendar; PM2 restart)
    - .planning/phases/10-superadmin-panel-extension/10-01-SUMMARY.md through 10-05-SUMMARY.md (what each wave shipped)
  </read_first>
  <action>
    Run the entire pytest suite and confirm no regressions across all phases, including the five new Phase 10 test files. Then restart the live process with pm2 restart face-recognition and inspect the most recent pm2 logs to confirm there are no import errors (especially the HolidayCalendar import) and no startup exceptions, and that the holiday_calendar table is present after startup (db.create_all() creates it idempotently). Confirm the login page and a superadmin page respond over HTTP locally (the app listens on the configured port; per project memory it runs on port 5051). If any check fails, capture the failing output for the human checkpoint rather than attempting silent fixes.
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && python -m pytest tests/ -q && pm2 restart face-recognition && sleep 2 && pm2 logs face-recognition --lines 40 --nostream</automated>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/ -q reports no failures
    - pm2 restart face-recognition completes and the process status is online
    - pm2 logs show no ImportError, no traceback, and no SQLAlchemy OperationalError referencing holiday_calendar
    - curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5051/login returns 200
  </acceptance_criteria>
  <done>Full suite passes, the process restarts cleanly, the holiday_calendar table exists, and the app serves the login page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual smoke of all seven superadmin features</name>
  <what-built>
    All seven Phase 10 capabilities: superadmin role creation (org_admin/dept_admin/hr_viewer with scoped dept selector), Employees tab, Devices tab with Revoke, Logs tab with filters, Holiday Calendar CRUD, Attendance Analytics chart, and Global multi-org Excel export.
  </what-built>
  <how-to-verify>
    1. Log in as superadmin and open /superadmin. Confirm the sidebar shows the new links: Сотрудники, Устройства, Логи, Календарь, Аналитика (plus existing Организации, Пользователи, Система).
    2. Пользователи: open the create-user form, select role dept_admin, confirm a department dropdown appears scoped to the chosen org; create a dept_admin and an hr_viewer; confirm both succeed.
    3. Сотрудники: confirm the table lists employees across all orgs with org, dept, face status, date; use the org filter and confirm it narrows the list.
    4. Устройства: confirm devices list across orgs; click Revoke on a test device and confirm it disappears and an entry appears in /audit.
    5. Логи: confirm up to 500 recognition events show; apply the org filter and the event-type filter and confirm the list updates.
    6. Календарь: pick a year, add a holiday (date + name), confirm it appears; delete it, confirm it disappears. Optionally open the T-13 timesheet for a month containing an added holiday and confirm that date shows В.
    7. Аналитика: confirm a line chart renders percent attendance over the last 30 days with no browser console errors; switch away and back and confirm it re-renders.
    8. Система: pick a month and click Скачать (global export); confirm an .xlsx downloads and opens with one sheet per organization.
    9. Confirm a non-superadmin (e.g. org_admin) cannot reach the new /api/superadmin/* endpoints (gets 403) — spot check one in the browser dev tools or via curl.
  </how-to-verify>
  <resume-signal>Type "approved" if all seven features work, or describe any issues to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live PM2 process restart | new code and schema go live for real superadmin sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-23 | Denial of Service | app startup with new model | mitigate | Clean-startup check inspects pm2 logs for import/DB errors before declaring success; rollback is pm2 restart of prior code if startup fails |
| T-10-24 | Elevation | new endpoints reachable by non-superadmins | mitigate | Human checkpoint step 9 spot-checks a 403 for org_admin; per-feature plans already enforce require_role with tests |
</threat_model>

<verification>
- python -m pytest tests/ -q passes
- pm2 restart face-recognition succeeds with clean logs
- Human confirms all seven features in the browser
</verification>

<success_criteria>
- No test regressions across the suite (covers SADM-01 through SADM-07 behavior)
- pm2 restart face-recognition succeeds; no import errors or startup exceptions (success criterion 7)
- All seven superadmin features verified working by the human checkpoint (success criterion 6 spot-checked for 403)
</success_criteria>

<output>
Create .planning/phases/10-superadmin-panel-extension/10-06-SUMMARY.md when done.
</output>
