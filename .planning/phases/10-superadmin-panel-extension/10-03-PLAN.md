---
phase: 10-superadmin-panel-extension
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - models.py
  - app.py
  - templates/superadmin.html
  - templates/base.html
  - tests/test_sadm05.py
autonomous: true
requirements: [SADM-05]
must_haves:
  truths:
    - "A holiday_calendar table exists and is auto-created at app startup via db.create_all()"
    - "GET /api/holidays?year=YYYY returns the list of holidays for that year"
    - "POST /api/holidays adds a {date, name} holiday and writes an audit entry"
    - "DELETE /api/holidays/<date> removes a holiday and writes an audit entry"
    - "compute_symbol marks В on a date that exists in HolidayCalendar for that year, even if it is absent from the hardcoded KZ_HOLIDAYS"
    - "get_holidays_set falls back to KZ_HOLIDAYS when no DB rows exist for the year"
    - "The Calendar tab lets superadmin add and delete holidays with a year filter"
  artifacts:
    - path: "models.py"
      provides: "HolidayCalendar ORM model"
      contains: "class HolidayCalendar"
    - path: "app.py"
      provides: "HolidayCalendar import, DB-backed get_holidays_set, /api/holidays GET/POST/DELETE"
      contains: "api/holidays"
    - path: "templates/superadmin.html"
      provides: "panelCalendar tab with add form and delete actions"
      contains: "panelCalendar"
    - path: "templates/base.html"
      provides: "Календарь nav link"
      contains: "/superadmin/calendar"
  key_links:
    - from: "app.py compute_symbol"
      to: "get_holidays_set"
      via: "holidays_set passed per grid render"
      pattern: "get_holidays_set"
    - from: "app.py get_holidays_set"
      to: "HolidayCalendar table"
      via: "filter_by(year=year)"
      pattern: "HolidayCalendar.query"
---

<objective>
Deliver SADM-05 (D-05): a DB-backed working holiday calendar. Add a HolidayCalendar model, switch get_holidays_set() to read from the DB (falling back to the hardcoded KZ_HOLIDAYS when a year has no DB rows), add GET/POST/DELETE /api/holidays endpoints, and add a Calendar tab for superadmin CRUD with a year filter.

Purpose: KZ public holidays are currently hardcoded and cannot be edited without a code change. This makes the calendar editable at runtime while preserving the existing hardcoded list as a fallback, so timesheets remain correct for years with no DB entries.
Output: HolidayCalendar model, modified get_holidays_set(), three /api/holidays endpoints, panelCalendar tab, a base.html nav link, and a pytest file.
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-superadmin-panel-extension/10-CONTEXT.md
@.planning/phases/10-superadmin-panel-extension/10-RESEARCH.md
@.planning/phases/10-superadmin-panel-extension/10-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (new symbols introduced by this plan)

- HolidayCalendar — new ORM model (models.py) with fields id, date (unique), name, year (indexed)
- holiday_calendar — new SQLite table auto-created by db.create_all()
- list_holidays, add_holiday, delete_holiday — Flask route handlers for /api/holidays (app.py)
- HolidayCalendar added to the models import line in app.py
- get_holidays_set — existing function, body changed to query HolidayCalendar first then fall back to KZ_HOLIDAYS
- panelCalendar — superadmin.html tab div id
- loadHolidays, renderHolidays, addHoliday, deleteHoliday, onHolidayYearChange — superadmin.html JS functions
- allHolidays, calendarLoaded — superadmin.html JS state variables
- holidayYear, holidayDate, holidayName, holidaysTableBody, holidayFormError — superadmin.html element ids
- tests/test_sadm05.py — new pytest file
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Model + get_holidays_set DB-backed + import</name>
  <files>models.py, app.py</files>
  <read_first>
    - models.py lines 1-204 (imports at lines 13-15; AppSetting lines 177-184; AuditLog lines 189-204 as the closest analog)
    - app.py lines 20-21 (models import lines), lines 289-291 (get_holidays_set current body)
    - app.py callers of get_holidays_set: lines 424, 1186/1514, 1632 (signature must not change)
    - .planning/phases/10-superadmin-panel-extension/10-PATTERNS.md (HolidayCalendar model definition, get_holidays_set replacement)
    - .planning/phases/10-superadmin-panel-extension/10-RESEARCH.md (SADM-05 section, Pitfall 3 about app context, Pitfall 4 about import)
  </read_first>
  <action>
    In models.py add a HolidayCalendar model under a box-drawing section divider. Fields: id Integer primary key autoincrement; date String(10) unique not null (YYYY-MM-DD); name String(128) not null; year Integer not null indexed. Integer and String are already imported — add no new imports.

    In app.py add HolidayCalendar to the existing models import on line 21. Then change the body of get_holidays_set(year) (keep the signature exactly): query HolidayCalendar.query.filter_by(year=year).all(); if any rows exist, return the set of their date strings; otherwise return set(KZ_HOLIDAYS.get(year, [])) (D-05). This keeps all existing callers and the hardcoded fallback intact. Note (Pitfall 3): get_holidays_set now touches the DB, so any test that calls it directly must run inside an app context; existing compute_symbol tests that pass holidays_set directly are unaffected.

    The holiday_calendar table is created automatically by the existing db.create_all() at startup once HolidayCalendar is imported (Pitfall 4 — the import is what registers the model).
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && grep -q "class HolidayCalendar" models.py && grep -q "HolidayCalendar.query.filter_by(year=year)" app.py && python -c "import ast; ast.parse(open('app.py').read()); ast.parse(open('models.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - models.py contains class HolidayCalendar with date, name, year columns
    - app.py models import on line ~21 includes HolidayCalendar
    - get_holidays_set queries HolidayCalendar before falling back to KZ_HOLIDAYS
    - python ast.parse of app.py and models.py exits 0
  </acceptance_criteria>
  <done>HolidayCalendar model exists and is imported; get_holidays_set returns DB dates when present and the hardcoded list otherwise; signature unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Holiday CRUD endpoints + tests</name>
  <files>app.py, tests/test_sadm05.py</files>
  <read_first>
    - app.py the "API: Superadmin Extensions" section (from plan 10-01)
    - app.py lines 2000-2016 (db.session commit/rollback and write_audit patterns), lines 313-365 (compute_symbol — verify В comes from holidays_set)
    - .planning/phases/10-superadmin-panel-extension/10-PATTERNS.md (Audit logging, DB session commit/rollback, No-Analog date-format validation note)
    - .planning/phases/10-superadmin-panel-extension/10-RESEARCH.md (SADM-05 endpoints, V5 input validation — validate date format)
    - tests/conftest.py lines 35-245 and tests/test_timesheet.py (compute_symbol / holidays test patterns)
  </read_first>
  <action>
    Add three endpoints under require_role superadmin. GET /api/holidays reads the year query param (default current year), queries HolidayCalendar.query.filter_by(year=year) ordered by date, and returns a JSON list of objects with date and name. POST /api/holidays parses JSON body date and name; validate the date with datetime.strptime(date, "%Y-%m-%d") inside try/except ValueError returning 400 "Неверный формат даты" on failure; derive year from the parsed date; insert a HolidayCalendar row inside a try/except with db.session.rollback on error returning 500; on duplicate date (unique constraint) return 409 "Дата уже существует"; on success call write_audit action "holiday_add" target_type "holiday" target_id date new_value with name and date, then return the created record (D-05). DELETE /api/holidays/<date> deletes the matching row (404 if absent), commits with rollback-on-error, and calls write_audit action "holiday_delete" target_type "holiday" target_id date old_value with the deleted name; return a status object (D-05).

    Create tests/test_sadm05.py with superadmin session injection, run inside the app context where needed: (a) POST /api/holidays with a valid date and name returns success and the row is retrievable via GET /api/holidays for that year; (b) POST with an invalid date string returns 400; (c) POST a duplicate date returns 409; (d) DELETE /api/holidays/<date> removes it and a subsequent GET omits it; (e) after inserting a holiday for a year, get_holidays_set(year) returns a set containing that date (call inside app_context); (f) compute_symbol returns В for that holiday date given a work-day schedule and the holidays_set from get_holidays_set; (g) GET /api/holidays as org_admin returns 403.
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && python -m pytest tests/test_sadm05.py -x -q && python -c "import ast; ast.parse(open('app.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_sadm05.py passes all seven tests
    - grep -q "def add_holiday" app.py and grep -q "def delete_holiday" app.py and grep -q "def list_holidays" app.py all succeed
    - grep -q holiday_add app.py and grep -q holiday_delete app.py succeed (audit calls)
    - python ast.parse of app.py exits 0
  </acceptance_criteria>
  <done>Holidays can be listed, added (with date validation and dedup), and deleted, each write audited; compute_symbol marks В on DB holidays; endpoints 403 for non-superadmins.</done>
</task>

<task type="auto">
  <name>Task 3: Calendar tab UI + nav link</name>
  <files>templates/superadmin.html, templates/base.html</files>
  <read_first>
    - templates/superadmin.html switchTab/init and state block (with plan 10-01/10-02 additions), createUser POST pattern (471-498), deleteOrg DELETE pattern (399-413), form-card pattern (104-131), escapeHtml (564-571)
    - .planning/phases/10-superadmin-panel-extension/10-PATTERNS.md (panel divs, form card, POST action pattern, DELETE action pattern, switchTab lazy-load, JS error element show/hide)
    - templates/base.html superadmin nav block (with prior links)
  </read_first>
  <action>
    Add a panelCalendar div with page-title "Календарь", a toolbar containing a year select with id holidayYear (current year default, a small range such as current year minus 1 through current year plus 2) calling onHolidayYearChange onchange, an add form-card with a date input id holidayDate (type date), a text input id holidayName, an error element id holidayFormError, and an "Добавить" button calling addHoliday; and a table-card with headers Дата, Название, Действие and tbody id holidaysTableBody. Add JS state allHolidays and calendarLoaded. Add loadHolidays which fetches /api/holidays for the selected holidayYear into allHolidays then calls renderHolidays. Add renderHolidays which renders rows with escapeHtml and a Delete button per row calling deleteHoliday with the row date; empty-state colspan row "Нет данных". Add addHoliday following the createUser POST pattern (read holidayDate and holidayName, POST /api/holidays, show holidayFormError on failure, reload on success). Add deleteHoliday(date) following the deleteOrg DELETE pattern (confirm, DELETE /api/holidays/<date>, reload). Add onHolidayYearChange which reloads holidays for the chosen year (D-05). Extend switchTab to toggle panelCalendar and lazy-load loadHolidays guarded by calendarLoaded. In templates/base.html add a Календарь nav link (href /superadmin/calendar, icon 📅) after the Логи link.
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && grep -q panelCalendar templates/superadmin.html && grep -q "function addHoliday" templates/superadmin.html && grep -q "function deleteHoliday" templates/superadmin.html && grep -q "/superadmin/calendar" templates/base.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/superadmin.html contains panelCalendar, holidayYear, holidayDate, holidayName, holidaysTableBody
    - templates/superadmin.html defines function loadHolidays, addHoliday, deleteHoliday, onHolidayYearChange
    - switchTab toggles panelCalendar with lazy-load guard
    - templates/base.html contains an anchor with href /superadmin/calendar
  </acceptance_criteria>
  <done>The Calendar tab lists holidays for the selected year, adds them via the form (with validation errors surfaced), and deletes them; nav link present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to /api/holidays POST/DELETE | authenticated write of date+name into the holiday table |
| holiday_calendar table to compute_symbol | DB holidays drive timesheet В marking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering | POST /api/holidays date param | mitigate | datetime.strptime validates YYYY-MM-DD before insert; unique constraint prevents duplicate dates (returns 409) |
| T-10-11 | Information Disclosure / Elevation | holiday endpoints | mitigate | require_role("superadmin") returns 403 for all other roles; verified by test (g) |
| T-10-12 | Tampering | holiday name rendering | mitigate | escapeHtml applied before innerHTML |
| T-10-13 | Denial of Service | get_holidays_set DB call per render | mitigate | get_holidays_set is called once per grid render (year-level), not per cell (Pitfall: do not call inside compute_symbol loop) |
| T-10-14 | Tampering | schema addition without migration | accept | db.create_all() is idempotent and only creates the missing holiday_calendar table; no data loss to existing tables |
</threat_model>

<verification>
- Run python -m pytest tests/test_sadm05.py -x -q — all tests pass
- Run python -m pytest tests/ -q — existing timesheet tests still pass (get_holidays_set fallback preserves prior behavior for years with no DB rows)
- grep confirms HolidayCalendar model, the three endpoints, the panelCalendar tab, and the nav link
</verification>

<success_criteria>
- HolidayCalendar table CRUD works; holidays consumed by compute_symbol via get_holidays_set with hardcoded fallback (SADM-05 / D-05)
- POST validates date format and rejects duplicates; POST and DELETE are audited
- Endpoints return 403 for non-superadmins
- No regression in existing timesheet/holiday tests
</success_criteria>

<output>
Create .planning/phases/10-superadmin-panel-extension/10-03-SUMMARY.md when done.
</output>
