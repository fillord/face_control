---
phase: 05-token-based-kiosk-registration-russian-ui
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - templates/superadmin.html
  - templates/org_admin.html
  - templates/dept_admin.html
  - templates/login.html
  - templates/admin.html
  - templates/profile.html
  - templates/dashboard.html
  - templates/403.html
  - app.py
autonomous: false
requirements:
  - UI-RU-01
  - UI-BRAND-01
  - UI-NAV-01

must_haves:
  truths:
    - "Every visible string across all admin/auth templates is in Russian"
    - "Every page header reads МедКонтроль; superadmin shows 'МедКонтроль — Суперадмин', org_admin shows 'МедКонтроль — [org name]', dept_admin shows 'МедКонтроль — [dept name]'"
    - "Each role's navigation shows ONLY links relevant to that role (no links to other roles' areas)"
    - "Role-scoped header names are passed from Flask (org name / dept name resolved server-side)"
  artifacts:
    - path: "templates/superadmin.html"
      provides: "Russian superadmin dashboard with scoped header/nav"
      contains: "Суперадмин"
    - path: "templates/org_admin.html"
      provides: "Russian org_admin dashboard with org-name header and org-scoped nav"
      contains: "МедКонтроль"
    - path: "templates/dept_admin.html"
      provides: "Russian dept_admin dashboard with dept-name header and dept-scoped nav"
      contains: "МедКонтроль"
  key_links:
    - from: "app.py role dashboard routes"
      to: "templates header context"
      via: "passing org_name/dept_name to render_template"
      pattern: "org_name=|dept_name="
    - from: "templates nav blocks"
      to: "session.role"
      via: "Jinja2 role conditionals"
      pattern: "session.role|role =="
---

<objective>
Audit and finish the Russian-language pass and МедКонтроль branding across all authenticated templates, and make every role's navigation strictly role-scoped with a role-appropriate header (superadmin -> "Суперадмин", org_admin -> org name, dept_admin -> dept name). Provide the org/dept names from Flask so headers can render them.

Purpose: The system must read as a clean professional Russian medical product where each role sees only their own tools. The templates are already mostly Russian; this plan closes remaining English strings, normalizes branding, and enforces role-scoped nav and headers.
Output: Updated admin/auth templates with consistent Russian text, branded role-scoped headers, and role-only navigation; Flask routes pass the names the headers need.
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-token-based-kiosk-registration-russian-ui/05-CONTEXT.md
@.planning/phases/05-token-based-kiosk-registration-russian-ui/05-RESEARCH.md
@CLAUDE.md
@app.py
@templates/superadmin.html
@templates/org_admin.html
@templates/dept_admin.html
@templates/login.html
@templates/admin.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass role-scoped header names from Flask + finish Russian/branding audit</name>
  <read_first>
    - app.py (superadmin_page 349-356, org_admin_page 359-366, dept_admin_page 369-376, admin_page 323-337, profile_page 387-409, ROLE_DISPLAY 315-321, load_orgs/load_depts)
    - templates/superadmin.html, templates/org_admin.html, templates/dept_admin.html (headers + every visible string)
    - templates/login.html, templates/admin.html, templates/profile.html, templates/dashboard.html, templates/403.html (scan for English text)
    - .planning/phases/05-token-based-kiosk-registration-russian-ui/05-CONTEXT.md (header spec per role)
  </read_first>
  <action>
    In app.py, update the three role dashboard routes to resolve and pass the header name:
    - `org_admin_page`: load the caller's org via load_orgs().get(session.get("org_id")); pass `org_name = org.get("name") if org else ""` to render_template.
    - `dept_admin_page`: load the caller's dept via load_depts().get(session.get("dept_id")); pass `dept_name = dept.get("name") if dept else ""`.
    - `superadmin_page`: no lookup needed (header is the fixed "Суперадмин").
    Keep existing username/role context. Do NOT change auth decorators or scope logic.

    In each template, set the header text per CONTEXT spec:
    - superadmin.html header -> "МедКонтроль — Суперадмин".
    - org_admin.html header -> "МедКонтроль — {{ org_name }}" (fallback to just "МедКонтроль" if org_name empty).
    - dept_admin.html header -> "МедКонтроль — {{ dept_name }}" (fallback "МедКонтроль").
    Also update each template's `<title>` to match (e.g. "Суперадмин — МедКонтроль").

    Russian/branding audit across login.html, admin.html, profile.html, dashboard.html, 403.html, superadmin.html, org_admin.html, dept_admin.html: replace any remaining English visible strings (button labels, table headers, placeholders, toasts, empty-state text) with Russian. Keep code identifiers, CSS class names, and HTML attributes unchanged. Where a template already uses ROLE_DISPLAY values, keep them. Do NOT translate data values, only UI chrome.
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && venv/bin/python -c "import sys; sa=open('templates/superadmin.html',encoding='utf-8').read(); oa=open('templates/org_admin.html',encoding='utf-8').read(); da=open('templates/dept_admin.html',encoding='utf-8').read(); ap=open('app.py',encoding='utf-8').read(); ok=('Суперадмин' in sa) and ('org_name' in oa) and ('dept_name' in da) and ('org_name=' in ap) and ('dept_name=' in ap); sys.exit(0 if ok else 1)" && echo BRAND_OK</automated>
  </verify>
  <acceptance_criteria>
    - app.py org_admin_page passes org_name; dept_admin_page passes dept_name.
    - superadmin.html header contains "МедКонтроль — Суперадмин"; org_admin.html header uses {{ org_name }}; dept_admin.html header uses {{ dept_name }}.
    - No remaining English UI strings in the audited templates (manual scan + checkpoint).
    - `venv/bin/python -m pytest tests/ -q` shows no new failures from the route context additions.
  </acceptance_criteria>
  <done>Every admin/auth page is Russian and branded; role headers show the correct scoped name supplied by Flask.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce role-scoped navigation across dashboards</name>
  <read_first>
    - templates/superadmin.html, templates/org_admin.html, templates/dept_admin.html, templates/admin.html (nav-tabs / nav blocks)
    - app.py (each role route already passes role; ROLE_HIERARCHY)
    - .planning/phases/05-token-based-kiosk-registration-russian-ui/05-CONTEXT.md (nav: each role sees ONLY its items, no hidden/disabled links for other roles)
  </read_first>
  <action>
    Make each dashboard's navigation strictly role-scoped so a role never sees links to another role's area:
    - superadmin.html: nav exposes only superadmin-relevant tabs (organizations, system-wide stats, user management, registration). No org_admin/dept_admin-only items.
    - org_admin.html: nav exposes only org-scoped tabs (departments within their org, employees, org settings [PIN/token panel from 05-05], reports for their org, registration). No superadmin system-wide items.
    - dept_admin.html: nav exposes only dept-scoped tabs (their department's employees, today attendance, schedules, registration). No org/superadmin items.
    Implement by REMOVING cross-role links rather than hiding them with CSS — per CONTEXT, no disabled/hidden links for other roles. Where a single template is shared by multiple roles (e.g. admin.html used by org_admin and dept_admin via creatable_roles), gate link rendering with Jinja2 `{% if session.role == '...' %}` so each role gets only its own links.
    Keep all hrefs pointing at existing routes. Do NOT add new routes here (05-05 adds the settings panel; reference its tab only inside org_admin.html where 05-05 will fill it).
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && venv/bin/python -c "import sys; da=open('templates/dept_admin.html',encoding='utf-8').read(); sys.exit(0 if ('/superadmin' not in da and 'org_admin' not in da.replace('dept_admin','')) else 1)" && echo NAV_SCOPED_OK</automated>
  </verify>
  <acceptance_criteria>
    - dept_admin.html nav contains no link to /superadmin or /org_admin areas.
    - org_admin.html nav contains no superadmin-only (system-wide) links.
    - superadmin.html nav contains no dept-only links.
    - Cross-role links are removed/Jinja-gated, not merely CSS-hidden.
  </acceptance_criteria>
  <done>Each role's navigation shows only that role's links; no cross-role navigation remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All admin/auth pages are Russian and МедКонтроль-branded with role-scoped headers (Суперадмин / org name / dept name) and role-only navigation.</what-built>
  <how-to-verify>
    1. Restart: `pm2 restart face-recognition`.
    2. Log in as superadmin -> header reads "МедКонтроль — Суперадмин"; nav shows only superadmin tabs.
    3. Log in as an org_admin (e.g. Nurlab_2) -> header reads "МедКонтроль — NurLab" (their org); nav shows only org tabs; no system-wide/superadmin links.
    4. Log in as a dept_admin -> header reads "МедКонтроль — [their dept]"; nav shows only dept tabs.
    5. Skim each page: confirm no English text remains in buttons, tables, placeholders, toasts, empty states.
  </how-to-verify>
  <resume-signal>Type "approved" if headers, nav scoping, and Russian text are correct on all three role dashboards; otherwise list what is wrong.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
- `app.py` — org_admin_page passes org_name; dept_admin_page passes dept_name for scoped headers.
- `templates/superadmin.html`, `org_admin.html`, `dept_admin.html` — Russian text, role-scoped branded headers, role-only navigation.
- `templates/login.html`, `admin.html`, `profile.html`, `dashboard.html`, `403.html` — Russian-only UI chrome.
</artifacts_this_phase_produces>

<verification>
- Manual checkpoint confirms headers/nav/Russian per role.
- `venv/bin/python -m pytest tests/ -q` green (no regressions from route context changes).
</verification>

<success_criteria>
- Professional Russian МедКонтроль UI; role-scoped headers and navigation; no English UI chrome; no cross-role links.
</success_criteria>

<output>
Create `.planning/phases/05-token-based-kiosk-registration-russian-ui/05-04-SUMMARY.md` when done.
</output>
