---
phase: 04-export-employee-cabinet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_export_employee.py
  - tests/conftest.py
autonomous: true
requirements: [EXP-01, EXP-02, EXP-03, EMP-01, EMP-02, EMP-03]
must_haves:
  truths:
    - "Running the Phase 4 test file collects 6 tests (one per requirement) without import errors"
    - "All 6 tests are xfail (expected-fail) until the feature plans implement them"
    - "conftest exposes a seed_attendance() helper that inserts AttendanceRecord rows via ORM"
  artifacts:
    - path: "tests/test_export_employee.py"
      provides: "Failing/xfail scaffold for EXP-01..03 and EMP-01..03"
      contains: "def test_export_xlsx_dept_admin"
      min_lines: 60
    - path: "tests/conftest.py"
      provides: "seed_attendance() ORM helper for AttendanceRecord"
      contains: "def seed_attendance"
  key_links:
    - from: "tests/test_export_employee.py"
      to: "tests/conftest.py"
      via: "import seed_attendance, seed_users, seed_employees, seed_depts, seed_orgs"
      pattern: "from tests.conftest import"
---

## Phase Goal

**As an** authorized HR user and **as an** employee, **I want to** export the T-13 grid to Excel/CSV and view my own attendance cabinet, **so that** I can file statutory timesheets and check my own record without admin access.

<objective>
Wave 0 test scaffold for Phase 4. Create a failing/xfail pytest scaffold covering all six Phase 4 requirements (EXP-01, EXP-02, EXP-03, EMP-01, EMP-02, EMP-03) and add the missing `seed_attendance()` conftest helper that the feature plans (04-02 Export, 04-03 Employee Cabinet) will turn green.

Per MVP vertical-slice rules, the first task of the phase is a failing end-to-end test for each slice's happy path. These tests define the contracts (route paths, response shapes, status codes) that 04-02 and 04-03 implement.

Purpose: Lock in the requirement-to-test mapping before any feature code exists, so the Nyquist rule is satisfied (every later `<verify>` has a real test to run).
Output: `tests/test_export_employee.py` (6 xfail tests), `tests/conftest.py` (+`seed_attendance` helper).
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-export-employee-cabinet/04-CONTEXT.md
@.planning/phases/04-export-employee-cabinet/04-RESEARCH.md
@.planning/phases/04-export-employee-cabinet/04-PATTERNS.md
@tests/conftest.py
@tests/test_timesheet.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add seed_attendance() helper to conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (study seed_employees at lines 193-241 for the exact ORM-insert-inside-app_context pattern, and the module imports)
    - models.py lines 109-121 (AttendanceRecord columns: id autoincrement int PK, emp_id, date, check_in_time, check_out_time, org_id, event_type)
  </read_first>
  <action>
    Add a `seed_attendance(tmp_data, records_list)` function to `tests/conftest.py`, modeled on the existing `seed_employees` helper. It accepts `tmp_data` (accepted but ignored, same convention as seed_employees) and `records_list`, a list of dicts with keys `id`, `emp_id`, `date`, `check_in_time`, `check_out_time`, optional `org_id`, optional `event_type`. Inside `with _app.app.app_context():` iterate the list; for each record, if an id is provided and `AttendanceRecord.query.get(rec["id"])` already exists, skip; otherwise `db.session.add(AttendanceRecord(...))` mapping each key; then `db.session.commit()`. Import `db, AttendanceRecord` from `models` inside the function (same lazy-import style as seed_employees uses `from models import db, Employee, EmployeeSchedule`). Note `AttendanceRecord.id` is an autoincrement Integer PK — callers may supply an explicit int id; only run the dedupe `.get` guard when an id key is present. Do NOT modify any existing conftest helper.
  </action>
  <verify>
    <automated>SECRET_KEY=test venv/bin/python -c "import tests.conftest as c; assert hasattr(c, 'seed_attendance'), 'seed_attendance missing'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` contains a top-level `def seed_attendance(` symbol
    - The helper imports `AttendanceRecord` from `models` and calls `db.session.add` then `db.session.commit` inside `_app.app.app_context()`
    - No existing helper (`seed_users`, `seed_employees`, `seed_depts`, `seed_orgs`, `seed_config`) is modified
  </acceptance_criteria>
  <done>seed_attendance() exists and is importable from tests.conftest without error.</done>
</task>

<task type="auto">
  <name>Task 2: Create xfail test scaffold for all 6 Phase 4 requirements</name>
  <files>tests/test_export_employee.py</files>
  <read_first>
    - tests/test_timesheet.py (copy the file header docstring style, login-then-GET client pattern, seed_* call sites, and @pytest.mark.xfail usage)
    - tests/conftest.py (confirm exports: client, tmp_data, seed_users, seed_employees, seed_depts, seed_orgs, seed_attendance, BCRYPT_HASH_SUPERADMIN — note BCRYPT_HASH_SUPERADMIN at line 20 is the hash for password "superadmin123")
    - .planning/phases/04-export-employee-cabinet/04-RESEARCH.md (Phase Requirements to Test Map section: exact test function names and assertions)
  </read_first>
  <action>
    Create `tests/test_export_employee.py`. Module docstring lists the 6 covered requirements (EXP-01..03, EMP-01..03). Import from `tests.conftest`: `seed_users, seed_employees, seed_depts, seed_orgs, seed_attendance, BCRYPT_HASH_SUPERADMIN`. Write exactly 6 test functions, each decorated `@pytest.mark.xfail(reason="implemented in 04-02/04-03", strict=False)`:
    (1) `test_export_xlsx_dept_admin(client, tmp_data)` — EXP-01: seed org-A, dept-A (name "ВОП-1"), a dept_admin user scoped to dept-A; POST /login with password "superadmin123"; GET `/timesheet/export/xlsx?dept_id=dept-A&month=2026-06`; assert status 200, `b"PK"` in `resp.data` (xlsx ZIP magic), and `"attachment"` plus `"T13_"` in the Content-Disposition header.
    (2) `test_export_csv_bom_encoding(client, tmp_data)` — EXP-02: same seeding/login; GET `/timesheet/export/csv?dept_id=dept-A&month=2026-06`; assert status 200, `resp.data.startswith(b"\xef\xbb\xbf")` (UTF-8 BOM), and that a semicolon byte `b";"` appears in `resp.data`.
    (3) `test_export_scope_enforcement(client, tmp_data)` — EXP-03: seed two depts (dept-A, dept-B) in org-A, with one employee in dept-B; login as dept_admin scoped to dept-A; GET `/timesheet/export/xlsx?dept_id=dept-B&month=2026-06`; dept_admin is forced to their own dept. Accept either outcome via `resp.status_code in (200, 403)` and, when 200, assert the dept-B employee's name bytes are absent from `resp.data`.
    (4) `test_employee_cabinet_renders(client, tmp_data)` — EMP-01: seed org-A, dept-A, one employee emp-1 (label 1), and a User with role "employee", active True, `emp_id="emp-1"`; login as that employee with password "superadmin123"; GET `/employee?month=2026-06`; assert status 200 and `"Мой табель"` (UTF-8 bytes) in `resp.data`.
    (5) `test_employee_tooltip_times(client, tmp_data)` — EMP-02: seed as in (4) plus an AttendanceRecord via seed_attendance for emp-1 on a date in 2026-06 with check_in_time "09:05:00" and check_out_time "18:00:00"; GET `/employee?month=2026-06`; assert `"Приход".encode()` and `"09:05".encode()` both appear in `resp.data` (tooltip title text).
    (6) `test_employee_stats_counts(client, tmp_data)` — EMP-03: seed as in (4); GET `/employee?month=2026-06`; assert all three stat labels `"Опоздания"`, `"Отсутствия"`, `"Ранний уход"` (UTF-8 bytes) appear in `resp.data`.
    Use `client.post("/login", data={...}, follow_redirects=True)` for login. Do NOT implement any production code in this plan — these tests must xfail because the routes/columns do not exist yet.
  </action>
  <verify>
    <automated>SECRET_KEY=test venv/bin/python -m pytest tests/test_export_employee.py -q 2>&1 | grep -v '^#' | grep -Eq '6 xfailed|xfailed' && echo SCAFFOLD_XFAIL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_export_employee.py` defines exactly these 6 functions: test_export_xlsx_dept_admin, test_export_csv_bom_encoding, test_export_scope_enforcement, test_employee_cabinet_renders, test_employee_tooltip_times, test_employee_stats_counts
    - Running `pytest tests/test_export_employee.py -q` reports the suite as xfailed (no errors, no unexpected pass) — collection succeeds with zero ImportError
    - Each test carries `@pytest.mark.xfail(... strict=False)`
    - The file imports `seed_attendance` from `tests.conftest`
  </acceptance_criteria>
  <done>All 6 tests collect and report xfail; full suite `pytest tests/ -q` still has no collection errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app | Tests drive the Flask app via the test client; no real network or untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | test fixtures seeding ORM rows | accept | In-memory SQLite per-test; no shared state; no production data touched |
| T-04-SC | Tampering | pip installs | accept | No package installs in this plan — openpyxl install + legitimacy gate is handled in 04-02 |
</threat_model>

<verification>
- `SECRET_KEY=test venv/bin/python -m pytest tests/test_export_employee.py -q` → 6 xfailed, 0 errors
- `SECRET_KEY=test venv/bin/python -m pytest tests/ -q` → no new collection errors introduced
</verification>

<success_criteria>
- seed_attendance() helper exists in conftest and inserts AttendanceRecord rows
- 6 requirement tests exist as xfail, defining the contracts for 04-02 and 04-03
- Test suite collects cleanly
</success_criteria>

<output>
Create `.planning/phases/04-export-employee-cabinet/04-01-SUMMARY.md` when done
</output>
