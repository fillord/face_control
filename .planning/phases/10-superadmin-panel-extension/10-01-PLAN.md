---
phase: 10-superadmin-panel-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - templates/superadmin.html
  - templates/base.html
  - tests/test_sadm07_02.py
autonomous: true
requirements: [SADM-07, SADM-02]
must_haves:
  truths:
    - "Superadmin can create a dept_admin account and the request returns success, not 403"
    - "Superadmin can create an hr_viewer account"
    - "When dept_admin role is selected in the create-user form, a department selector scoped to the chosen org appears"
    - "Superadmin still cannot create an employee account via /api/users (403)"
    - "The Employees tab lists every employee across all orgs with org name, dept name, face status, and registration date"
    - "Superadmin can filter the Employees tab by organization client-side"
    - "GET /api/superadmin/employees returns 403 for any non-superadmin role"
  artifacts:
    - path: "app.py"
      provides: "create_user() role allowlist fix + dept_admin dept_id guard + GET /api/superadmin/employees endpoint"
      contains: "superadmin_employees"
    - path: "templates/superadmin.html"
      provides: "panelEmployees tab + role/dept selector in create-user form"
      contains: "panelEmployees"
    - path: "templates/base.html"
      provides: "Сотрудники nav link in superadmin sidebar block"
      contains: "/superadmin/employees"
  key_links:
    - from: "templates/superadmin.html createUser()"
      to: "POST /api/users"
      via: "fetch with role + dept_id payload"
      pattern: "dept_id"
    - from: "templates/superadmin.html loadSuperEmployees()"
      to: "GET /api/superadmin/employees"
      via: "fetch in switchTab lazy-load"
      pattern: "api/superadmin/employees"
---

<objective>
Deliver SADM-07 (D-01) and SADM-02 (D-02): let superadmin create org_admin, dept_admin, and hr_viewer accounts (with a scoped department selector for dept_admin), and add a read-only Employees tab that lists every employee across all organizations.

Purpose: Superadmins currently can only create org_admin accounts and have no system-wide employee directory. These two features unblock org administration and give visibility into the full roster.
Output: GET /api/superadmin/employees endpoint, fixed create_user() role guard, a panelEmployees tab, an extended create-user form, a base.html nav link, and a pytest file covering both features.
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-superadmin-panel-extension/10-CONTEXT.md
@.planning/phases/10-superadmin-panel-extension/10-RESEARCH.md
@.planning/phases/10-superadmin-panel-extension/10-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (new symbols introduced by this plan)

- superadmin_employees — Flask route handler for GET /api/superadmin/employees (app.py)
- _SA_ALLOWED — set constant of roles superadmin may create, inside create_user() (app.py)
- panelEmployees — superadmin.html tab div id
- loadSuperEmployees, renderSuperEmployees, filterEmployees — superadmin.html JS functions
- allSuperEmployees, employeesLoaded, allDepts — superadmin.html JS state variables
- onRoleChange, populateDeptSelector — superadmin.html JS functions for the create-user form
- deptSelectGroup, newDeptId, empOrgFilter, employeesTableBody — superadmin.html element ids
- tests/test_sadm07_02.py — new pytest file
- VALID_TABS extended in superadmin_page (app.py existing symbol, new values employees/devices/logs/calendar/analytics)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Backend — create_user() role fix, Employees endpoint, VALID_TABS, tests</name>
  <files>app.py, tests/test_sadm07_02.py</files>
  <read_first>
    - app.py lines 1967-2018 (create_user) — current restriction and dept_id handling
    - app.py lines 289-291, 1229-1231 (VALID_TABS in superadmin_page)
    - app.py lines 1950-1966 (list_users ORM-plus-jsonify pattern to copy)
    - models.py lines 1-204 (Employee, Organization, Department fields)
    - .planning/phases/10-superadmin-panel-extension/10-PATTERNS.md (exact replacement for create_user line 1985, employees endpoint pattern, section-divider style)
    - .planning/phases/10-superadmin-panel-extension/10-RESEARCH.md (SADM-07 and SADM-02 sections)
    - tests/conftest.py lines 35-245 (client fixture, seed_users, seed_orgs, seed_depts, seed_employees, BCRYPT_HASH_SUPERADMIN)
    - tests/test_org_dept.py lines 28-72 (session_transaction superadmin-injection test pattern)
  </read_first>
  <action>
    In app.py create_user() at line 1985, replace the single-role check with an allowlist: define _SA_ALLOWED equal to the set of org_admin, dept_admin, hr_viewer, and return 403 with the Russian message "Суперадминистратор может создавать org_admin, dept_admin, hr_viewer" only when creator_role is superadmin and target_role is not in _SA_ALLOWED. Keep the existing per-level hierarchy check for non-superadmin creators unchanged. After new_dept_id is resolved (around line 1996), add a server-side guard (RESEARCH Pitfall 7): if target_role is dept_admin and new_dept_id is falsy, return 400 with message "Для роли dept_admin необходимо указать отдел". Leave the existing write_audit() call at lines 2015-2016 untouched (D-01).

    Add endpoint superadmin_employees() at GET /api/superadmin/employees guarded by require_role for superadmin only, under a new box-drawing section divider reading "API: Superadmin Extensions". Load Employee.query.all(), build id-to-name maps from Organization.query.all() and Department.query.all(), and return a JSON list where each item carries id, name, org_id, org_name (fallback dash), dept_id, dept_name (fallback dash), face_enrolled (boolean face_count greater than zero), registered_at (or empty string) (D-02).

    Extend VALID_TABS in superadmin_page() (line 1230) to the complete final set: orgs, users, system, employees, devices, logs, calendar, analytics, so later plans do not re-edit this line.

    Create tests/test_sadm07_02.py using the conftest client fixture, the seed_users/seed_orgs/seed_depts/seed_employees helpers, and client.session_transaction() to inject a superadmin session (mirror tests/test_org_dept.py lines 42-48). Cover: (a) POST /api/users role=dept_admin with valid org_id and dept_id returns a non-403 success status; (b) POST /api/users role=dept_admin without dept_id returns 400; (c) POST /api/users role=employee returns 403; (d) POST /api/users role=hr_viewer with org_id returns success; (e) GET /api/superadmin/employees as superadmin returns 200 and a list whose items contain keys name, org_name, dept_name, face_enrolled; (f) GET /api/superadmin/employees with an injected org_admin session returns 403.
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && python -m pytest tests/test_sadm07_02.py -x -q && python -c "import ast; ast.parse(open('app.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_sadm07_02.py passes all six tests under python -m pytest tests/test_sadm07_02.py -x -q
    - grep -q _SA_ALLOWED app.py succeeds
    - grep -q "def superadmin_employees" app.py succeeds
    - grep -q analytics app.py confirms VALID_TABS extended (line ~1230 includes employees/devices/logs/calendar/analytics)
    - python -c "import ast; ast.parse(open('app.py').read())" exits 0
  </acceptance_criteria>
  <done>Superadmin can create dept_admin/hr_viewer but not employee via /api/users; dept_admin without a dept is rejected 400; GET /api/superadmin/employees returns the full roster to superadmin and 403 to others; all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — create-user role options and scoped dept selector</name>
  <files>templates/superadmin.html</files>
  <read_first>
    - templates/superadmin.html lines 104-131 (createUserPanel), 167-169 (state vars), 452-498 (toggleUserForm and createUser)
    - .planning/phases/10-superadmin-panel-extension/10-PATTERNS.md (create_user form extension SADM-07 — role options, deptSelectGroup markup, onRoleChange/populateDeptSelector, createUser dept_id payload)
    - app.py lines 2591-2602 (GET /api/depts returns all depts for superadmin, each with org_id)
  </read_first>
  <action>
    Extend the newRole select in createUserPanel to three options with values org_admin ("Администратор организации"), dept_admin ("Администратор отдела"), hr_viewer ("HR-наблюдатель"), and add onchange calling onRoleChange. Insert a hidden form-group with id deptSelectGroup (display none) containing a select with id newDeptId and a default empty option "— выберите отдел —", placed after the existing newOrgId form-group. Add JS state allDepts as an empty array. Add onRoleChange which shows deptSelectGroup only when the role is dept_admin and then calls populateDeptSelector. Add populateDeptSelector which lazily fetches /api/depts into allDepts once, reads the selected newOrgId value, and repopulates newDeptId with depts whose org_id equals the selected org id. Also invoke populateDeptSelector from the newOrgId change path when dept_admin is the active role. Update createUser to include dept_id read from newDeptId (or null) in the POST body alongside username, password, role, org_id (D-01). Reuse the existing userFormError element for errors. /api/* is CSRF-exempt (Phase 9 SEC-03), so the fetch needs no CSRF token.
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && grep -q deptSelectGroup templates/superadmin.html && grep -q newDeptId templates/superadmin.html && grep -q "function populateDeptSelector" templates/superadmin.html && grep -q "function onRoleChange" templates/superadmin.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/superadmin.html newRole select contains option values org_admin, dept_admin, hr_viewer
    - templates/superadmin.html contains id="deptSelectGroup" and id="newDeptId"
    - templates/superadmin.html defines function onRoleChange and function populateDeptSelector
    - createUser body includes dept_id
  </acceptance_criteria>
  <done>Selecting dept_admin reveals a department dropdown scoped to the chosen org; submitting creates the account with the chosen dept_id; org_admin/hr_viewer hide the dept field.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend — Employees tab panel, lazy-load, filter, nav link</name>
  <files>templates/superadmin.html, templates/base.html</files>
  <read_first>
    - templates/superadmin.html lines 68-163 (panelSystem/panelUsers blocks), 172-181 (init and switchTab), 417-422 (loadUsers fetch pattern), 564-571 (escapeHtml)
    - .planning/phases/10-superadmin-panel-extension/10-PATTERNS.md (New panel divs, JS state/init/switchTab extensions, table-card, toolbar + filter, table empty-state placeholder)
    - templates/base.html lines 153-171 (superadmin nav block, existing link pattern)
  </read_first>
  <action>
    Add a panelEmployees div (class "page hidden") with page-title "Сотрудники", a toolbar containing a select with id empOrgFilter and onchange filterEmployees (default option "Все организации"), and a table-card with headers Имя, Организация, Отдел, Лицо, Дата добавления and tbody id employeesTableBody. No edit or delete buttons (read-only, D-02). Add JS state allSuperEmployees as empty array and employeesLoaded false. Add loadSuperEmployees which fetches /api/superadmin/employees into allSuperEmployees, populates empOrgFilter options from the distinct org_name and org_id pairs, then calls renderSuperEmployees. Add renderSuperEmployees(list) which renders rows using escapeHtml, shows Лицо as a check mark or dash, and shows an empty-state row with colspan 5 reading "Нет данных" when the list is empty. Add filterEmployees which filters allSuperEmployees by the selected org_id client-side and re-renders. Extend switchTab to toggle panelEmployees and lazy-load: when tab is employees and not employeesLoaded, call loadSuperEmployees and set employeesLoaded true. Keep init loading only orgs, stats, users (do not eager-load employees). In templates/base.html, add after the Пользователи nav link a Сотрудники link with href /superadmin/employees, icon 👤, using the existing active-class pattern keyed on request.path.
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && grep -q panelEmployees templates/superadmin.html && grep -q "function loadSuperEmployees" templates/superadmin.html && grep -q "function filterEmployees" templates/superadmin.html && grep -q "/superadmin/employees" templates/base.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/superadmin.html contains panelEmployees, empOrgFilter, employeesTableBody
    - templates/superadmin.html defines function loadSuperEmployees, renderSuperEmployees, filterEmployees
    - switchTab toggles panelEmployees and lazy-loads on employeesLoaded false
    - templates/base.html contains an anchor with href /superadmin/employees in the superadmin nav block
  </acceptance_criteria>
  <done>Clicking Сотрудники shows a read-only table of all employees across orgs, filterable by org; data loads only when the tab is first opened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to /api/users | authenticated superadmin submits role + org_id + dept_id (untrusted) |
| browser to /api/superadmin/employees | authenticated request reads full roster across all orgs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation | create_user() role param | mitigate | Server-side allowlist _SA_ALLOWED rejects any target_role not in {org_admin, dept_admin, hr_viewer} with 403; employee/superadmin creation blocked server-side |
| T-10-02 | Tampering | create_user() dept_id for dept_admin | mitigate | Server-side guard returns 400 when dept_admin selected with empty dept_id (Pitfall 7) |
| T-10-03 | Information Disclosure | GET /api/superadmin/employees | mitigate | require_role("superadmin") returns 403 for org_admin/dept_admin/hr_viewer/employee; verified by test (f) |
| T-10-04 | Tampering | employee name rendering in tab | mitigate | escapeHtml applied to all DB-sourced text before innerHTML |
</threat_model>

<verification>
- Run python -m pytest tests/test_sadm07_02.py -x -q — all tests pass
- Run python -m pytest tests/ -q — no pre-existing tests regress from the create_user() change
- grep confirms superadmin_employees, panelEmployees, deptSelectGroup, and the base.html nav link exist
</verification>

<success_criteria>
- Superadmin can create org_admin, dept_admin, and hr_viewer accounts; dept_admin requires a department (SADM-07 / D-01)
- Superadmin cannot create employee accounts via /api/users (403)
- Employees tab lists all employees across all orgs with org, dept, face status, registration date, filterable by org, read-only (SADM-02 / D-02)
- GET /api/superadmin/employees returns 403 to non-superadmins
- python -c import ast app.py parse exits 0; no test regressions
</success_criteria>

<output>
Create .planning/phases/10-superadmin-panel-extension/10-01-SUMMARY.md when done.
</output>
