---
phase: 02-org-dept-data-model
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_org_dept.py
  - tests/test_migration.py
autonomous: true
requirements: [ORG-01, ORG-02, ORG-03, ORG-04, MIG-01, MIG-02, T13-06, DASH-01, DASH-02, KIOSK-01]
must_haves:
  truths:
    - "Running pytest collects new org/dept and migration tests without import errors"
    - "Every Phase 2 requirement has at least one named failing (xfail) test"
    - "conftest tmp_data fixture isolates ORGS_FILE and DEPTS_FILE to tmp_path"
  artifacts:
    - path: "tests/test_org_dept.py"
      provides: "Failing requirement tests for ORG-01..04, T13-06, DASH-01, DASH-02, KIOSK-01"
      contains: "def test_org_crud"
    - path: "tests/test_migration.py"
      provides: "Failing requirement tests for MIG-01, MIG-02"
      contains: "def test_migration_additive"
    - path: "tests/conftest.py"
      provides: "ORGS_FILE / DEPTS_FILE monkeypatch + org/dept seed helpers"
      contains: "ORGS_FILE"
  key_links:
    - from: "tests/conftest.py"
      to: "app.ORGS_FILE"
      via: "monkeypatch.setattr guarded by hasattr"
      pattern: "hasattr\\(_app, \"ORGS_FILE\"\\)"
---

<objective>
Wave 0 test scaffold for Phase 2. Create the failing test suite and conftest extensions that every later plan must turn green. This is the Nyquist gate: no Phase 2 feature ships without an automated test that exercises it.

Purpose: Establish executable acceptance criteria for all 10 Phase 2 requirements before any production code exists. Tests are RED now and turn GREEN as plans 02–05 land.
Output: Extended `tests/conftest.py`, new `tests/test_org_dept.py`, new `tests/test_migration.py` — all collect cleanly and report xfail/fail (never collection error).
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-org-dept-data-model/02-RESEARCH.md
@.planning/phases/02-org-dept-data-model/02-PATTERNS.md
@.planning/phases/02-org-dept-data-model/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend conftest.py with org/dept isolation and seed helpers</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (the file being modified — copy the existing tmp_data fixture, USERS_FILE hasattr guard at lines 54-56, and seed_users helper at lines ~111-130)
    - .planning/phases/02-org-dept-data-model/02-PATTERNS.md (conftest extension section — the exact hasattr guard and seed helper pattern)
  </read_first>
  <action>
    In the `tmp_data` fixture, after the existing USERS_FILE hasattr guard, add two hasattr-guarded monkeypatches: `if hasattr(_app, "ORGS_FILE"): monkeypatch.setattr(_app, "ORGS_FILE", str(data_dir / "orgs.json"))` and the equivalent for `DEPTS_FILE` → `str(data_dir / "depts.json")`. Use hasattr guards because ORGS_FILE/DEPTS_FILE do not exist in app.py until plan 02-02 — this keeps Wave 0 tests collectable.
    Add two seed helpers mirroring `seed_users`: `seed_orgs(tmp_data, orgs_dict)` writes to `tmp_data / "data" / "orgs.json"`, and `seed_depts(tmp_data, depts_dict)` writes to `tmp_data / "data" / "depts.json"`. Both use `json.dumps(..., ensure_ascii=False, indent=2)` and `encoding="utf-8"` exactly like seed_users.
    Add a `seed_employees(tmp_data, employees_dict)` helper that writes to `tmp_data / "data" / "employees.json"` the same way (later tests seed employees with org_id/dept_id/schedule and label fields).
    Do NOT remove or alter any existing fixture, constant, or helper.
  </action>
  <verify>
    <automated>/var/www/sites/face-almgp33/venv/bin/pytest tests/ --collect-only -q 2>&1 | grep -v '^#' | grep -c "error" | grep -qx 0 && echo COLLECT_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py contains `hasattr(_app, "ORGS_FILE")` and `hasattr(_app, "DEPTS_FILE")`
    - tests/conftest.py contains `def seed_orgs(`, `def seed_depts(`, and `def seed_employees(`
    - `/var/www/sites/face-almgp33/venv/bin/pytest tests/ --collect-only -q` exits 0 with no collection errors
    - Existing Phase 1 tests (test_auth.py, test_rbac.py) still collect (2 passed + 8 xpassed baseline unchanged when run)
  </acceptance_criteria>
  <done>conftest exposes ORGS_FILE/DEPTS_FILE isolation and seed_orgs/seed_depts/seed_employees; full suite still collects without error.</done>
</task>

<task type="auto">
  <name>Task 2: Write failing requirement tests for org/dept CRUD, schedule, dashboards, kiosk</name>
  <files>tests/test_org_dept.py</files>
  <read_first>
    - tests/conftest.py (client fixture, session_transaction injection pattern, the new seed_orgs/seed_depts/seed_employees helpers from Task 1)
    - tests/test_rbac.py (existing pattern for authenticated client tests + session injection — replicate session_transaction usage)
    - .planning/phases/02-org-dept-data-model/02-RESEARCH.md (Phase Requirements → Test Map table lines ~707-718 for exact test names and behaviors; Code Examples for endpoint shapes)
  </read_first>
  <action>
    Create `tests/test_org_dept.py` with one test function per requirement, each named exactly as in the RESEARCH test map. Mark every test `@pytest.mark.xfail(reason="implemented in 02-03/02-04/02-05", strict=False)` so RED now / GREEN later without breaking the suite.
    Tests to author (assert real behavior, not stubs):
    - `test_org_crud`: superadmin session POSTs `/api/orgs` with `{name, description}` → 200 + returned id; GET `/api/orgs` lists it; PUT `/api/orgs/<id>` renames it; DELETE `/api/orgs/<id>` removes it (ORG-01).
    - `test_dept_crud_scope`: org_admin (session org_id="org-A") POSTs `/api/depts` with org_id="org-A" → 200; same caller POSTs dept with org_id="org-B" → 403 (ORG-02).
    - `test_employee_dept_scope`: dept_admin (session dept_id="dept-A") POSTs `/api/employees` for own dept → success; attempt to add/edit employee in dept-B → 403 (ORG-03).
    - `test_employee_reassign`: org_admin PATCHes `/api/employees/<id>` setting dept_id to another dept within their org → 200; reassign to a dept outside their org → 403 (ORG-04).
    - `test_schedule_update`: PATCH `/api/employees/<id>/schedule` with `{start:"08:00", end:"17:00", work_days:[1,2,3,4,5]}` → 200 and persisted; invalid time format "25:00" → 400 (T13-06).
    - `test_superadmin_stats`: GET `/api/superadmin_stats` returns JSON with keys `orgs` (count), `employees` (count), `checkins_today` (count) (DASH-01).
    - `test_dept_attendance_scope`: dept_admin GET `/api/dept_attendance_today` returns `{employees, stats:{present,absent,late}}` scoped to their dept_id; employees from other depts absent from the result (DASH-02).
    - `test_recognize_dept_name`: assert the JSON contract that `/api/recognize` includes a `dept_name` key; seed an employee with a dept_id whose dept.name is known and assert the lookup path — where face decoding is impractical in a unit test, assert via a direct call to the dept-name lookup helper or document the assertion against the response schema (KIOSK-01).
    Seed data using seed_orgs/seed_depts/seed_employees and inject sessions via `client.session_transaction()` setting user_id, role, org_id, dept_id.
  </action>
  <verify>
    <automated>/var/www/sites/face-almgp33/venv/bin/pytest tests/test_org_dept.py -q 2>&1 | grep -v '^#' | grep -Eq "xfail|fail" && echo TESTS_RED</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_org_dept.py defines functions named test_org_crud, test_dept_crud_scope, test_employee_dept_scope, test_employee_reassign, test_schedule_update, test_superadmin_stats, test_dept_attendance_scope, test_recognize_dept_name
    - Each test carries `@pytest.mark.xfail`
    - `/var/www/sites/face-almgp33/venv/bin/pytest tests/test_org_dept.py -q` reports only xfail/xpass/fail — zero collection errors
    - No test asserts a placeholder/stub value; assertions reference the real endpoint contracts in RESEARCH Code Examples
  </acceptance_criteria>
  <done>Eight named requirement tests exist, all xfail, collecting cleanly — they form the GREEN target for plans 02-03/02-04/02-05.</done>
</task>

<task type="auto">
  <name>Task 3: Write failing migration tests (MIG-01, MIG-02)</name>
  <files>tests/test_migration.py</files>
  <read_first>
    - tests/conftest.py (tmp_data fixture, seed_employees helper from Task 1)
    - .planning/phases/02-org-dept-data-model/02-RESEARCH.md (Pattern 5 migration script, Pitfall 2 getLabels flatten, Runtime State Inventory — employee 1781173156953 with label 1)
    - .planning/phases/02-org-dept-data-model/02-CONTEXT.md (D-04 through D-08 migration decisions)
  </read_first>
  <action>
    Create `tests/test_migration.py` with two xfail tests (`@pytest.mark.xfail(reason="implemented in 02-02", strict=False)`):
    - `test_migration_additive`: seed employees.json with one employee carrying `{id, name, role, label:1, face_count:10, registered_at}` (NO org_id/dept_id/schedule). Import and invoke migrate.py's migration entrypoint (e.g. `import migrate; migrate.run_migration()` — final name set in 02-02) against the tmp_data dir. Assert post-run: the employee dict still contains the original `label` value 1 and original name verbatim, AND now contains `org_id`, `dept_id`, and `schedule == {"start":"09:00","end":"18:00","work_days":[1,2,3,4,5]}`. Assert orgs.json now has exactly one org named "Главная организация" and depts.json one dept named "Основной отдел". Assert a `employees_backup_*.json` file was written (MIG-01, D-05/D-07).
    - `test_label_integrity_warn`: seed an employee whose `label` integer has no matching face images, run migration, and assert the migration completes (does not raise / does not abort) and that a warning is surfaced (captured stdout contains "WARN" or a returned warnings list is non-empty) — warn-only per D-06 (MIG-02).
    Because migrate.py reads module-level path constants, ensure the tests monkeypatch migrate's DATA_DIR/FACES_DIR/EMPLOYEES_FILE/ORGS_FILE/DEPTS_FILE to tmp_data (guard with hasattr or import-inside-test so collection does not fail before migrate.py exists). Use a try/except ImportError inside the test body guarded by the xfail marker so Wave 0 collection never errors.
  </action>
  <verify>
    <automated>/var/www/sites/face-almgp33/venv/bin/pytest tests/test_migration.py -q 2>&1 | grep -v '^#' | grep -Eq "xfail|fail" && echo MIG_TESTS_RED</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_migration.py defines test_migration_additive and test_label_integrity_warn, both xfail
    - test_migration_additive asserts the original `label` value is preserved AND org_id/dept_id/schedule are added
    - test_label_integrity_warn asserts migration does not abort and surfaces a warning
    - `/var/www/sites/face-almgp33/venv/bin/pytest tests/test_migration.py -q` collects with zero errors (ImportError on missing migrate.py is swallowed under xfail, not a collection error)
  </acceptance_criteria>
  <done>Migration tests exist and are RED; they become the GREEN target for plan 02-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → filesystem | Tests must never write to production data/ — all paths redirected to tmp_path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-T1 | Tampering | test fixtures writing real data/ files | mitigate | tmp_data fixture monkeypatches ORGS_FILE/DEPTS_FILE/EMPLOYEES_FILE to tmp_path; no test references absolute data/ paths |
| T-02-T2 | Denial of Service | collection error blocks whole suite | mitigate | hasattr guards + xfail markers keep tests collectable before app.py/migrate.py symbols exist |
| T-02-SC | Tampering | npm/pip installs | accept | No package installs in this phase (RESEARCH: no new packages); legitimacy gate N/A |
</threat_model>

<verification>
- `/var/www/sites/face-almgp33/venv/bin/pytest tests/ --collect-only -q` exits 0 (no collection errors)
- `/var/www/sites/face-almgp33/venv/bin/pytest tests/ -q` shows Phase 1 baseline still green (2 passed + 8 xpassed) plus new Phase 2 xfails
</verification>

<success_criteria>
- conftest exposes ORGS_FILE/DEPTS_FILE isolation + seed_orgs/seed_depts/seed_employees
- 10 named requirement tests exist (8 in test_org_dept.py, 2 in test_migration.py), all RED/xfail
- Full suite collects and runs with zero errors; Phase 1 baseline unregressed
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Wave 0 scope)

New test symbols (created here, consumed by later plans as the GREEN target):
- `tests/conftest.py` additions: `seed_orgs`, `seed_depts`, `seed_employees` functions; ORGS_FILE/DEPTS_FILE monkeypatches
- `tests/test_org_dept.py`: test_org_crud, test_dept_crud_scope, test_employee_dept_scope, test_employee_reassign, test_schedule_update, test_superadmin_stats, test_dept_attendance_scope, test_recognize_dept_name
- `tests/test_migration.py`: test_migration_additive, test_label_integrity_warn
</artifacts_produced>

<output>
Create `.planning/phases/02-org-dept-data-model/02-01-SUMMARY.md` when done
</output>
