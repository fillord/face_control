---
phase: 01-rbac-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_auth.py
  - tests/test_rbac.py
  - pytest.ini
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07, MIG-03, DASH-03]
must_haves:
  truths:
    - "pytest is installed in the venv and runs"
    - "tests/ directory exists with conftest.py providing an isolated Flask test client"
    - "A failing end-to-end auth test exists (login → role redirect) that defines the happy path"
    - "Every Phase 1 requirement has a named test function (initially failing/skipped) per the VALIDATION map"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Flask test client fixture with isolated temp data dir"
      min_lines: 30
    - path: "tests/test_auth.py"
      provides: "Auth requirement test stubs (AUTH-01/02/04/06/07, MIG-03)"
      contains: "def test_login_valid"
    - path: "tests/test_rbac.py"
      provides: "RBAC requirement test stubs (AUTH-03/05, DASH-03)"
      contains: "def test_post_login_redirect"
  key_links:
    - from: "tests/conftest.py"
      to: "app.py"
      via: "import app, set app.testing=True, point DATA_DIR at tmp_path"
      pattern: "import app"
---

<objective>
Establish the Nyquist Wave 0 test foundation for Phase 1 and write the failing end-to-end happy-path test that defines "working login + role-appropriate landing." This is the MVP failing-test-first task: every later plan turns these RED tests GREEN.

Purpose: No test infrastructure exists in this project. Every requirement in this phase (AUTH-01..07, MIG-03, DASH-03) has an entry in `01-VALIDATION.md` pointing at a named pytest function that does not exist yet. This plan creates them all as failing/skipped stubs and one real failing end-to-end test, so executors in Waves 1-3 have an executable definition of done.
Output: `pytest.ini`, `tests/conftest.py`, `tests/test_auth.py`, `tests/test_rbac.py`, pytest installed in venv.
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rbac-foundation/01-CONTEXT.md
@.planning/phases/01-rbac-foundation/01-RESEARCH.md
@.planning/phases/01-rbac-foundation/01-VALIDATION.md
@app.py
@data/config.json
</context>

## Artifacts this phase produces

New symbols/files introduced across Phase 1 (executors and reviewers: treat these as newly-created, not drift):

- Files: `tests/conftest.py`, `tests/test_auth.py`, `tests/test_rbac.py`, `pytest.ini`, `templates/403.html`, `templates/dashboard.html`, `templates/profile.html`, `data/users.json` (created at runtime), `ecosystem.config.js`
- `app.py` constants: `USERS_FILE`, `ROLE_HIERARCHY`
- `app.py` functions: `load_users()`, `save_users()`, `init_users()`, `require_role()` decorator factory, `dashboard_page()`, `profile_page()`, `create_user()`, `update_user()`, `list_users()`
- `app.py` routes: `GET /dashboard`, `GET/POST /profile`, `GET /api/users`, `POST /api/users`, `PATCH /api/users/<user_id>`
- Session keys: `user_id`, `role`, `org_id`, `dept_id` (replacing `logged_in`, `username`)
- pytest fixtures: `client`, `tmp_data` (or equivalent isolated-data fixture)

<tasks>

<task type="auto">
  <name>Task 1: Install pytest and create isolated Flask test client fixture</name>
  <files>pytest.ini, tests/conftest.py</files>
  <read_first>
    - app.py (module-level startup: `init_config()` runs at import time line 418; `app` defined line 9; `DATA_DIR` line 12; `os.makedirs(FACES_DIR)` line 18 runs at import — the fixture must tolerate this)
    - .planning/phases/01-rbac-foundation/01-RESEARCH.md (Validation Architecture section: framework=pytest 9.0.3, commands, conftest fixture requirements)
    - .planning/phases/01-rbac-foundation/01-VALIDATION.md (Test Infrastructure + Wave 0 Requirements)
    - data/config.json (real hash `$2b$12$F0kPJqHfW3hWU9QJm7kCyee3xD/eYhdcBwwmWgBvXuC9wqwKyPMym` — fixtures that exercise MIG-03 will copy this)
  </read_first>
  <action>
    Install pytest 9.0.3 into the project venv using `/var/www/sites/face-almgp33/venv/bin/pip install pytest==9.0.3`.
    Create `pytest.ini` at repo root with `[pytest]`, `testpaths = tests`, and `python_files = test_*.py`.
    Create `tests/conftest.py` exposing a `client` fixture and an isolated-data fixture. The fixture MUST NOT mutate the real `data/` directory. Approach: import the `app` module, set `app.app.testing = True` and a fixed `app.app.secret_key = "test-secret"`, monkeypatch the module-level path constants (`app.DATA_DIR`, `app.USERS_FILE`, `app.CONFIG_FILE`, `app.EMPLOYEES_FILE`, `app.ATTENDANCE_FILE`, `app.LOGS_FILE`, `app.FACES_DIR`) to point inside pytest's `tmp_path`, then `yield app.app.test_client()`.
    Note in a comment that `app.USERS_FILE`, `app.load_users`, `app.init_users`, and `app.require_role` do not exist yet (created in plan 01-02) — fixtures may reference them with `getattr`/`hasattr` guards or the tests that use them stay `@pytest.mark.skip` until 01-02 lands. Provide a `seed_users(client, users_dict)` helper that writes a users.json into the temp data dir via `json.dump` so RBAC tests can stage accounts.
    Provide a `BCRYPT_HASH_SUPERADMIN` constant in conftest equal to the exact hash string from data/config.json so MIG-03 tests assert verbatim copy.
    Do NOT add any production code to app.py in this task.
  </action>
  <verify>
    <automated>/var/www/sites/face-almgp33/venv/bin/python -c "import pytest; print(pytest.__version__)" && /var/www/sites/face-almgp33/venv/bin/pytest tests/ --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `/var/www/sites/face-almgp33/venv/bin/python -c "import pytest"` exits 0 and prints `9.0.3`
    - `pytest.ini` exists at repo root and contains `testpaths = tests`
    - `tests/conftest.py` defines a `client` fixture that yields `app.app.test_client()`
    - conftest monkeypatches `app.DATA_DIR`/`app.USERS_FILE` (or sets them) so no test writes to the real `/var/www/sites/face-almgp33/data/` directory
    - `pytest tests/ --collect-only` exits 0 (collection succeeds even though tests will fail)
    - conftest contains the literal string `$2b$12$F0kPJqHfW3hWU9QJm7kCyee3xD/eYhdcBwwmWgBvXuC9wqwKyPMym`
  </acceptance_criteria>
  <done>pytest installed; conftest provides an isolated test client and seeding helpers; collection succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Write failing requirement tests including the end-to-end login happy path</name>
  <files>tests/test_auth.py, tests/test_rbac.py</files>
  <read_first>
    - tests/conftest.py (the fixtures and helpers created in Task 1 — `client`, `seed_users`, `BCRYPT_HASH_SUPERADMIN`)
    - .planning/phases/01-rbac-foundation/01-VALIDATION.md (Per-Task Verification Map: exact test function names required — test_login_valid, test_init_users_bootstrap, test_init_users_migrates_hash, test_privilege_hierarchy, test_session_contents, test_unauthenticated_redirect, test_public_routes, test_password_change, test_deactivated_user, test_post_login_redirect)
    - .planning/phases/01-rbac-foundation/01-CONTEXT.md (D-02 ROLE_HIERARCHY order; D-05 three-step check; D-08 redirect table; D-09 dashboard)
    - app.py (existing routes `/`, `/api/recognize` line 264, `/api/detect` line 243, `/login` line 136 — used by public-route and redirect tests)
  </read_first>
  <action>
    Create `tests/test_auth.py` and `tests/test_rbac.py` with one function per row of the 01-VALIDATION.md verification map, using the EXACT function names listed there.
    The end-to-end happy-path test `test_post_login_redirect` (in test_rbac.py) is the MVP failing test and must be REAL (not skipped): seed a `superadmin` user (active, password_hash=BCRYPT_HASH_SUPERADMIN), POST `/login` with username=`superadmin` password=`superadmin123`, assert HTTP 302 with `Location` ending `/admin`; then seed a `viewer` user, log in, assert redirect to `/dashboard`. This test WILL fail now because login still uses config.json and `/dashboard` does not exist — that is expected RED.
    `test_public_routes` must assert `GET /` returns 200 and `POST /api/recognize` / `POST /api/detect` do NOT redirect to `/login` (they may 400 on missing image, but never 302→/login).
    `test_unauthenticated_redirect` asserts `GET /admin` without session returns 302 to `/login`.
    `test_privilege_hierarchy` asserts a `dept_admin` can create a `viewer` via `POST /api/users` (expect 200/201) and a `viewer` posting to `/api/users` is rejected (expect 403).
    `test_init_users_migrates_hash` asserts that after `app.init_users()` runs against a config.json containing BCRYPT_HASH_SUPERADMIN, the created superadmin record's `password_hash` equals BCRYPT_HASH_SUPERADMIN byte-for-byte (no re-hash).
    Tests that depend on not-yet-built code (everything except the public-route checks) may use `@pytest.mark.xfail(reason="implemented in 01-02/01-03/01-04")` so the suite reports RED-expected rather than ERROR — but `test_public_routes` must PASS now (kiosk is already public). Do NOT mark `test_post_login_redirect` as skip; xfail is acceptable so the suite stays green-on-expected-failure.
    Each test function must contain a real assertion — no bare `pass` bodies.
  </action>
  <verify>
    <automated>/var/www/sites/face-almgp33/venv/bin/pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "def (test_login_valid|test_init_users_bootstrap|test_init_users_migrates_hash|test_privilege_hierarchy|test_session_contents|test_unauthenticated_redirect|test_public_routes|test_password_change|test_deactivated_user|test_post_login_redirect)" tests/` finds all 10 function names
    - `pytest tests/ -q` exits 0 (no collection ERRORs; failing requirement tests are xfail, not error)
    - `test_public_routes` is reported as PASSED (kiosk routes are already public in current app.py)
    - No test function body is a bare `pass` — `grep -c "    pass" tests/test_auth.py tests/test_rbac.py` returns 0 for standalone-pass bodies (every test has at least one `assert`)
    - `test_init_users_migrates_hash` references `BCRYPT_HASH_SUPERADMIN` from conftest
  </acceptance_criteria>
  <done>All 10 named requirement tests exist with real assertions; public-route test passes; the rest are xfail-RED awaiting Waves 1-3; suite collection and run exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real data/ | Tests must not read/write the production `data/` JSON files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-T1 | Tampering | conftest fixture writing to real `data/users.json` | mitigate | Monkeypatch `app.DATA_DIR`/`app.USERS_FILE` to pytest `tmp_path` before any client request |
| T-01-SC | Tampering | pip install pytest==9.0.3 | mitigate | pytest is the canonical test runner (github.com/pytest-dev/pytest), pinned version, verified in RESEARCH Package Legitimacy Audit; no [ASSUMED]/[SUS] runtime package introduced |
</threat_model>

<verification>
- `/var/www/sites/face-almgp33/venv/bin/pytest tests/ -q` exits 0
- Real `data/users.json` is NOT created by the test run (it does not exist yet and tests use tmp_path)
- All 10 requirement test function names from 01-VALIDATION.md are present
</verification>

<success_criteria>
pytest 9.0.3 installed; isolated test client and seeding helpers exist; the failing end-to-end login→redirect test defines the happy path; every Phase 1 requirement has a named test function; `test_public_routes` already passes proving kiosk stays public.
</success_criteria>

<output>
Create `.planning/phases/01-rbac-foundation/01-01-SUMMARY.md` when done.
</output>
