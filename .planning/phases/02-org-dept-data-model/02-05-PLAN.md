---
phase: 02-org-dept-data-model
plan: 05
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - app.py
  - templates/kiosk.html
autonomous: false
requirements: [KIOSK-01]
must_haves:
  truths:
    - "When a face is recognized at the kiosk, the employee's department name appears below their name"
    - "If the employee has no dept_id or the dept is not found, no department line is shown (graceful degradation)"
  artifacts:
    - path: "app.py"
      provides: "recognize() returns dept_name in JSON response"
      contains: "dept_name"
    - path: "templates/kiosk.html"
      provides: "Dept name rendered below employee name in result panel"
      contains: "empDept"
  key_links:
    - from: "app.py recognize"
      to: "depts.json via emp.dept_id"
      via: "load_depts lookup of emp dept_id"
      pattern: "depts\\.get\\(emp\\[\"dept_id\"\\]\\)"
    - from: "templates/kiosk.html"
      to: "data.dept_name"
      via: "showResult sets empDept text and visibility"
      pattern: "data\\.dept_name"
---

<objective>
Final vertical slice: the kiosk shows the department name on recognition. After this plan, when an employee checks in at the public kiosk their department name appears below their name and time. If they have no department (null dept_id or missing dept), nothing extra is shown — the kiosk degrades gracefully and the public check-in flow is otherwise unchanged.

Purpose: Deliver KIOSK-01 — the user-visible payoff of the org/dept model on the public surface.
Output: Extended `app.py` recognize() (dept_name lookup + response field), patched `templates/kiosk.html` (render dept_name). Ends with a human-verify checkpoint because the kiosk is camera/visual and cannot be fully unit-tested.
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-org-dept-data-model/02-RESEARCH.md
@.planning/phases/02-org-dept-data-model/02-PATTERNS.md
@.planning/phases/02-org-dept-data-model/02-CONTEXT.md
@.planning/phases/02-org-dept-data-model/02-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich recognize() with dept_name lookup</name>
  <files>app.py</files>
  <behavior>
    - On a successful match, recognize() looks up emp.dept_id in depts.json and includes dept.name as `dept_name` in the JSON response
    - When emp.dept_id is null or the dept is missing, dept_name is null (response still well-formed)
    - The existing kiosk is_late hardcoded "09:00:00" threshold is left unchanged
  </behavior>
  <read_first>
    - app.py recognize() body (lines ~473-509 — the emp-found branch and the final return jsonify payload)
    - .planning/phases/02-org-dept-data-model/02-PATTERNS.md (recognize() dept_name enrichment: exact insertion point + return payload extension)
    - .planning/phases/02-org-dept-data-model/02-RESEARCH.md (Code Example 2 KIOSK-01, Pitfall 4 — do NOT change is_late)
    - tests/test_org_dept.py (RED test test_recognize_dept_name — match the response key `dept_name` and the null-dept behavior)
  </read_first>
  <action>
    In recognize(), after the employee is confirmed found, add a dept lookup: `dept_name = None; if emp.get("dept_id"): dept = load_depts().get(emp["dept_id"]); if dept: dept_name = dept.get("name")`. Add `"dept_name": dept_name` as a new key to the existing `return jsonify({...})` payload (do not remove or rename any existing key — strictly additive). Leave the `is_late = now > "09:00:00"` line untouched (Pitfall 4 — schedule-aware kiosk late detection is Phase 3). If the test asserts dept_name via a lookup helper rather than full face decode, factor the lookup into a small helper (e.g. `_dept_name_for(emp)`) the test can call directly, while recognize() uses it inline.
  </action>
  <verify>
    <automated>/var/www/sites/face-almgp33/venv/bin/pytest tests/test_org_dept.py::test_recognize_dept_name -q 2>&1 | grep -v '^#' | grep -Eq "xpass|passed" && echo KIOSK_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - app.py recognize() return payload includes a `dept_name` key
    - dept_name resolves from load_depts().get(emp["dept_id"]).name when present, else None
    - The `is_late = now > "09:00:00"` line is unchanged from its Phase 1 value
    - test_recognize_dept_name passes
  </acceptance_criteria>
  <done>recognize() returns dept_name (or null) additively; kiosk late logic untouched; KIOSK-01 backend test GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Render dept_name below employee name in kiosk.html</name>
  <files>templates/kiosk.html</files>
  <read_first>
    - templates/kiosk.html (employee-card div around lines 156-179 and showResult() around lines 312-370 — the empName/empRole render pattern to mirror)
    - .planning/phases/02-org-dept-data-model/02-PATTERNS.md (kiosk.html dept_name patch: exact HTML insertion + JS display block)
    - .planning/phases/02-org-dept-data-model/02-UI-SPEC.md (Kiosk Dept Name Display: position below name, font-size 13px, color #7986cb, margin-top 4px, render only if non-empty)
  </read_first>
  <action>
    In the employee-card result panel, after the `.emp-role` element, add `<div class="emp-dept" id="empDept" style="display:none; font-size:13px; color:#7986cb; margin-top:4px;"></div>` (UI-SPEC Kiosk Dept Name Display tokens — do not introduce other colors/sizes).
    In showResult(), after the existing empRole assignment, add: read `data.dept_name`; if truthy, set `empDept` textContent and `style.display = ""`; else set `style.display = "none"` (graceful degradation — never show an empty dept line). Change nothing else in the kiosk flow (camera, cooldown, recognition loop all unchanged).
  </action>
  <verify>
    <automated>grep -q "empDept" templates/kiosk.html && grep -q "data.dept_name" templates/kiosk.html && grep -q "#7986cb" templates/kiosk.html && echo KIOSK_HTML_OK</automated>
  </verify>
  <acceptance_criteria>
    - templates/kiosk.html contains a `<div ... id="empDept" ...>` styled per UI-SPEC (font-size 13px, color #7986cb, margin-top 4px)
    - showResult() reads `data.dept_name` and toggles empDept visibility (hidden when falsy)
    - No other kiosk behavior changed
    - Automated grep prints KIOSK_HTML_OK
  </acceptance_criteria>
  <done>kiosk.html renders dept name below the employee name when present, hidden otherwise.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify kiosk dept name + full Phase 2 smoke</name>
  <what-built>recognize() returns dept_name; kiosk.html renders it below the employee name. This checkpoint also smoke-tests the whole phase end-to-end after migration.</what-built>
  <how-to-verify>
    1. Ensure migration has run on the live data: `cd /var/www/sites/face-almgp33 && venv/bin/python migrate.py` — confirm stdout shows the success summary "Миграция завершена: N сотрудников обновлено, W предупреждений." and that `data/employees_backup_*.json` was created.
    2. `pm2 restart face-recognition`.
    3. Open the kiosk (port 5051) and check in with a registered face → confirm the department name "Основной отдел" appears below the employee name and time.
    4. Log in as superadmin → /superadmin → create/edit/delete an org; verify stat cards show real numbers.
    5. Log in as a dept_admin (create one first via user management if needed) → /dept_admin → verify present/absent/late cards and edit an employee schedule.
    6. Confirm the kiosk public check-in still works for an employee whose dept lookup fails (no crash, no empty dept line).
  </how-to-verify>
  <resume-signal>Type "approved" once the dept name shows on the kiosk and all dashboards render, or describe any issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public kiosk → /api/recognize | unauthenticated public endpoint; must not leak data beyond the matched employee's own dept name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-I3 | Information Disclosure | recognize() exposing org/dept internals to public kiosk | mitigate | response includes only the matched employee's dept_name string; no org list, no ids, no other employees |
| T-02-D3 | Denial of Service | missing/null dept_id crashing recognize() | mitigate | guarded lookup (emp.get("dept_id") then dept truthiness); dept_name defaults None; kiosk JS hides element when falsy |
| T-02-R1 | Repudiation | kiosk change accidentally altering is_late/check-in logic | mitigate | change is strictly additive; is_late line left unchanged (Pitfall 4); human smoke-test confirms check-in still works |
| T-02-SC | Tampering | npm/pip installs | accept | No new packages this phase; legitimacy gate N/A |
</threat_model>

<verification>
- `/var/www/sites/face-almgp33/venv/bin/pytest tests/test_org_dept.py::test_recognize_dept_name -q` passes
- `/var/www/sites/face-almgp33/venv/bin/pytest tests/ -q` — full Phase 2 suite GREEN, no Phase 1 regression
- Human checkpoint: dept name visible on live kiosk; all role dashboards render; public check-in intact
</verification>

<success_criteria>
- KIOSK-01 delivered: dept name shows on recognition, hidden when absent
- Full Phase 2 test suite GREEN
- Live smoke test (migration → kiosk → dashboards) approved by human
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 02-05 scope)

New symbols created here (excluded from drift verification):
- app.py: `dept_name` key added to recognize() JSON response; optional helper `_dept_name_for`
- templates/kiosk.html: `empDept` element + dept_name render in showResult()
</artifacts_produced>

<output>
Create `.planning/phases/02-org-dept-data-model/02-05-SUMMARY.md` when done
</output>
