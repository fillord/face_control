---
phase: 08-i-want-to-radically-change-the-navigation-of-the-website-and
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - templates/superadmin.html
autonomous: true
requirements: [D-09, D-10, D-11, D-14]
must_haves:
  truths:
    - "superadmin.html extends base.html and renders inside the sidebar layout"
    - "The old <header> and .nav-tabs horizontal bar are removed from superadmin.html"
    - "Org and Users panels still switch via switchTab() driven by sidebar nav clicks"
    - "No #1565C0 or #0d47a1 blue remains in superadmin.html"
  artifacts:
    - path: "templates/superadmin.html"
      provides: "Superadmin page converted to base.html child (pilot conversion)"
      contains: "{% extends 'base.html' %}"
  key_links:
    - from: "templates/superadmin.html"
      to: "templates/base.html"
      via: "{% extends %} + {% block content %}"
      pattern: "extends 'base.html'"
    - from: "sidebar nav (base.html)"
      to: "superadmin panels"
      via: "switchTab() onclick"
      pattern: "function switchTab"
---

<objective>
Convert `templates/superadmin.html` to extend `base.html`. This is the PILOT conversion (most complex tab-based page) — it validates the base.html contract end-to-end before the batch conversions in plans 03–04. Remove the old document shell, top header, and horizontal nav-tabs; move shared CSS out (it now lives in base.html); keep page-specific CSS in `{% block head %}`; keep the org/users panels and JS; adapt `switchTab()` to drop references to the now-removed `.tab` elements (D-09, D-10, D-11, D-14).

Purpose: Prove the conversion pattern works for a multi-panel tabbed page so plans 03–04 can follow it mechanically.
Output: `templates/superadmin.html` (modified to extend base.html).
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-PATTERNS.md
@.planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-RESEARCH.md
@templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert superadmin.html to extend base.html — strip shell/header/nav-tabs, wrap content, adapt switchTab()</name>
  <read_first>
    - templates/superadmin.html (the ENTIRE current file — head, style, header, nav-tabs, panels, and all JS)
    - templates/base.html (to know which shared CSS classes already exist and must NOT be redefined: .stat-card, .table-card, table/th/td, .btn-primary, .btn-secondary, .btn-edit, .btn-delete, .form-group, .card, .badge*, .hidden, .toolbar, .page, .page-title)
    - .planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-PATTERNS.md (superadmin.html MODIFY section + switchTab JS adaptation, lines 308–360 and 635–647)
  </read_first>
  <action>
Rewrite `templates/superadmin.html` so it begins with `{% extends 'base.html' %}` and `{% block title %}Суперадмин — МедКонтроль{% endblock %}`.

Removals (D-10, D-11):
  1. Delete the `<!DOCTYPE html>`, `<html>`, `<head>`, `<meta>`, font links, `<body>` open/close, and `</html>` tags — base.html owns the document shell.
  2. Delete the `<header>` block (logo + user badge + logout) entirely — the sidebar replaces it (D-04).
  3. Delete the `<div class="nav-tabs">...</div>` horizontal tab bar — the sidebar nav replaces it.
  4. Delete the CSS reset (`* { box-sizing... }`) and `body {...}` rule and every shared component rule that now lives in base.html: `.stat-card`, `.stat-label`, `.stat-val*`, `.table-card`, bare `table`/`thead`/`th`/`td`, `.btn-primary`, `.btn-secondary`, `.btn-edit`, `.btn-delete`, `.form-group*`, `.card`, `.badge*`, `.hidden`, `.toolbar`, `.header`/`.nav-tabs`/`.tab`/`.logo*`/`.user-badge`/`.btn-logout` styles.

Retain in a `{% block head %}<style> ... </style>{% endblock %}` ONLY superadmin-specific CSS that base.html does not provide (e.g., any org-table column widths, modal/dialog styles, or panel-specific layout unique to this page). If after removing shared rules nothing page-specific remains, emit an empty `{% block head %}{% endblock %}`.

Wrap the surviving body markup in `{% block content %}` ... `{% endblock %}`:
  - First element: `<h1 class="page-title">Организации</h1>` (D-05 — section title is the first content element).
  - Then the existing `panelOrgs` and `panelUsers` `<div>`s with their `.hidden` toggling intact (the Users panel keeps its `{% if session.role == 'superadmin' %}` guard if present).
  - Then the existing `<script>` block(s) at the bottom, UNCHANGED except for the switchTab adaptation below.

switchTab() adaptation (research Pitfall 3 + PATTERNS.md lines 635–647): the `.tab` header elements (`tabOrgs`, `tabUsers`) no longer exist. In the `switchTab()` function, DELETE the lines that do `document.getElementById('tabOrgs').classList.toggle('active', ...)` and `tabUsers` equivalent. KEEP only the panel `.hidden` toggle lines: `document.getElementById('panelOrgs').classList.toggle('hidden', tab !== 'orgs');` and the `panelUsers` equivalent. The sidebar items in base.html call `switchTab('orgs')` / `switchTab('users')` via onclick, so the function name and signature must stay identical.

Color replacements (D-07, D-09) — search the file and replace every occurrence:
  - `#1565C0` → `var(--accent)`
  - `#0d47a1` → `var(--accent-hover)`
  - `rgba(21,101,192,0.1)` → `rgba(13,148,136,0.15)`; `rgba(21,101,192,0.07)` → `rgba(13,148,136,0.07)`
  - any `.stat-val.blue` class usage in markup → `.stat-val.teal` (base.html also aliases `.blue`, so either resolves to teal).
  </action>
  <verify>
    <automated>SECRET_KEY=testkey venv/bin/python -c "from app import app; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 templates/superadmin.html` is exactly `{% extends 'base.html' %}`.
    - `grep -c "{% block content %}" templates/superadmin.html` returns ≥1 and `grep -c "<!DOCTYPE" templates/superadmin.html` returns 0 and `grep -c "<header" templates/superadmin.html` returns 0.
    - `grep -c "class=\"nav-tabs\"" templates/superadmin.html` returns 0 (horizontal tab bar removed).
    - `grep -c "page-title" templates/superadmin.html` returns ≥1 (section title present as first content, D-05).
    - `grep -vE '^\s*/?\*|^\s*//' templates/superadmin.html | grep -c "#1565C0\|#0d47a1"` returns 0 (no old blue, comments excluded).
    - `grep -c "getElementById('tabOrgs')" templates/superadmin.html` returns 0 (dead .tab references removed) AND `grep -c "function switchTab" templates/superadmin.html` returns ≥1 (function kept).
    - `SECRET_KEY=testkey venv/bin/python -c "from app import app"` prints OK (Jinja2 parses extends/block correctly).
  </acceptance_criteria>
  <done>superadmin.html extends base.html, has no document shell / header / nav-tabs, exposes its content via {% block content %} with an h1.page-title, keeps switchTab() panel logic (sans dead .tab refs), and contains no old blue. App imports cleanly.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

Modified file: `templates/superadmin.html` — now a `base.html` child.

Symbols consumed from base.html: `{% block title %}`, `{% block head %}`, `{% block content %}`, `.page-title`, `.page`, `.stat-card`, `.table-card`, `.btn-primary`, `.badge*`, `.hidden`.
Symbols retained in this file: `switchTab(tab)` JS function (panel-toggle only), `panelOrgs` / `panelUsers` div IDs, all org/user fetch + CRUD JS functions (unchanged).
</artifacts_produced>

<verification>
- `SECRET_KEY=testkey venv/bin/python -c "from app import app; print('OK')"` — parses.
- First line is `{% extends 'base.html' %}`; no `<!DOCTYPE>`, `<header>`, or `.nav-tabs`.
- `grep -vE '^\s*/?\*|^\s*//' templates/superadmin.html | grep -c "#1565C0\|#0d47a1"` → 0.
</verification>

<success_criteria>
- superadmin.html extends base.html and renders inside the sidebar layout (D-10, D-14).
- Org/Users panels switch via sidebar-driven switchTab() (D-09).
- No old blue, no top header, no horizontal nav-tabs.
- App imports without Jinja2 error.
</success_criteria>

<output>
Create `.planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-02-SUMMARY.md` when done.
</output>
