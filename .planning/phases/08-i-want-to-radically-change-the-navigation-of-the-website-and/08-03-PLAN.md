---
phase: 08-i-want-to-radically-change-the-navigation-of-the-website-and
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - templates/org_admin.html
  - templates/dept_admin.html
autonomous: true
requirements: [D-09, D-10, D-11, D-14]
must_haves:
  truths:
    - "org_admin.html and dept_admin.html each extend base.html and render inside the sidebar layout"
    - "Old <header> and .nav-tabs bars are removed from both files"
    - "All in-page panels still switch via switchTab() triggered by sidebar nav clicks"
    - "No #1565C0 or #0d47a1 blue remains in either file"
  artifacts:
    - path: "templates/org_admin.html"
      provides: "Org admin page converted to base.html child"
      contains: "{% extends 'base.html' %}"
    - path: "templates/dept_admin.html"
      provides: "Dept admin page converted to base.html child"
      contains: "{% extends 'base.html' %}"
  key_links:
    - from: "templates/org_admin.html"
      to: "templates/base.html"
      via: "{% extends %} + {% block content %}"
      pattern: "extends 'base.html'"
    - from: "templates/dept_admin.html"
      to: "templates/base.html"
      via: "{% extends %} + {% block content %}"
      pattern: "extends 'base.html'"
---

<objective>
Convert `templates/org_admin.html` and `templates/dept_admin.html` to extend `base.html`, following the pilot pattern proven in plan 02. Both are multi-panel tabbed pages using `switchTab()`. Remove the document shell, top header, and horizontal nav-tabs; move shared CSS out; keep page-specific CSS in `{% block head %}`; keep panels + JS; adapt `switchTab()` to drop dead `.tab` references (D-09, D-10, D-11, D-14).

Purpose: Migrate the two org/dept admin dashboards to the new shell.
Output: `templates/org_admin.html`, `templates/dept_admin.html` (both modified).
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-PATTERNS.md
@templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert org_admin.html to extend base.html</name>
  <read_first>
    - templates/org_admin.html (the ENTIRE current file — head, style, header, nav-tabs, all panels: Отделы/Сотрудники/Настройки/Сводка/Пользователи/Отчёты, and all JS including switchTab and any include of reports_partial/timesheet_partial)
    - templates/base.html (shared CSS classes that must NOT be redefined)
    - .planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-PATTERNS.md (org_admin/dept_admin MODIFY section, lines 363–381, + switchTab adaptation lines 635–647)
    - .planning/phases/08-.../08-02-PLAN.md (the proven pilot conversion steps — apply the same recipe)
  </read_first>
  <action>
Rewrite `templates/org_admin.html` to begin with `{% extends 'base.html' %}` and `{% block title %}{{ org_name }} — МедКонтроль{% endblock %}`.

Apply the SAME conversion recipe as the superadmin pilot (plan 02):
  1. Remove document shell (`<!DOCTYPE>`, `<html>`, `<head>`, font links, `<body>`, `</html>`).
  2. Remove the `<header>` block entirely.
  3. Remove the `<div class="nav-tabs">...</div>` bar.
  4. Remove the CSS reset + `body` rule + all shared component CSS now in base.html (`.stat-card`, `.table-card`, `table/th/td`, `.btn-*`, `.form-group*`, `.card`, `.badge*`, `.hidden`, `.toolbar`, header/nav-tabs/logo/user-badge styles).
  5. Put any org_admin-specific CSS (e.g., kiosk-settings card styling, department-card grid, sortable-table arrow styles, summary-table styles) into `{% block head %}<style>...</style>{% endblock %}`. If none remains, emit `{% block head %}{% endblock %}`.
  6. Wrap surviving markup in `{% block content %}`: first element `<h1 class="page-title">Отделы</h1>` (D-05), then all existing panel `<div>`s (Отделы, Сотрудники, Настройки, Сводка, Пользователи, Отчёты) with `.hidden` toggling intact, then the existing `<script>` blocks.
  7. CRITICAL — preserve any `{% include 'reports_partial.html' %}` / `{% include 'timesheet_partial.html' %}` statements exactly where they are; those partials are updated separately in plan 06.

switchTab() adaptation: drop the `.tab` header `classList.toggle('active', ...)` lines (those `<span class="tab">` elements were in the removed nav-tabs bar); keep only the panel `.hidden` toggle lines for each panel (employees, settings, summary, users, reports, depts). Keep the function name/signature so the base.html sidebar `onclick="switchTab('employees')"` etc. still works.

Color replacements (D-07, D-09): `#1565C0` → `var(--accent)`, `#0d47a1` → `var(--accent-hover)`, `rgba(21,101,192,...)` → `rgba(13,148,136,...)`, `.stat-val.blue` → `.stat-val.teal`.
  </action>
  <verify>
    <automated>SECRET_KEY=testkey venv/bin/python -c "from app import app; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 templates/org_admin.html` is exactly `{% extends 'base.html' %}`.
    - `grep -c "<!DOCTYPE" templates/org_admin.html` returns 0, `grep -c "<header" templates/org_admin.html` returns 0, `grep -c "class=\"nav-tabs\"" templates/org_admin.html` returns 0.
    - `grep -c "{% block content %}" templates/org_admin.html` returns ≥1 and `grep -c "page-title" templates/org_admin.html` returns ≥1.
    - `grep -vE '^\s*/?\*|^\s*//' templates/org_admin.html | grep -c "#1565C0\|#0d47a1"` returns 0.
    - `grep -c "function switchTab" templates/org_admin.html` returns ≥1 (function preserved).
    - Any pre-existing `{% include` lines for partials still present: `grep -c "{% include" templates/org_admin.html` equals the count before conversion (no partial include removed).
    - `SECRET_KEY=testkey venv/bin/python -c "from app import app"` prints OK.
  </acceptance_criteria>
  <done>org_admin.html extends base.html, has no shell/header/nav-tabs, exposes content via {% block content %} with an h1.page-title, keeps switchTab() panel logic and partial includes, and contains no old blue. App imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Convert dept_admin.html to extend base.html</name>
  <read_first>
    - templates/dept_admin.html (the ENTIRE current file — head, style, header, nav-tabs: Посещаемость/Сотрудники/Табель, panels, and all JS)
    - templates/base.html (shared CSS classes that must NOT be redefined)
    - .planning/phases/08-.../08-03-PLAN.md Task 1 (apply the identical recipe to dept_admin)
  </read_first>
  <action>
Rewrite `templates/dept_admin.html` to begin with `{% extends 'base.html' %}` and `{% block title %}{{ dept_name }} — МедКонтроль{% endblock %}`.

Apply the SAME recipe as Task 1 / the pilot:
  1. Remove document shell, `<header>`, and `<div class="nav-tabs">` bar.
  2. Remove CSS reset + `body` + shared component CSS now in base.html.
  3. Page-specific CSS (attendance-table, employee-card, dept-specific styling) → `{% block head %}<style>...</style>{% endblock %}` (or empty block if none).
  4. Wrap markup in `{% block content %}`: first element `<h1 class="page-title">Посещаемость</h1>` (D-05), then existing panels (Посещаемость, Сотрудники) with `.hidden` toggling, then existing `<script>` blocks. Preserve any `{% include 'timesheet_partial.html' %}` exactly.
  5. switchTab() adaptation: drop dead `.tab` `classList.toggle('active', ...)` lines; keep panel `.hidden` toggles; keep function name/signature (base.html sidebar calls `switchTab('employees')`).
  6. Color replacements: `#1565C0` → `var(--accent)`, `#0d47a1` → `var(--accent-hover)`, `rgba(21,101,192,...)` → `rgba(13,148,136,...)`, `.stat-val.blue` → `.stat-val.teal`.
  </action>
  <verify>
    <automated>SECRET_KEY=testkey venv/bin/python -c "from app import app; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 templates/dept_admin.html` is exactly `{% extends 'base.html' %}`.
    - `grep -c "<!DOCTYPE" templates/dept_admin.html` returns 0, `grep -c "<header" templates/dept_admin.html` returns 0, `grep -c "class=\"nav-tabs\"" templates/dept_admin.html` returns 0.
    - `grep -c "{% block content %}" templates/dept_admin.html` returns ≥1 and `grep -c "page-title" templates/dept_admin.html` returns ≥1.
    - `grep -vE '^\s*/?\*|^\s*//' templates/dept_admin.html | grep -c "#1565C0\|#0d47a1"` returns 0.
    - `SECRET_KEY=testkey venv/bin/python -c "from app import app"` prints OK.
  </acceptance_criteria>
  <done>dept_admin.html extends base.html, no shell/header/nav-tabs, content in {% block content %} with h1.page-title, switchTab() preserved, no old blue. App imports cleanly.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

Modified files: `templates/org_admin.html`, `templates/dept_admin.html` — both `base.html` children.

Symbols consumed from base.html: `{% block title %}`, `{% block head %}`, `{% block content %}`, `.page-title`, `.page`, shared component classes.
Symbols retained: `switchTab(tab)` (panel-toggle only) in each file; all panel div IDs and CRUD/fetch JS unchanged; `{% include %}` of reports_partial.html / timesheet_partial.html preserved.
</artifacts_produced>

<verification>
- `SECRET_KEY=testkey venv/bin/python -c "from app import app; print('OK')"` — both parse.
- Each file: first line `{% extends 'base.html' %}`; no DOCTYPE/header/nav-tabs.
- `grep -vE '^\s*/?\*|^\s*//' templates/org_admin.html templates/dept_admin.html | grep -c "#1565C0\|#0d47a1"` → 0.
</verification>

<success_criteria>
- Both pages extend base.html and render in the sidebar layout (D-10, D-14).
- All panels switch via sidebar-driven switchTab() (D-09).
- Partial includes preserved; no old blue, header, or nav-tabs remain.
- App imports without Jinja2 error.
</success_criteria>

<output>
Create `.planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-03-SUMMARY.md` when done.
</output>
