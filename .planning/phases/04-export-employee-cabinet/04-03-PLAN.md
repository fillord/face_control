---
phase: 04-export-employee-cabinet
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - models.py
  - app.py
  - templates/employee.html
  - templates/admin.html
autonomous: true
requirements: [EMP-01, EMP-02, EMP-03]
must_haves:
  truths:
    - "An employee-role user can log in and lands directly on /employee (their cabinet), not the generic dashboard"
    - "The cabinet shows the employee's own T-13 row for the selected month, restricted to current and previous month only"
    - "Hovering a day cell shows Приход/Уход times pulled from face check-in records"
    - "Three stat cards show late, absence, and early-departure counts for the current month"
    - "An employee who manipulates the URL cannot view another employee's data (server-side scope via session)"
    - "An admin creating an employee-role user can supply the linking emp_id so the new account resolves to an Employee record"
  artifacts:
    - path: "models.py"
      provides: "User.emp_id FK column linking a login account to an Employee record"
      contains: "emp_id"
    - path: "app.py"
      provides: "Rewritten employee_page() route, 'employee' in ALLOWED_LOGIN_ROLES, employee login redirect, startup ALTER TABLE guard, emp_id accepted by create_user for employee role"
      contains: "def employee_page"
    - path: "templates/employee.html"
      provides: "Employee cabinet: stats cards + month selector + read-only single-row T-13 grid"
      contains: "Мой табель"
    - path: "templates/admin.html"
      provides: "emp_id input in the create-user form shown for the employee role, sent to /api/users"
      contains: "emp_id"
  key_links:
    - from: "app.py employee_page"
      to: "User.emp_id then Employee"
      via: "session user_id then User.emp_id then Employee.query.get"
      pattern: "user\\.emp_id"
    - from: "templates/employee.html"
      to: "times_by_date"
      via: "title attribute on day td cells"
      pattern: "Приход"
    - from: "app.py login dispatch"
      to: "employee_page"
      via: "redirect to employee_page when role is employee"
      pattern: "employee_page"
    - from: "templates/admin.html create-user form"
      to: "app.py create_user"
      via: "emp_id field posted to /api/users, persisted on User.emp_id when role == employee"
      pattern: "emp_id"
user_setup: []
---

## Phase Goal

**As an** employee, **I want to** log in and see my own T-13 timesheet with my exact arrival/departure times and a summary of my late/absent/early-departure counts, **so that** I can check my own attendance record without needing an admin.

<objective>
Employee Cabinet vertical slice (EMP-01, EMP-02, EMP-03). Add the missing `User.emp_id` FK (D-10), enable employee-role login (research Gap 2), rewrite the `/employee` route to render a dedicated cabinet (D-07), and build `employee.html` with three sections: stats cards, a month selector clamped to current/previous month, and a read-only single-row T-13 grid with arrival/departure tooltips (D-08, D-09, D-11). Also close the link gap (RESEARCH Open Question 3) by accepting an optional `emp_id` in `create_user` for employee-role accounts and exposing the field in the admin create-user form.

This is a complete end-to-end slice: after this plan, an employee who previously had no self-service view can log in and inspect their own record. It turns tests 4-6 of 04-01 from xfail to pass.

Purpose: EMP-01/02/03 — employee self-service is the second Phase 4 deliverable; the emp_id linking path makes employee accounts usable end-to-end.
Output: User.emp_id column + idempotent ALTER TABLE migration, ALLOWED_LOGIN_ROLES + login redirect edits, rewritten employee_page(), new templates/employee.html, emp_id plumbing in create_user + admin.html.
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-export-employee-cabinet/04-CONTEXT.md
@.planning/phases/04-export-employee-cabinet/04-RESEARCH.md
@.planning/phases/04-export-employee-cabinet/04-PATTERNS.md
@.planning/phases/04-export-employee-cabinet/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add User.emp_id column + enable employee login (schema + auth)</name>
  <files>models.py, app.py</files>
  <read_first>
    - models.py lines 29-41 (User class — add emp_id after dept_id, mirroring the Optional[str] String(36) pattern used by org_id/dept_id)
    - app.py line 80 (ALLOWED_LOGIN_ROLES tuple)
    - app.py lines 549-569 (login role dispatch: superadmin/org_admin/dept_admin redirects, else dashboard)
    - app.py startup block where db.create_all() is called (grep for create_all)
    - .planning/phases/04-export-employee-cabinet/04-RESEARCH.md Critical Findings Gaps 1 and 2, Pattern 4, Pitfalls 4 and 5
  </read_first>
  <action>
    1. models.py: add emp_id as Mapped[Optional[str]] mapped_column(String(36), nullable=True) to the User class immediately after the dept_id column (D-10 — links a login account to an Employee record). Optional and String are already imported.
    2. app.py line 80: change ALLOWED_LOGIN_ROLES from ("superadmin", "org_admin", "dept_admin") to also include "employee" as a fourth element (Gap 2 — employees currently cannot log in).
    3. app.py login dispatch (around line 566): add an elif role == "employee" branch returning redirect(url_for("employee_page")) before the existing else redirect to dashboard_page.
    4. app.py startup: immediately after the db.create_all() call, add an idempotent column migration guarded by OperationalError, because create_all() does NOT alter existing tables (Pitfall 4): import text and exc as sa_exc from sqlalchemy; in a try block open with db.engine.connect() as conn, execute text("ALTER TABLE user ADD COLUMN emp_id TEXT"), then conn.commit(); except sa_exc.OperationalError pass. This must run inside the same app context as create_all.
    Do not rename or remove any existing User column or login branch.
  </action>
  <verify>
    <automated>SECRET_KEY=test venv/bin/python -c "import app; from models import User; assert hasattr(User, 'emp_id'); assert 'employee' in app.ALLOWED_LOGIN_ROLES; print('SCHEMA_AUTH_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - User.emp_id exists as a nullable String(36) column in models.py
    - app.ALLOWED_LOGIN_ROLES contains "employee"
    - The login handler has an elif role == "employee" branch redirecting to employee_page
    - The startup block runs ALTER TABLE user ADD COLUMN emp_id TEXT inside try/except OperationalError (idempotent)
    - Importing app does not raise (startup migration is re-run-safe)
  </acceptance_criteria>
  <done>Employee-role users can log in and are routed to /employee; the user table has an emp_id column.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite employee_page() to render the cabinet</name>
  <files>app.py</files>
  <read_first>
    - app.py lines 729-734 (current /employee stub that renders dashboard.html — replace its body)
    - app.py lines 928-995 (/timesheet route: days build, ORM load, inline per-cell grid build, render_template call — adapt to single employee)
    - app.py lines 296-340 (compute_employee_totals returns days_worked/hours_worked/absences/late/vac_sick; compute_symbol; get_holidays_set at 219; _emp_to_dict at 101)
    - .planning/phases/04-export-employee-cabinet/04-RESEARCH.md Pattern 3 and Pitfall 6 (month clamp)
    - .planning/phases/04-export-employee-cabinet/04-PATTERNS.md (employee_page rewrite section: User+emp_id resolution, month clamp, single-employee ORM load, stats with early count, render_template args)
  </read_first>
  <action>
    Replace the body of employee_page() (keep the @app.route("/employee") and @require_role("employee") decorators):
    - Resolve user = User.query.get(session.get("user_id")) and username. emp_id = user.emp_id if user else None. If not emp_id, render_template("employee.html", username=username, emp=None, error="Ваш аккаунт не привязан к записи сотрудника. Обратитесь к администратору.") (UI-SPEC empty-state copy). emp_obj = Employee.query.get(emp_id); if None, return render_template("403.html"), 403 (D-10 IDOR guard).
    - Month clamp (Pitfall 6, D-08): now = datetime.now(); current_month = now.strftime("%Y-%m"); prev_month = (now.replace(day=1) - timedelta(days=1)).strftime("%Y-%m"); month_str = request.args.get("month", current_month); if month_str < prev_month or month_str > current_month set month_str = current_month; then parse year, month_num. Never compute prev_month from the client.
    - Build days via calendar.monthrange. Single-employee ORM load: AttendanceRecord.query.filter(emp_id == emp_id, date >= start_str, date <= end_str); build both the attendance dict (date then emp_id then check_in/check_out) and times_by_date (date then check_in/check_out) in the same loop (EMP-02). overrides = {emp_id: {r.date: r.symbol for r in TimesheetOverride.query.filter_by(emp_id=emp_id).all()}}. holidays_set = get_holidays_set(year).
    - Build the single grid row using the SAME inline per-cell loop as /timesheet (cells are {sym, auto, date} dicts): schedule = _emp_to_dict(emp_obj).get("schedule", default 09:00-18:00 Mon-Fri); for each day compute sym and auto; collect cells; symbols = [c["sym"] for c in cells]; totals = compute_employee_totals(symbols, schedule). grid_row = (emp_id, emp_obj.name, cells, totals).
    - Stats (EMP-03 — early count not returned by compute_employee_totals, research Gap 3): early_count = sum(1 for s in symbols if s in ("У", "ОУ")); stats = {"late": totals["late"], "absences": totals["absences"], "early": early_count}.
    - render_template("employee.html", username=username, emp_name=emp_obj.name, grid_row=grid_row, stats=stats, times_by_date=times_by_date, days=days, month_str=month_str, current_month=current_month, prev_month=prev_month, holidays_set=holidays_set, error=None).
  </action>
  <verify>
    <automated>SECRET_KEY=test venv/bin/python -m pytest "tests/test_export_employee.py::test_employee_cabinet_renders" "tests/test_export_employee.py::test_employee_tooltip_times" "tests/test_export_employee.py::test_employee_stats_counts" -q 2>&1 | grep -v '^#' | grep -Eq 'passed|xpassed' && echo CABINET_ROUTE_OK</automated>
  </verify>
  <acceptance_criteria>
    - employee_page() no longer renders dashboard.html; it renders employee.html with grid_row, stats, times_by_date
    - emp_id is read only from user.emp_id (session-derived), never from a URL param (IDOR mitigation)
    - month_str is clamped server-side to the range [prev_month, current_month]
    - stats dict contains late, absences, and early keys; early is computed from У/ОУ symbols
    - times_by_date maps date -> {check_in, check_out} for the employee's attendance rows
  </acceptance_criteria>
  <done>employee_page() returns the cabinet with the employee's own grid, stats, and tooltip time data; the 3 cabinet tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create templates/employee.html cabinet template</name>
  <files>templates/employee.html</files>
  <read_first>
    - templates/timesheet.html (copy the page skeleton, header structure, inline CSS for body/header/.selector-bar/.form-group/input[type=month]/.btn-primary/.table-card/table/th/td/.sym-cell/.sym-ou, and the sym_titles/sym_bg color maps used per cell)
    - templates/dashboard.html (header structure: logo, user badge, Выйти link — employee.html inherits this exact header per UI-SPEC note 6)
    - .planning/phases/04-export-employee-cabinet/04-UI-SPEC.md (Component Inventory: stats cards CSS, month selector, read-only grid rules; Copywriting Contract for all Russian labels; Interaction Contract)
    - .planning/phases/04-export-employee-cabinet/04-PATTERNS.md (employee.html section: stats cards markup, month selector, single-row grid with title tooltip)
  </read_first>
  <action>
    Create templates/employee.html (lang="ru"). Copy the inline CSS, header, and grid styling from timesheet.html so the design language matches (D-08, UI-SPEC). Structure top to bottom:
    1. Header identical to dashboard.html (logo, МедКонтроль — Личный кабинет title, user badge with {{ username }}, Выйти link to /logout). No back-to-admin link (UI-SPEC note 6).
    2. If error is set, render the empty-state message ({{ error }}) and stop (no grid). Otherwise:
    3. h1 page title "Мой табель".
    4. Stats grid: a .stats-grid with three .stat-card cells (UI-SPEC CSS: grid 3 columns, gap 12px). Card 1 label "Опоздания" value {{ stats.late }}; Card 2 label "Отсутствия" value {{ stats.absences }}; Card 3 label "Ранний уход" value {{ stats.early }}. All values use .stat-val.orange (#E65100) per UI-SPEC note 2; integer counts.
    5. Month selector: form method="GET" action="/employee" .selector-bar with label "Месяц", input type="month" name="month" value="{{ month_str }}" min="{{ prev_month }}" max="{{ current_month }}", and a "Показать" .btn-primary submit (D-08, UI-SPEC restricts to current/prev month).
    6. T-13 grid in a .table-card: define sym_titles and sym_bg maps copied from timesheet.html. thead row: "Сотрудник", then each day number from days, then "Я","Ч","П/НН","О","Б/К". Unpack grid_row as emp_id, emp_name, cells, totals. One data row: emp_name cell, then for each cell a td.sym-cell (add .sym-ou class when cell.sym == "ОУ") with background from sym_bg, and a title attribute: when times_by_date.get(cell.date) exists use "Приход: HH:MM / Уход: HH:MM" (slice check_in/check_out to [:5], use — when check_out is empty per D-11); when no time data use "Нет данных" (UI-SPEC note 3); cell text is cell.sym or empty. No onclick, no tabindex, no edit dropdown — read-only (UI-SPEC, D-09). Then totals columns days_worked/hours_worked/absences/late/vac_sick. Add a bottom totals row labeled "Итого" repeating the totals.
    Reuse only existing CSS tokens/classes plus the .stats-grid/.stat-card/.stat-label/.stat-val styles from UI-SPEC. Do not introduce new hex colors beyond the UI-SPEC palette.
  </action>
  <verify>
    <automated>SECRET_KEY=test venv/bin/python -m pytest tests/test_export_employee.py -q 2>&1 | grep -v '^#' | grep -Eq 'passed' && grep -q "Мой табель" templates/employee.html && grep -q "Приход" templates/employee.html && echo EMPLOYEE_HTML_OK</automated>
  </verify>
  <acceptance_criteria>
    - templates/employee.html exists, lang="ru", contains the title "Мой табель"
    - Three stat cards with labels Опоздания, Отсутствия, Ранний уход bound to stats.late, stats.absences, stats.early
    - Month input has name="month", value="{{ month_str }}", min="{{ prev_month }}", max="{{ current_month }}"
    - Day cells carry a title attribute producing "Приход:" tooltip text when time data exists, "Нет данных" otherwise
    - No onclick/tabindex/edit controls on grid cells (read-only)
    - All 6 Phase 4 tests pass (test_export_employee.py)
  </acceptance_criteria>
  <done>The employee cabinet renders stats, month selector, and a read-only single-row T-13 grid with arrival/departure tooltips; full Phase 4 test file is green.</done>
</task>

<task type="auto">
  <name>Task 4: Link employee accounts to Employee records via emp_id (create_user API + admin UI)</name>
  <files>app.py, templates/admin.html</files>
  <read_first>
    - app.py lines 1100-1148 (create_user route: JSON parse, validation, the db.session.add(User(...)) block at 1134-1142 — emp_id must be added there)
    - templates/admin.html lines 133-155 (the #createUserPanel create-user form: #newUsername, #newPassword, #newRole select) and lines 271-286 (the createUser() JS that POSTs {username, password, role} to /api/users)
    - .planning/phases/04-export-employee-cabinet/04-RESEARCH.md Open Question 3 (emp_id link is the minimal mechanism connecting a login account to an Employee record)
  </read_first>
  <action>
    Close the User↔Employee link gap (RESEARCH Open Question 3) so an admin-created employee account resolves to an Employee record:
    1. app.py create_user(): after computing new_dept_id, read `new_emp_id = (data.get("emp_id") or None)` and apply it ONLY when target_role == "employee" (for any other role force emp_id to None — non-employee accounts have no Employee link). Add `emp_id=new_emp_id` to the `db.session.add(User(...))` constructor call. Do not change any existing validation, role-hierarchy, or org/dept-scoping logic. Add `emp_id={new_emp_id!r}` to the existing USER_CREATED print line.
    2. templates/admin.html: in #createUserPanel, add a `.form-group` after the Роль select containing label "ID сотрудника" and `<input type="text" id="newEmpId" placeholder="emp_id" style="width:100%;">`, wrapped so it is shown only for the employee role — give the wrapping div `id="empIdGroup"` styled `display:none;` and add an `onchange="toggleEmpIdField()"` to the #newRole select. Add a small `toggleEmpIdField()` JS function that sets `#empIdGroup` display to "block" when `#newRole` value === "employee" else "none", and call it once at the end of toggleCreateForm() so state is correct when the panel opens.
    3. templates/admin.html createUser(): read `const empId = document.getElementById("newEmpId").value.trim();` and include `emp_id: empId` in the JSON body posted to /api/users (the server ignores it for non-employee roles).
    Deferral note (do NOT implement, record in SUMMARY): the #createUserPanel / USERS tab is currently gated `{% if session.role == 'superadmin' %}` (admin.html line 127) while create_user only lets superadmin create org_admin — so producing employee accounts through this tab requires later opening the USERS tab to org_admin/dept_admin. That visibility change touches RBAC tab-gating beyond Phase 4's locked decisions (CONTEXT.md has no decision for it) and is deferred to a follow-up RBAC task; the emp_id field and API plumbing added here make the link reachable wherever the create-user form renders.
  </action>
  <verify>
    <automated>grep -q "emp_id" app.py && grep -q "newEmpId" templates/admin.html && SECRET_KEY=test venv/bin/python -c "import app; print('CREATE_USER_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - create_user() persists emp_id on the new User only when role == "employee" (None otherwise)
    - templates/admin.html create-user form has an emp_id input (#newEmpId) shown only for the employee role
    - createUser() includes emp_id in the POST body to /api/users
    - importing app does not raise; existing create_user validation/scoping is unchanged
  </acceptance_criteria>
  <done>An admin creating an employee-role user can supply emp_id, which is stored on User.emp_id and lets employee_page() resolve the linked Employee; the panel-visibility limitation is recorded as a deferred RBAC follow-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| employee browser → /employee | month query param and any URL manipulation are untrusted; employee must only ever see their own record |
| login form → session | role drives the post-login redirect; employee role newly admitted |
| admin browser → create_user | emp_id field is untrusted input; only meaningful for employee role and scoped by existing role-hierarchy checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-EMP-IDOR | Spoofing/Information Disclosure | employee_page emp_id resolution | mitigate | emp_id read only from session user.emp_id; never accept emp_id from URL; Employee.query.get(None/invalid) returns 403 |
| T-04-EMP-MONTH | Tampering | month query param | mitigate | Server-side clamp to [prev_month, current_month]; prev_month computed server-side, never trusted from client (Pitfall 6) |
| T-04-EMP-AUTH | Elevation | ALLOWED_LOGIN_ROLES employee admission | mitigate | employee role still gated by @require_role("employee") on /employee; bcrypt + active-flag checks unchanged in login handler |
| T-04-EMP-LINK | Elevation | create_user emp_id param | mitigate | emp_id applied only when target_role == "employee"; create_user still enforces role-hierarchy + org/dept scoping; non-employee roles forced to emp_id=None |
| T-04-MIGRATE | Tampering | startup ALTER TABLE | accept | Additive nullable column only; guarded by OperationalError for idempotency; no data destruction |
| T-04-SC | Tampering | package installs | accept | No package installs in this plan (openpyxl handled in 04-02) |
</threat_model>

<verification>
- SECRET_KEY=test venv/bin/python -m pytest tests/test_export_employee.py -q → all 6 pass
- SECRET_KEY=test venv/bin/python -m pytest tests/ -q → full suite green (no regressions)
- `grep -q "emp_id" app.py && grep -q "emp_id" templates/admin.html` → emp_id link plumbing present (RESEARCH Open Q3)
- Manual: log in as an employee-role user linked via emp_id → land on /employee → see stats, month selector, own grid with hover tooltips
</verification>

<success_criteria>
- EMP-01: employee views own T-13 grid for current and previous month only (read-only)
- EMP-02: arrival/departure times appear as tooltips on day cells
- EMP-03: stats cards show late, absence, early-departure counts for current month
- Employee cannot view another employee's data via URL manipulation (403)
- An admin can link an employee account to an Employee record via emp_id at creation time (RESEARCH Open Q3); broader USERS-tab visibility deferred to a follow-up RBAC task
</success_criteria>

<output>
Create `.planning/phases/04-export-employee-cabinet/04-03-SUMMARY.md` when done
</output>
