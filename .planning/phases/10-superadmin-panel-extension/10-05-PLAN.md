---
phase: 10-superadmin-panel-extension
plan: 05
type: execute
wave: 5
depends_on: ["10-04"]
files_modified:
  - app.py
  - templates/superadmin.html
  - tests/test_sadm01.py
autonomous: true
requirements: [SADM-01]
must_haves:
  truths:
    - "GET /api/superadmin/export/xlsx?month=YYYY-MM downloads a valid .xlsx file"
    - "The workbook has one sheet per organization, named after the org (truncated to 31 chars, deduplicated)"
    - "Each org sheet contains a T-13 grid (employees as rows, calendar days as columns, totals) grouped by department"
    - "Shared attendance/overrides/holidays are loaded once before the org loop (not per employee)"
    - "When no organizations exist the workbook still saves with a placeholder sheet"
    - "GET /api/superadmin/export/xlsx returns 403 for any non-superadmin role"
  artifacts:
    - path: "app.py"
      provides: "GET /api/superadmin/export/xlsx multi-org T-13 export endpoint"
      contains: "superadmin_export_xlsx"
    - path: "templates/superadmin.html"
      provides: "month picker + download button in panelSystem"
      contains: "exportAllXlsx"
  key_links:
    - from: "app.py superadmin_export_xlsx"
      to: "_build_export_grid / compute_symbol / compute_employee_totals"
      via: "per-dept grid building reusing existing helpers"
      pattern: "_build_export_grid"
    - from: "templates/superadmin.html exportAllXlsx()"
      to: "GET /api/superadmin/export/xlsx"
      via: "window.location navigation with month param"
      pattern: "export/xlsx"
---

<objective>
Deliver SADM-01 (D-07): a global multi-org T-13 Excel export. Add GET /api/superadmin/export/xlsx that builds one worksheet per organization (each a department-grouped T-13 grid) for a selected month/year, and a download button with a month picker in the existing System tab.

Purpose: Superadmins need a single downloadable workbook covering every organization's timesheet for a month, rather than exporting each department separately.
Output: One export endpoint reusing the existing grid helpers, a month picker plus download button in panelSystem, and a pytest file that validates the workbook structure.
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-superadmin-panel-extension/10-CONTEXT.md
@.planning/phases/10-superadmin-panel-extension/10-RESEARCH.md
@.planning/phases/10-superadmin-panel-extension/10-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (new symbols introduced by this plan)

- superadmin_export_xlsx — Flask route handler for GET /api/superadmin/export/xlsx (app.py)
- exportAllXlsx — superadmin.html JS function
- exportMonth — month-picker input id in panelSystem (superadmin.html)
- tests/test_sadm01.py — new pytest file
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Backend — multi-org T-13 export endpoint + tests</name>
  <files>app.py, tests/test_sadm01.py</files>
  <read_first>
    - app.py lines 1562-1650 (_resolve_export_scope and _build_export_grid — reuse _build_export_grid, do NOT call _resolve_export_scope), lines 1655-1714 (export_timesheet_xlsx — BytesIO/send_file, header rows, totals columns)
    - app.py lines 163-180 (_emp_to_dict), lines 90-... (MONTHS_RU), lines 313-405 (compute_symbol, compute_employee_totals), get_holidays_set (DB-backed from plan 10-03)
    - models.py AttendanceRecord, TimesheetOverride, Organization, Department, Employee
    - app.py top imports (confirm Workbook, Font, Alignment, send_file, BytesIO, calendar, date, timedelta, datetime available; add calendar if missing)
    - .planning/phases/10-superadmin-panel-extension/10-RESEARCH.md (SADM-01 full code sketch, Pitfall 6 load-once, Pitfall 8 empty-workbook guard, Pitfall 2 sheet-name uniqueness)
    - .planning/phases/10-superadmin-panel-extension/10-PATTERNS.md (superadmin_export_xlsx analog, sheet-name uniqueness, empty-workbook guard, header/totals patterns)
  </read_first>
  <action>
    Add superadmin_export_xlsx() at GET /api/superadmin/export/xlsx guarded by require_role superadmin (D-07). Parse the month query param as YYYY-MM (default current month via datetime.now strftime); split into year and month_num; use calendar.monthrange to build the list of date objects for the month. Load shared data ONCE before the org loop (Pitfall 6): all AttendanceRecord rows for the month into an attendance dict keyed by date then emp_id with check_in/check_out; all TimesheetOverride rows into an overrides dict keyed by emp_id then date; holidays_set from get_holidays_set(year) (now DB-backed). Create a Workbook and remove the default sheet. Maintain a used_sheet_names set. For each Organization ordered by name: compute a sheet name from org.name truncated to 31 chars, and if it collides append a numeric suffix keeping within 31 chars (Pitfall 2); create the sheet; write a bold merged title row "ТАБЕЛЬ Т-13 — <org name> — <MONTHS_RU month> <year>"; for each Department in the org ordered by name with at least one employee, write a bold dept subtitle row, a bold header row (Сотрудник, then each day number, then Я, Ч, П/НН, О, Б/К), then employee rows built by reusing _build_export_grid(days, scoped_employees, attendance, overrides, holidays_set) where scoped_employees maps emp.id to _emp_to_dict(emp); write each row's symbols across the day columns and the five totals columns (days_worked, hours_worked, absences, late, vac_sick) following the existing totals-column write pattern; add a blank spacer row between departments. Set column A width to a readable value. After the loop, if the workbook has no sheets, create a single "Нет данных" sheet (Pitfall 8). Save to a BytesIO, seek(0), and return send_file as an attachment named like T13_ALL_<month>.xlsx.

    Create tests/test_sadm01.py with superadmin session injection and seeded orgs/depts/employees/attendance: (a) GET /api/superadmin/export/xlsx?month=YYYY-MM returns 200 with an xlsx content type and non-empty body; load the bytes with openpyxl.load_workbook and assert there is one sheet per seeded org and the sheet titles correspond to org names (truncated); (b) GET with no orgs seeded returns a workbook containing the "Нет данных" sheet; (c) GET as org_admin returns 403. Use BytesIO(resp.data) to load the workbook.
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && python -m pytest tests/test_sadm01.py -x -q && python -c "import ast; ast.parse(open('app.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_sadm01.py passes all three tests
    - grep -q "def superadmin_export_xlsx" app.py succeeds
    - grep -q "export/xlsx" app.py succeeds
    - The export reuses _build_export_grid (grep -q _build_export_grid app.py succeeds) and does NOT call _resolve_export_scope inside the new endpoint
    - python ast.parse of app.py exits 0
  </acceptance_criteria>
  <done>The endpoint returns a valid multi-sheet xlsx (one sheet per org, dept-grouped T-13 grids) for the requested month, with shared data loaded once, an empty-workbook guard, and 403 for non-superadmins.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — month picker + download button in System tab</name>
  <files>templates/superadmin.html</files>
  <read_first>
    - templates/superadmin.html panelSystem block (lines 68-95) and surrounding card markup
    - .planning/phases/10-superadmin-panel-extension/10-RESEARCH.md (SADM-01 frontend — month picker default current month, window.location download)
    - .planning/phases/10-superadmin-panel-extension/10-PATTERNS.md (form/card patterns)
  </read_first>
  <action>
    In the existing panelSystem tab, add a card titled for the global export (e.g. "Экспорт Т-13 — все организации") containing a month input id exportMonth (type month, defaulting to the current YYYY-MM) and a "Скачать" button calling exportAllXlsx (D-07). Add exportAllXlsx which reads exportMonth value (falling back to the current month) and navigates the browser to /api/superadmin/export/xlsx with the month query param via window.location so the file downloads. No new tab or nav link is required — the export lives in the existing System tab per D-07.
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 && grep -q exportMonth templates/superadmin.html && grep -q "function exportAllXlsx" templates/superadmin.html && grep -q "superadmin/export/xlsx" templates/superadmin.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/superadmin.html panelSystem contains a month input id exportMonth and a Скачать button
    - templates/superadmin.html defines function exportAllXlsx that navigates to /api/superadmin/export/xlsx with the month param
  </acceptance_criteria>
  <done>The System tab has a month picker and download button that fetches the global T-13 workbook for the chosen month.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to /api/superadmin/export/xlsx | authenticated read of all-org timesheet data exported to a file |
| employee names to openpyxl cells | DB strings written into spreadsheet cells |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-18 | Information Disclosure / Elevation | export endpoint | mitigate | require_role("superadmin") returns 403 for all other roles; verified by test (c) |
| T-10-19 | Tampering | Excel formula injection via names | mitigate | Names written as openpyxl cell values (data), not formulas; openpyxl does not evaluate leading "=" as a formula on plain string assignment |
| T-10-20 | Denial of Service | large dataset export | mitigate | Attendance/overrides loaded once before the org loop (Pitfall 6); no per-employee DB queries inside the grid loop |
| T-10-21 | Availability | empty workbook on no orgs | mitigate | "Нет данных" placeholder sheet guard prevents openpyxl save failure (Pitfall 8) |
| T-10-22 | Tampering | duplicate sheet names | mitigate | used_sheet_names dedup with numeric suffix within 31-char limit (Pitfall 2) |
</threat_model>

<verification>
- Run python -m pytest tests/test_sadm01.py -x -q — all tests pass (workbook loads, one sheet per org, empty-data placeholder, 403)
- Run python -m pytest tests/ -q — existing export tests unaffected (the per-dept export_timesheet_xlsx route is untouched)
- grep confirms the export endpoint and the System-tab month picker/button
</verification>

<success_criteria>
- GET /api/superadmin/export/xlsx returns a multi-sheet workbook (one sheet per org, dept-grouped T-13) for the selected month/year (SADM-01 / D-07)
- Shared data loaded once; sheet names deduplicated and truncated to 31 chars; empty-workbook guarded
- Endpoint returns 403 for non-superadmins
- System tab exposes a month picker and download button
</success_criteria>

<output>
Create .planning/phases/10-superadmin-panel-extension/10-05-SUMMARY.md when done.
</output>
