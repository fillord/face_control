---
phase: 08-i-want-to-radically-change-the-navigation-of-the-website-and
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/base.html
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-10, D-11, D-13, D-15]
must_haves:
  truths:
    - "templates/base.html exists and is a valid full HTML document with sidebar + content layout"
    - "Inter font loads from Google Fonts CDN on every page that extends base.html"
    - "Sidebar renders only the nav items appropriate to session.role for all 6 roles"
    - "User name, role, and logout link appear at the bottom of the sidebar (no top header bar)"
    - "On screens ≤768px the sidebar hides and a hamburger button toggles it via an overlay"
    - "All color tokens (teal accent, dark sidebar, light content) are defined as CSS custom properties"
  artifacts:
    - path: "templates/base.html"
      provides: "Shared layout shell: CSS token :root block, sidebar, role-aware nav, hamburger JS, content block"
      min_lines: 220
      contains: "{% block content %}"
  key_links:
    - from: "templates/base.html"
      to: "session.role"
      via: "Jinja2 {% if session.role == ... %} chain in sidebar nav"
      pattern: "session.role"
    - from: "templates/base.html"
      to: "Inter font"
      via: "Google Fonts CDN <link> in <head>"
      pattern: "fonts.googleapis.com/css2\\?family=Inter"
---

<objective>
Create `templates/base.html` — the single shared Jinja2 layout that every authenticated page in Phase 8 extends. This is the foundation of the entire navigation/design overhaul (D-10, D-11). It contains the Inter font CDN link, the complete CSS custom-property token system, the dark fixed sidebar with role-aware nav, the bottom-of-sidebar user footer, the light content area with `{% block content %}`, and the hamburger toggle JS for mobile.

Purpose: Without base.html, no child template can be converted. Every other plan in this phase depends on the tokens, layout classes, and sidebar defined here.
Output: `templates/base.html` (new file).
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-CONTEXT.md
@.planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-RESEARCH.md
@.planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create base.html document shell, CSS token :root block, and layout/sidebar/component CSS</name>
  <read_first>
    - templates/superadmin.html (lines 1–73: existing head, CSS reset, header, nav-tabs, and component CSS to model the shared blocks on)
    - templates/admin.html (badge CSS to fold into shared component CSS)
    - .planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-PATTERNS.md (the base.html section — exact CSS blocks to use verbatim)
    - .planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-RESEARCH.md (Patterns 3, 5, 7 — sidebar layout, token system, component redesign)
  </read_first>
  <action>
Create `templates/base.html` as a full HTML5 document with `<html lang="ru">`. In `<head>` include, in this order:
  1. `<meta charset="UTF-8">` and `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
  2. `<title>{% block title %}МедКонтроль{% endblock %}</title>`
  3. The three Inter font links (D-08): `<link rel="preconnect" href="https://fonts.googleapis.com">`, `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`, and `<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">`
  4. A single `<style>` block containing ALL shared CSS (see below)
  5. `{% block head %}{% endblock %}` AFTER the `<style>` block so child page-specific CSS can override shared CSS.

Inside the `<style>` block, in this order:
  a. CSS reset: `* { box-sizing: border-box; margin: 0; padding: 0; }`
  b. The complete `:root` token block from PATTERNS.md lines 88–114 — copy every variable verbatim, including: `--sidebar-bg: #0f172a;`, `--sidebar-hover: #1e293b;`, `--sidebar-text: #94a3b8;`, `--sidebar-text-active: #f1f5f9;`, `--sidebar-accent: #0d9488;`, `--sidebar-border: #1e293b;`, `--sidebar-width: 256px;`, `--content-bg: #f8fafc;`, `--card-bg: #ffffff;`, `--border: #e2e8f0;`, `--text-primary: #0f172a;`, `--text-secondary: #64748b;`, `--text-muted: #94a3b8;`, `--accent: #0d9488;`, `--accent-hover: #0f766e;`, `--accent-alt: #0891b2;`, `--accent-light: #ccfbf1;`, `--accent-text: #0f766e;`, the four status color pairs (`--green-bg #dcfce7`/`--green-text #15803d`, `--orange-bg #fff7ed`/`--orange-text #c2410c`, `--red-bg #fef2f2`/`--red-text #dc2626`, `--gray-bg #f8fafc`/`--gray-text #64748b`), `--radius-card: 12px;`, `--radius-btn: 8px;`, `--shadow-card: 0 1px 3px rgba(0,0,0,0.06), 0 1px 2px rgba(0,0,0,0.04);` (D-06, D-07).
  c. `body` rule: `font-family: 'Inter', 'Segoe UI', system-ui, sans-serif; background: var(--content-bg); color: var(--text-primary); min-height: 100vh;`
  d. Sidebar + content layout CSS from PATTERNS.md lines 117–158 verbatim: `.layout`, `.sidebar` (position:fixed, width var(--sidebar-width), background var(--sidebar-bg), z-index 200, transition transform 0.25s ease), `.sidebar-logo`, `.nav-section`, `.nav-item`, `.nav-item:hover`, `.nav-item.active` (background var(--sidebar-accent)), `.nav-icon`, `.sidebar-footer`, `.sidebar-user-name`, `.sidebar-user-role`, `.content` (margin-left var(--sidebar-width)), `.page` (max-width:1200px;margin:0 auto), `.page-title`, `.hamburger` (display:none default), and the `@media (max-width:768px)` block that translates the sidebar off-canvas, sets `.sidebar.open { transform: translateX(0); }`, sets `.content { margin-left:0; padding:16px; }`, shows `.hamburger { display:block; }`, and defines `.sidebar-overlay` (display:none default, .visible → display:block) (D-15).
  e. Shared component CSS from PATTERNS.md lines 160–218 verbatim: `.stat-card` + `.stat-label` + `.stat-val` (+ `.stat-val.teal/.green/.orange/.gray/.blue` where `.blue` aliases `var(--accent)`), `.table-card` + `table`/`thead`/`th`/`td`/`tr:last-child td`, `.btn-primary` + `:hover`, `.btn-secondary`, `.btn-edit` + `:hover`, `.btn-delete` + `:hover`, `.form-group` + label + inputs + `:focus` (border-color var(--accent); outline 2px solid rgba(13,148,136,0.15)), `.card`, `.badge` + all `.badge-*` variants, `.hidden`, `.toolbar` + label, `.error-msg`, `.form-actions`, `.stats-grid` + its `@media (max-width:700px)` rule.

Do NOT add any `#1565C0`, `#0d47a1`, or `'Segoe UI'`-only font-family anywhere — teal tokens and Inter-first stack only (D-07, D-08).
  </action>
  <verify>
    <automated>SECRET_KEY=testkey venv/bin/python -c "from app import app; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `templates/base.html` exists and starts with `<!DOCTYPE html>` and `<html lang="ru">`.
    - `grep -c "fonts.googleapis.com/css2?family=Inter:wght@400;500;600" templates/base.html` returns ≥1 (Inter font link present, D-08).
    - `grep -c -- "--accent: *#0d9488" templates/base.html` returns ≥1 and `grep -c -- "--sidebar-bg: *#0f172a" templates/base.html` returns ≥1 (teal accent + dark sidebar tokens, D-06/D-07).
    - `grep -c "{% block content %}" templates/base.html` returns ≥1 and `grep -c "{% block head %}" templates/base.html` returns ≥1.
    - `grep -c "@media (max-width: 768px)" templates/base.html` returns ≥1 and the file contains `.hamburger` and `.sidebar.open` rules (D-15).
    - `grep -vE '^\s*/?\*|^\s*//' templates/base.html | grep -c "#1565C0\|#0d47a1"` returns 0 (no old blue, comments excluded).
    - `SECRET_KEY=testkey venv/bin/python -c "from app import app"` prints OK with no Jinja2 syntax error.
  </acceptance_criteria>
  <done>base.html has a valid HTML shell, Inter font CDN link, the full CSS token :root block, sidebar/content layout CSS with mobile hamburger media query, and all shared component CSS. App imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add role-aware sidebar nav, logo, user footer, hamburger button, overlay, and toggle JS</name>
  <read_first>
    - templates/base.html (the file from Task 1 — see current head/style state before adding body)
    - .planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-PATTERNS.md (base.html role-aware sidebar nav block, lines 220–304, and document shell lines 38–77)
    - .planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-RESEARCH.md (Pattern 2 role strings + Pattern 4 hamburger JS)
  </read_first>
  <action>
Add the `<body>` of `templates/base.html`. Structure:

  1. `<div class="layout">` wrapping:
     - `<aside class="sidebar" id="sidebar">` containing, top to bottom:
       - `.sidebar-logo` div with the 🏥 МедКонтроль logo (an icon span + name; reuse the existing logo styling concept — a small icon box and "МедКонтроль" text in light color).
       - `<nav class="nav-section">` containing the role-aware nav from PATTERNS.md lines 220–294 VERBATIM. This is a `{% if session.role == 'superadmin' %} ... {% elif session.role == 'org_admin' %} ... {% elif session.role in ('dept_admin', 'viewer') %} ... {% elif session.role == 'hr_viewer' %} ... {% elif session.role == 'employee' %} ... {% endif %}` chain. Each role's exact nav items (D-02, D-03):
         · superadmin: Организации (`/superadmin`, active via `request.path == '/superadmin'`, onclick switchTab('orgs') when already on page), Пользователи (`/superadmin` onclick switchTab('users')), Регистрация (`/register`), Аудит (`/audit`), Табель Т-13 (`/timesheet`).
         · org_admin: Отделы (`/org_admin`), Сотрудники (onclick switchTab('employees')), Сводка (onclick switchTab('summary')), Отчёты (onclick switchTab('reports')), Пользователи (onclick switchTab('users')), Настройки (onclick switchTab('settings')), Табель Т-13 (`/timesheet`), Регистрация (`/register`).
         · dept_admin/viewer: Посещаемость (`/dept_admin`), Сотрудники (onclick switchTab('employees')), Табель Т-13 (`/timesheet`), Регистрация (`/register`).
         · hr_viewer: Табель Т-13 (`/timesheet`) only.
         · employee: Мой табель (`/employee`) only.
         Each item is an `<a class="nav-item">` with a `<span class="nav-icon">` Unicode glyph (⊞ 👥 ➕ 📋 📅 📊 📈 🔑 ⚙) + a Russian text label, and `{% if request.path == '<url>' %}active{% endif %}` on fixed-URL items.
         After the `{% endif %}`, add the universal item `<a href="/account" class="nav-item {% if request.path == '/account' %}active{% endif %}"><span class="nav-icon">⚙</span> Аккаунт</a>` visible to all roles (D-02 — account is universal).
       - `.sidebar-footer` div (D-04) with: `<div class="sidebar-user-name">{{ session.get('username', '') }}</div>`, `<div class="sidebar-user-role">{{ session.get('role', '') }}</div>`, and `<a href="/logout" ...>Выйти →</a>`. CRITICAL (research Pitfall 2): use `session.get('username', '')` NOT `{{ username }}` — `profile.html` and other routes do not pass a `username` variable, and `{{ username }}` would raise UndefinedError.
     - `<main class="content">` containing FIRST a `<button class="hamburger" id="hamburgerBtn" onclick="toggleSidebar()">☰</button>`, THEN `{% block content %}{% endblock %}`.
  2. AFTER `</div>` (closing `.layout`), as a sibling at root level (research Pitfall 4): `<div class="sidebar-overlay" id="sidebarOverlay" onclick="toggleSidebar()"></div>`.
  3. A `<script>` with `toggleSidebar()` (toggles `.open` on #sidebar and `.visible` on #sidebarOverlay) and an Escape-key listener that removes both classes (RESEARCH.md Pattern 4 verbatim).

Do NOT add any `<header>` top bar (D-04 — no top header at all). Do NOT pass or require any new Python template variable — the sidebar reads only `session` (research anti-pattern: passing nav context from Python).
  </action>
  <verify>
    <automated>SECRET_KEY=testkey venv/bin/python -c "from app import app; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "session.role == 'superadmin'" templates/base.html` returns ≥1 and the file also contains `session.role == 'org_admin'`, `session.role in ('dept_admin', 'viewer')`, `session.role == 'hr_viewer'`, and `session.role == 'employee'` (all 6 roles handled, D-02).
    - `grep -c "session.get('username', '')" templates/base.html` returns ≥1 and `grep -c "{{ username }}" templates/base.html` returns 0 (footer uses session.get, not the unpassed variable — research Pitfall 2).
    - File contains `id="sidebar"`, `id="sidebarOverlay"`, `id="hamburgerBtn"`, and a `toggleSidebar()` function definition.
    - File contains `href="/logout"` inside a `.sidebar-footer` block and contains NO `<header` tag (D-04).
    - `grep -c "switchTab('employees')" templates/base.html` returns ≥1 (org_admin/dept_admin sub-tab nav uses onclick switchTab).
    - `SECRET_KEY=testkey venv/bin/python -c "from app import app"` prints OK (no Jinja2 template-parse error from the `{% if %}` chain or block tags).
  </acceptance_criteria>
  <done>base.html renders a complete dark sidebar with role-scoped nav for all 6 roles, a bottom user/logout footer reading session.get('username'), a hamburger button + overlay, and working toggle JS. No top header exists. App imports cleanly.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New file: `templates/base.html`

CSS custom properties (in `:root`): `--sidebar-bg`, `--sidebar-hover`, `--sidebar-text`, `--sidebar-text-active`, `--sidebar-accent`, `--sidebar-border`, `--sidebar-width`, `--content-bg`, `--card-bg`, `--border`, `--text-primary`, `--text-secondary`, `--text-muted`, `--accent`, `--accent-hover`, `--accent-alt`, `--accent-light`, `--accent-text`, `--green-bg`, `--green-text`, `--orange-bg`, `--orange-text`, `--red-bg`, `--red-text`, `--gray-bg`, `--gray-text`, `--radius-card`, `--radius-btn`, `--shadow-card`

CSS classes: `.layout`, `.sidebar`, `.sidebar-logo`, `.nav-section`, `.nav-item`, `.nav-item.active`, `.nav-icon`, `.sidebar-footer`, `.sidebar-user-name`, `.sidebar-user-role`, `.content`, `.page`, `.page-title`, `.hamburger`, `.sidebar-overlay`, `.stat-card`, `.stat-label`, `.stat-val` (+ `.teal/.green/.orange/.gray/.blue`), `.table-card`, `.btn-primary`, `.btn-secondary`, `.btn-edit`, `.btn-delete`, `.form-group`, `.card`, `.badge` (+ variants), `.hidden`, `.toolbar`, `.error-msg`, `.form-actions`, `.stats-grid`

Jinja2 blocks (consumed by all child templates): `{% block title %}`, `{% block head %}`, `{% block content %}`

JS functions: `toggleSidebar()`
</artifacts_produced>

<verification>
- `SECRET_KEY=testkey venv/bin/python -c "from app import app; print('OK')"` — app imports, base.html parses.
- `grep -vE '^\s*/?\*|^\s*//' templates/base.html | grep -c "#1565C0\|#0d47a1"` → 0 (no old blue, comments excluded).
- File defines all 6 role branches and reads `session.get('username','')` in the footer.
- `grep -c "{% block content %}" templates/base.html` → ≥1.
</verification>

<success_criteria>
- `templates/base.html` exists, is a valid HTML document, and imports without Jinja2 error.
- Sidebar nav renders only role-appropriate items for all of superadmin, org_admin, dept_admin, viewer, hr_viewer, employee (D-02).
- Inter font CDN link present (D-08); teal accent + dark sidebar tokens defined (D-06, D-07).
- User info + logout at sidebar bottom; no top header (D-04).
- Hamburger toggle works at ≤768px via overlay (D-15).
- `{% block content %}` and `{% block head %}` exposed for child templates (D-10, D-11).
</success_criteria>

<output>
Create `.planning/phases/08-i-want-to-radically-change-the-navigation-of-the-website-and/08-01-SUMMARY.md` when done.
</output>
