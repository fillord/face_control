---
phase: 09-security-hardening-and-critical-bug-fixes
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - app.py
  - requirements.txt
  - templates/login.html
autonomous: false
requirements: [SEC-01, SEC-02]
user_setup: []
must_haves:
  truths:
    - "A 6th login attempt within 15 minutes from one IP is rejected with HTTP 429 and a Russian message"
    - "After 10 failed PIN attempts the registration token is locked (expired) and returns 429 JSON"
    - "Kiosk routes (/, /api/recognize, /api/detect) are never rate limited"
  artifacts:
    - path: "app.py"
      provides: "Flask-Limiter instance, @limiter limits on /login and verify_pin, 429 handler"
    - path: "requirements.txt"
      provides: "flask-limiter dependency pin"
  key_links:
    - from: "POST /login"
      to: "limiter '5 per 15 minutes'"
      via: "@limiter.limit decorator"
      pattern: "limiter.limit"
    - from: "POST /api/register/<reg_token>/verify_pin"
      to: "reg_token_expires set to past on breach"
      via: "limiter limit + on-breach token lock"
      pattern: "reg_token_expires"
---

<objective>
Add brute-force protection to the two credential-checking endpoints using Flask-Limiter (in-memory storage, single PM2 worker): 5 attempts / 15 min on POST /login (SEC-01) and 10 attempts on the PIN verify endpoint, after which the registration token is locked (SEC-02). Kiosk routes stay public and unlimited.

Purpose: Stop password and PIN guessing without Redis or new infrastructure.
Output: `flask-limiter` installed and pinned; a module-level `Limiter` wired into `app`; rate limits on `/login` and `verify_pin`; a 429 handler that renders the Russian login message or returns JSON; login.html shows the throttle message.
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-security-hardening-and-critical-bug-fixes/09-CONTEXT.md
@CLAUDE.md
@app.py
@templates/login.html
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify flask-limiter package legitimacy (no RESEARCH audit on file)</name>
  <what-built>No code yet — this gate precedes the first package install. No `## Package Legitimacy Audit` table exists for this phase, so `flask-limiter` is treated as [ASSUMED] and must be human-verified before install.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/Flask-Limiter/ — confirm it is the official project (maintainer "alisaifee", actively maintained, millions of downloads, links to github.com/alisaifee/flask-limiter).
    2. Confirm the version you will install is the current stable release (e.g. 3.x) and is compatible with Flask 3.1.
    3. Confirm there is no typosquat (name is exactly `Flask-Limiter`, import `flask_limiter`).
  </how-to-verify>
  <acceptance_criteria>Human confirms the package on pypi.org is legitimate and approves the install.</acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install + wire Limiter, rate-limit /login (SEC-01)</name>
  <read_first>
    - app.py lines 1-46 (imports + `app = Flask(__name__)` + config block — where Limiter is constructed)
    - app.py lines 635-685 (`login_page` route — the POST credential check)
    - templates/login.html lines 42-56 (error display + form)
    - requirements.txt (current pins)
  </read_first>
  <action>
    Install and pin: `venv/bin/pip install flask-limiter` then add the resolved `flask-limiter==<version>` line to requirements.txt.

    In app.py import `from flask_limiter import Limiter` and `from flask_limiter.util import get_remote_address`. Construct a module-level `limiter = Limiter(key_func=get_remote_address, app=app, storage_uri="memory://", default_limits=[])` immediately after `app.secret_key` is set (do NOT set a global default that would touch kiosk routes — leave `default_limits` empty so ONLY decorated routes are limited). Per D SEC-01 use in-memory storage (single PM2 worker, no Redis).

    Decorate `login_page` with `@limiter.limit("5 per 15 minutes", methods=["POST"])` so only POST attempts count and GET renders are unaffected. The decorator must sit below `@app.route(...)` and above `def login_page`.

    Add a 429 error handler `@app.errorhandler(429)` that branches on the request: for the login path it renders `login.html` with `error="Слишком много попыток. Попробуйте через 15 минут."` and status 429; for `/api/...` JSON paths it returns `jsonify({"error": "Слишком много попыток"})`, 429 (this also serves SEC-02 in Task 3). Branch on `request.path.startswith("/api/")`.

    Kiosk safety: confirm `/`, `/api/recognize`, `/api/detect` carry NO limiter decorator and that `default_limits` is empty so they remain unlimited per the phase constraint.
  </action>
  <acceptance_criteria>
    - `venv/bin/pip show flask-limiter` prints a Name/Version; `grep -ci 'flask-limiter' requirements.txt` returns 1.
    - `grep -c 'limiter.limit("5 per 15 minutes"' app.py` returns 1 and it decorates `login_page`.
    - `grep -c 'errorhandler(429)' app.py` returns 1.
    - Behavioral: `cd /var/www/sites/face-almgp33 && venv/bin/python -c "import app; c=app.app.test_client(); rs=[c.post('/login', data={'username':'x','password':'y'}) for _ in range(6)]; assert rs[-1].status_code==429, [r.status_code for r in rs]"` exits 0 (6th POST throttled). (Limiter counts across the loop in one process.)
    - Kiosk unaffected: `grep -n 'limiter.limit' app.py` shows NO decorator on `kiosk`, `recognize`, or `detect`.
  </acceptance_criteria>
  <done>flask-limiter installed/pinned; /login POST limited to 5 per 15 min per IP; 429 renders the Russian message; kiosk routes untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Rate-limit + lock PIN verify endpoint (SEC-02)</name>
  <read_first>
    - app.py lines 744-761 (`register_token_verify_pin` route)
    - app.py lines 479-495 (`is_reg_token_expired` — confirms how expiry is read so a past datetime locks the token)
    - models.py lines 81-94 (`Organization.reg_token_expires` column)
    - app.py errorhandler(429) added in Task 2
  </read_first>
  <action>
    Decorate `register_token_verify_pin` with `@limiter.limit("10 per 15 minutes", methods=["POST"])` per D SEC-02.

    Token lock on breach: when the 10-attempt limit is exceeded for this endpoint, the registration token must be locked by setting `reg_token_expires` to a past datetime so `is_reg_token_expired` treats it as expired. Implement this in the 429 handler (or via a limiter `on_breach`/`request_filter`): when `request.path` matches `/api/register/<reg_token>/verify_pin`, extract the `reg_token` from the path, load `Organization.query.filter_by(reg_token=reg_token).first()`, set `org.reg_token_expires` to an ISO datetime in the past (e.g. `(datetime.now() - timedelta(days=1)).isoformat()`), `db.session.commit()`, then return `jsonify({"error": "Слишком много попыток"}), 429`. Roll back on commit error. The JSON 429 branch added in Task 2 already covers the response shape for `/api/` paths.

    Do NOT change the normal 200/401/410 responses of the route — only the over-limit path locks the token.
  </action>
  <acceptance_criteria>
    - `grep -c 'limiter.limit("10 per 15 minutes"' app.py` returns 1 and decorates `register_token_verify_pin`.
    - `grep -c 'reg_token_expires' app.py` increased by >= 1 versus baseline (the lock-on-breach write).
    - Behavioral (requires a seeded org with a reg_token + reg_pin; use the test client and an in-context fixture or skip if no fixture): the 11th POST to the verify_pin path returns 429 with JSON `{"error": "Слишком много попыток"}` and a subsequent GET `/register/<reg_token>` renders the expired-token page (HTTP 410). At minimum verify statically: `venv/bin/python -c "import app"` imports cleanly and the decorator + lock logic are present via grep above.
    - `venv/bin/python -c "import app"` exits 0.
  </acceptance_criteria>
  <done>verify_pin limited to 10 attempts; on breach the registration token's reg_token_expires is set to the past (locked) and a 429 JSON error is returned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet→POST /login | Untrusted credential guesses |
| public internet→POST verify_pin | Untrusted 4-digit PIN guesses against a public registration link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Spoofing | /login password guessing | mitigate | SEC-01 — 5 attempts / 15 min per IP, 429 |
| T-09-05 | Spoofing | verify_pin 4-digit brute force (10k space) | mitigate | SEC-02 — 10 attempts then token lock (reg_token_expires→past) |
| T-09-06 | Denial of Service | limiter exhausting memory | accept | in-memory store, single PM2 worker, bounded keyspace by IP; acceptable for v1 |
| T-09-SC | Tampering | pip install flask-limiter | mitigate | blocking-human PyPI legitimacy checkpoint (Task 1) before install |
</threat_model>

<verification>
- `flask-limiter` installed and pinned in requirements.txt.
- 6th /login POST → 429 + Russian message; kiosk routes show no limiter decorator.
- 11th verify_pin POST → 429 JSON and token locked (reg_token_expires in the past).
- `venv/bin/python -c "import app"` clean. Post-deploy: `pm2 restart face-recognition`.
</verification>

<success_criteria>
SEC-01 and SEC-02 implemented exactly per 09-CONTEXT.md; in-memory limiter; kiosk routes unlimited; package legitimacy human-verified before install.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 09-02)

| Symbol / Route / Key | Type | Location | Notes |
|----------------------|------|----------|-------|
| `limiter` | Limiter instance | app.py | key=remote address, memory://, empty default_limits |
| `@limiter.limit("5 per 15 minutes")` | rate limit | app.py login_page | POST only |
| `@limiter.limit("10 per 15 minutes")` | rate limit | app.py verify_pin | POST only; locks token on breach |
| `@app.errorhandler(429)` | handler | app.py | HTML for /login, JSON for /api/* |
| `flask-limiter==<ver>` | dependency | requirements.txt | new pin |
</artifacts_produced>

<output>
Create `.planning/phases/09-security-hardening-and-critical-bug-fixes/09-02-SUMMARY.md` when done.
</output>
