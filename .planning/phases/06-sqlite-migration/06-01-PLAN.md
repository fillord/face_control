---
phase: 06-sqlite-migration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements.txt
  - models.py
  - tests/test_sqlite_migration.py
autonomous: true
requirements: [DB-01, DB-02]
must_haves:
  truths:
    - "flask-sqlalchemy 3.1.1 and sqlalchemy 2.0.x are importable in the venv"
    - "models.py defines all 9 ORM model classes and a single db = SQLAlchemy(...) object"
    - "Employee.label is a plain Integer column with autoincrement=False (LBPH label preserved, D-14)"
    - "tests/test_sqlite_migration.py exists and collects (xfail/skip until migrate_to_sqlite.py exists)"
  artifacts:
    - path: "models.py"
      provides: "db object + 9 ORM model classes (D-07)"
      contains: "class Employee(db.Model)"
    - path: "tests/test_sqlite_migration.py"
      provides: "DB-02 migration idempotency + zero-data-loss test scaffold"
      contains: "def test_"
  key_links:
    - from: "models.py"
      to: "flask_sqlalchemy.SQLAlchemy"
      via: "db = SQLAlchemy(model_class=Base)"
      pattern: "SQLAlchemy\\(model_class"
---

<objective>
Establish the ORM foundation for the SQLite migration: install flask-sqlalchemy, create `models.py` with all 9 model classes, and create the failing/skipped test scaffold for the migration script (Wave 0 test gap from VALIDATION.md).

Purpose: Define the database schema as Python model classes before any route or fixture code touches the database. This is the contract every downstream plan builds against (D-07).
Output: `models.py` (9 ORM models + db object), `requirements.txt` entry, `tests/test_sqlite_migration.py` (DB-02 scaffold).
</objective>

<artifacts_this_phase_produces>
This phase (06) creates the following NEW symbols. Downstream drift verification must treat these as newly-created, not pre-existing:

- **New file** `models.py` containing:
  - `db` (Flask-SQLAlchemy instance, `db = SQLAlchemy(model_class=Base)`)
  - `Base` (DeclarativeBase subclass)
  - Model classes: `User`, `Employee`, `EmployeeSchedule`, `Organization`, `Department`, `AttendanceRecord`, `LogEntry`, `TimesheetOverride`, `AppSetting`
- **New file** `migrate_to_sqlite.py` (created in plan 06-04) with `run_migration()`, `_load_json()`, per-table migrate functions
- **New file** `tests/test_sqlite_migration.py` (created here)
- **New file** `.gitignore` (created in plan 06-04)
- **New config** `app.config["SQLALCHEMY_DATABASE_URI"]`, `app.config["SQLALCHEMY_TRACK_MODIFICATIONS"]` in `app.py`
- **New env var** `DATABASE_URL` (optional; default `sqlite:///<abspath>/data/app.db`)
- **New conftest fixture behavior**: `tmp_data` becomes a stub returning `tmp_path`; `client` overrides `SQLALCHEMY_DATABASE_URI` to `sqlite:///:memory:`

REMOVED symbols (deleted across plans 03–04): `load_config`, `save_config`, `load_users`, `save_users`, `load_employees`, `save_employees`, `load_attendance`, `save_attendance`, `load_orgs`, `save_orgs`, `load_depts`, `save_depts`, `load_timesheet_overrides`, `save_timesheet_overrides`, `import fcntl`, and the module-level path constants `EMPLOYEES_FILE`, `ATTENDANCE_FILE`, `CONFIG_FILE`, `LOGS_FILE`, `USERS_FILE`, `ORGS_FILE`, `DEPTS_FILE`, `TIMESHEET_OVERRIDES_FILE`. `append_log` and `init_users`/`init_config` are kept but rewritten to use ORM.
</artifacts_this_phase_produces>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sqlite-migration/06-CONTEXT.md
@.planning/phases/06-sqlite-migration/06-RESEARCH.md
@.planning/phases/06-sqlite-migration/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install flask-sqlalchemy and record it in requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - /var/www/sites/face-almgp33/.planning/phases/06-sqlite-migration/06-RESEARCH.md (Standard Stack section + Package Legitimacy Audit — both packages approved despite SUS false-positive)
    - /var/www/sites/face-almgp33/requirements.txt (if it exists; create it if absent)
  </read_first>
  <action>
    Install `flask-sqlalchemy==3.1.1` into the project venv using `/var/www/sites/face-almgp33/venv/bin/pip install flask-sqlalchemy==3.1.1`. This pulls `sqlalchemy` 2.0.x transitively. Both packages are pre-approved in RESEARCH.md Package Legitimacy Audit (the SUS verdict is a confirmed false positive from a PyPI metadata gap — flask-sqlalchemy is the official Pallets project, sqlalchemy is the canonical 20-year ORM; no human checkpoint required per the audit). Then create or append to `requirements.txt` with pinned lines `flask-sqlalchemy==3.1.1` and `sqlalchemy==2.0.50` (use the actual resolved sqlalchemy version from `pip show sqlalchemy` if it differs from 2.0.50). Do NOT add fcntl-related or other packages.
  </action>
  <verify>
    <automated>/var/www/sites/face-almgp33/venv/bin/pip show flask-sqlalchemy | grep -q "Version: 3.1.1" && /var/www/sites/face-almgp33/venv/bin/python -c "import flask_sqlalchemy, sqlalchemy; print(sqlalchemy.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `pip show flask-sqlalchemy` reports `Version: 3.1.1`
    - `python -c "import flask_sqlalchemy, sqlalchemy"` exits 0
    - `requirements.txt` contains a line matching `flask-sqlalchemy==3.1.1`
  </acceptance_criteria>
  <done>flask-sqlalchemy 3.1.1 is importable from the venv and pinned in requirements.txt.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create models.py with all 9 ORM model classes</name>
  <files>models.py</files>
  <read_first>
    - /var/www/sites/face-almgp33/.planning/phases/06-sqlite-migration/06-PATTERNS.md (models.py section — exact column definitions copied 1:1 from JSON dict keys)
    - /var/www/sites/face-almgp33/.planning/phases/06-sqlite-migration/06-RESEARCH.md (Pattern 1: ORM Model Definition)
    - /var/www/sites/face-almgp33/data/employees.json (confirm Employee/EmployeeSchedule field names)
    - /var/www/sites/face-almgp33/data/orgs.json (confirm Organization Phase 5 token field names)
    - /var/www/sites/face-almgp33/data/users.json (confirm User field names)
  </read_first>
  <action>
    Create `models.py` defining a `Base(DeclarativeBase)` class and `db = SQLAlchemy(model_class=Base)`, then the 9 model classes per D-07 and the exact column specs in 06-PATTERNS.md. Use `Mapped[...]` + `mapped_column(...)` typed columns from SQLAlchemy 2.0. Tables and columns:
    - `User` (table `user`): id String(36) PK, username String(64) unique not-null, password_hash String(128) not-null, role String(20) not-null, active Boolean not-null default True, org_id String(36) nullable, dept_id String(36) nullable.
    - `Employee` (table `employee`): id String(36) PK, name String(128) not-null, role String(20) not-null default "employee", label Integer not-null `autoincrement=False` (CRITICAL D-14 — label is the LBPH recognizer id, NOT a PK, must not autoincrement), face_count Integer not-null default 0, registered_at String(32) nullable, org_id String(36) nullable, dept_id String(36) nullable.
    - `EmployeeSchedule` (table `employee_schedule`, D-02): id Integer PK autoincrement, emp_id String(36) not-null unique, start_time String(5) not-null default "09:00", end_time String(5) not-null default "18:00", work_days_json Text not-null default "[1,2,3,4,5]".
    - `Organization` (table `organization`, D-06): id String(36) PK, name String(128) not-null, description Text nullable, created_at String(32) nullable, kiosk_pin String(128) nullable, org_token String(8) nullable unique, reg_token String(8) nullable unique, reg_pin String(128) nullable, reg_token_expires String(32) nullable, kiosk_display_name String(128) nullable.
    - `Department` (table `department`): id String(36) PK, org_id String(36) not-null, name String(128) not-null, head_name String(128) nullable, created_at String(32) nullable.
    - `AttendanceRecord` (table `attendance_record`, D-01): id Integer PK autoincrement, emp_id String(36) not-null indexed, date String(10) not-null indexed, check_in_time String(8) nullable, check_out_time String(8) nullable.
    - `LogEntry` (table `log_entry`, D-03): id Integer PK autoincrement, ts String(32) not-null, event String(32) nullable, emp_id String(36) nullable, name String(128) nullable, confidence_raw Float nullable, confidence_pct Float nullable.
    - `TimesheetOverride` (table `timesheet_override`, D-04): composite PK on (emp_id String(36), date String(10)), symbol String(4) not-null, updated_by String(36) nullable, updated_at String(32) nullable.
    - `AppSetting` (table `app_setting`, D-05): key String(64) PK, value Text nullable.
    Use the project section-header style `# ─── Section Name ───` to group models. Do NOT import `app` in models.py (avoids circular import per RESEARCH.md Open Question 3). Do NOT define any `load_*`/`save_*` functions here.
  </action>
  <verify>
    <automated>/var/www/sites/face-almgp33/venv/bin/python -c "import models; assert hasattr(models,'db'); [models.__dict__[n] for n in ['User','Employee','EmployeeSchedule','Organization','Department','AttendanceRecord','LogEntry','TimesheetOverride','AppSetting']]; col=models.Employee.__table__.c['label']; assert col.autoincrement is False, 'label must not autoincrement'; print('models OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `models.py` contains `class Base(DeclarativeBase)` and `db = SQLAlchemy(model_class=Base)`
    - `models.py` defines all 9 classes: User, Employee, EmployeeSchedule, Organization, Department, AttendanceRecord, LogEntry, TimesheetOverride, AppSetting
    - `models.Employee.__table__.c['label'].autoincrement is False` (verified by the automated command)
    - `models.TimesheetOverride.__table__.primary_key` contains both `emp_id` and `date` columns
    - `models.py` does NOT contain `import app`
    - `import models` exits 0
  </acceptance_criteria>
  <done>models.py imports cleanly and exposes db + all 9 model classes with label non-autoincrement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/test_sqlite_migration.py scaffold for DB-02</name>
  <files>tests/test_sqlite_migration.py</files>
  <read_first>
    - /var/www/sites/face-almgp33/.planning/phases/06-sqlite-migration/06-RESEARCH.md (Pattern 5 + Migration Script Skeleton + Phase Requirements → Test Map row DB-02)
    - /var/www/sites/face-almgp33/.planning/phases/06-sqlite-migration/06-VALIDATION.md (Wave 0 Gaps)
    - /var/www/sites/face-almgp33/tests/conftest.py (current fixture structure — will be rewritten in plan 02; this test must not depend on file-path fixtures)
    - /var/www/sites/face-almgp33/data/employees.json, /var/www/sites/face-almgp33/data/users.json (record shapes the migration must preserve)
  </read_first>
  <behavior>
    - Test 1 (idempotency): running the migration twice produces the same row counts as running it once — no duplicate rows, no error on second run.
    - Test 2 (zero data loss): after migrating a known fixture JSON set, the number of rows in each table equals the number of source records (employees, users, orgs, depts, attendance records, log entries).
    - Test 3 (label preservation, D-14): an employee whose JSON `label` is a specific integer (e.g. 7) has exactly that `label` value in the `employee` table after migration — not auto-generated.
  </behavior>
  <action>
    Create `tests/test_sqlite_migration.py` with the three behaviors above. Because `migrate_to_sqlite.py` does not exist until plan 06-04, guard the whole module so it collects without erroring: at top, attempt `import migrate_to_sqlite` inside a try/except and set a module-level `MIGRATION_AVAILABLE` flag; decorate each test with `@pytest.mark.skipif(not MIGRATION_AVAILABLE, reason="migrate_to_sqlite.py created in plan 06-04")`. Each test should: build an in-memory SQLite app context using `models.db` (override `SQLALCHEMY_DATABASE_URI="sqlite:///:memory:"`, call `db.create_all()`), write small fixture JSON files into a `tmp_path/data` dir, invoke the migration entry point against that dir (the test should call the migration's per-table functions or a parametrizable `run_migration` that accepts a data dir — coordinate the signature so plan 06-04 implements `run_migration(data_dir=..., database_uri=...)`), then assert row counts and the label value. Use `models.Employee.query.count()` etc. Do NOT modify any existing `test_*.py` file (D-10).
  </action>
  <verify>
    <automated>SECRET_KEY=test /var/www/sites/face-almgp33/venv/bin/python -m pytest tests/test_sqlite_migration.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_sqlite_migration.py` collects without import/collection errors (skips are allowed while migrate_to_sqlite.py is absent)
    - The file defines at least 3 test functions covering idempotency, zero-data-loss row counts, and label preservation
    - `pytest tests/test_sqlite_migration.py -q` exits 0 (skipped == passing for this gate)
    - The file does not import or monkeypatch any `*_FILE` path constant from app.py
  </acceptance_criteria>
  <done>The DB-02 migration test scaffold collects and skips cleanly, ready to activate once migrate_to_sqlite.py exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip → venv | Package installation introduces third-party code into the runtime |
| Python import → ORM schema | Model definitions become the DB schema; an incorrect column type silently corrupts data on migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Employee.label column | mitigate | label declared Integer `autoincrement=False`; automated verify asserts `autoincrement is False` so LBPH ids cannot be silently renumbered (D-14) |
| T-06-02 | Tampering | models.py schema vs JSON shape | mitigate | column names copied 1:1 from confirmed data/*.json keys (06-PATTERNS.md); migration test (Task 3) asserts zero row loss |
| T-06-SC | Tampering | flask-sqlalchemy / sqlalchemy installs | accept | RESEARCH.md Package Legitimacy Audit confirms both are canonical Pallets/SQLAlchemy projects; SUS verdict is a documented false positive (PyPI download-count metadata gap), not actual suspicion — no human checkpoint required |
</threat_model>

<verification>
- `pip show flask-sqlalchemy` → Version: 3.1.1
- `python -c "import models"` exits 0 with all 9 models present and label non-autoincrement
- `pytest tests/test_sqlite_migration.py -q` exits 0 (skipped)
- Existing suite unaffected: `SECRET_KEY=test venv/bin/python -m pytest tests/ -q --tb=no` still reports the baseline 31 passed / 4 xfailed / 25 xpassed (models.py is not yet imported by app.py)
</verification>

<success_criteria>
- flask-sqlalchemy 3.1.1 installed and pinned
- models.py defines db + 9 ORM models matching the JSON schemas, with Employee.label non-autoincrement (D-14)
- tests/test_sqlite_migration.py scaffold collects and skips, ready for plan 06-04
</success_criteria>

<output>
Create `.planning/phases/06-sqlite-migration/06-01-SUMMARY.md` when done.
</output>
