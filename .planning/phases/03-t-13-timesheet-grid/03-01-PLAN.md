---
phase: 03-t-13-timesheet-grid
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_timesheet.py
  - tests/conftest.py
autonomous: true
requirements: [T13-01, T13-02, T13-03, T13-04, T13-05, T13-07, T13-08, DASH-04]
must_haves:
  truths:
    - "Running pytest tests/test_timesheet.py collects all 11 timesheet tests without import error"
    - "Every test asserts real T-13 behavior (not stub/placeholder values) and is marked xfail until its implementing wave lands"
    - "conftest monkeypatches TIMESHEET_OVERRIDES_FILE into the isolated tmp data dir using the hasattr guard"
  artifacts:
    - path: "tests/test_timesheet.py"
      provides: "Failing/xfail test suite for T13-01..08, DASH-04, D-05, D-08"
      contains: "def test_compute_symbol_all_cases"
    - path: "tests/conftest.py"
      provides: "TIMESHEET_OVERRIDES_FILE monkeypatch guard"
      contains: "TIMESHEET_OVERRIDES_FILE"
  key_links:
    - from: "tests/test_timesheet.py"
      to: "tests/conftest.py"
      via: "imports seed_users/seed_employees/seed_depts and uses client+tmp_data fixtures"
      pattern: "from tests.conftest import"
---

<objective>
Wave 0: create the failing/xfail pytest scaffold for the entire T-13 timesheet phase, plus the conftest monkeypatch guard for the new override file. This is the validation contract every later wave turns green.

Purpose: The MVP vertical-slice rule and the Nyquist rule both require the failing end-to-end test to exist before any production code. This plan satisfies `03-VALIDATION.md` Wave 0 requirements so every subsequent task has a concrete `<automated>` command to flip from xfail to pass.
Output: `tests/test_timesheet.py` (11 tests, all xfail) and a `TIMESHEET_OVERRIDES_FILE` guard in `tests/conftest.py`.
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-t-13-timesheet-grid/03-RESEARCH.md
@.planning/phases/03-t-13-timesheet-grid/03-VALIDATION.md
@.planning/phases/03-t-13-timesheet-grid/03-PATTERNS.md
@tests/conftest.py
@tests/test_org_dept.py
</context>

## Phase Goal

**As an** HR user (dept_admin, org_admin, or superadmin), **I want to** view and edit the T-13 timesheet grid for a department and month with symbols auto-derived from face check-in data, **so that** I can produce a statutory monthly attendance record.

## Artifacts this phase produces

New symbols created across Phase 3 (exclude from drift checks):
- `app.py`: `TIMESHEET_OVERRIDES_FILE` constant, `KZ_HOLIDAYS` dict, `MANUAL_SYMBOLS` set, `load_timesheet_overrides()`, `save_timesheet_overrides()`, `get_holidays_set(year)`, `is_holiday_year_missing(year)`, `compute_symbol(...)`, `compute_employee_totals(...)`, `compute_timesheet_grid(...)`, `compute_dept_summary(...)`, `timesheet()` route (`GET /timesheet`), `timesheet_override()` route (`POST/DELETE /api/timesheet/override`)
- `templates/timesheet.html` (new)
- `templates/org_admin.html`: DASH-04 "Сводка по отделам" section + optional `?summary_month=` handling in the `/org_admin` route
- `tests/test_timesheet.py` (new); `tests/conftest.py`: `TIMESHEET_OVERRIDES_FILE` monkeypatch guard
- `data/timesheet_overrides.json` (created at runtime on first override save)

<tasks>

<task type="auto">
  <name>Task 1: Add TIMESHEET_OVERRIDES_FILE guard to conftest</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (see existing ORGS_FILE/DEPTS_FILE hasattr guard block, ~lines 58-62, and the seeding helpers)
    - .planning/phases/03-t-13-timesheet-grid/03-PATTERNS.md (section "tests/conftest.py — TIMESHEET_OVERRIDES_FILE guard")
  </read_first>
  <action>
    In `tests/conftest.py`, inside the `tmp_data` fixture, immediately after the existing `DEPTS_FILE` hasattr guard block, add a guarded monkeypatch for `TIMESHEET_OVERRIDES_FILE`. Use the exact same pattern as the ORGS_FILE/DEPTS_FILE guards: `if hasattr(_app, "TIMESHEET_OVERRIDES_FILE"): monkeypatch.setattr(_app, "TIMESHEET_OVERRIDES_FILE", str(data_dir / "timesheet_overrides.json"))`. Include a one-line comment noting the constant does not exist in app.py until plan 03-02. Do NOT seed any timesheet_overrides.json by default — tests that need overrides seed it explicitly.
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 &amp;&amp; python -m pytest tests/ -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` contains the string `TIMESHEET_OVERRIDES_FILE`
    - `grep -c "hasattr(_app, \"TIMESHEET_OVERRIDES_FILE\")" tests/conftest.py` returns 1
    - The full existing test suite still passes/xfails (no collection errors introduced): `python -m pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>The TIMESHEET_OVERRIDES_FILE monkeypatch is present and guarded; existing tests are unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Write failing/xfail test suite for all T-13 requirements</name>
  <files>tests/test_timesheet.py</files>
  <read_first>
    - .planning/phases/03-t-13-timesheet-grid/03-RESEARCH.md (sections: "Validation Architecture" test map, "Pattern 1 compute_symbol", "Pattern 2 totals", "Pattern 5 /timesheet route", "Pattern 6 override API", "Common Pitfalls")
    - .planning/phases/03-t-13-timesheet-grid/03-VALIDATION.md (Per-Task Verification Map — exact test function names)
    - tests/test_org_dept.py (login-via-session_transaction pattern and @pytest.mark.xfail usage)
    - tests/conftest.py (seed_users, seed_employees, seed_depts, seed_orgs, BCRYPT_HASH_SUPERADMIN, client + tmp_data fixtures)
  </read_first>
  <action>
    Create `tests/test_timesheet.py` with these 11 test functions, each decorated `@pytest.mark.xfail(reason="implemented in 03-02/03-03/03-04", strict=False)` and asserting REAL behavior (never stub values). Use the exact function names from 03-VALIDATION.md:
    (1) `test_compute_symbol_all_cases` — import `compute_symbol` from app; assert it returns Я/О/У/В/НН/ОУ for crafted (day_date, emp_id, attendance, overrides, schedule, holidays_set) inputs, and that an override in the overrides dict wins over auto-derivation (T13-02).
    (2) `test_symbol_auto_derivation` — Я for on-time check-in on a work day; НН for a past work day with no record; В for a Saturday (isoweekday 6) given work_days [1..5] (T13-03).
    (3) `test_symbol_late` — first check-in `"09:16:00"` with schedule start `"09:00"` returns О; `"09:15:00"` returns Я (boundary: >15 min, not >=) (T13-04).
    (4) `test_symbol_early_and_combined` — check-out `"17:44:00"` with schedule end `"18:00"` returns У; late check-in + early check-out returns ОУ (T13-05).
    (5) `test_totals_row` — import `compute_employee_totals`; for a symbol list mixing Я/О/У/ОУ/НН/Б/К assert days_worked counts Я+О+У+ОУ, late counts О+ОУ, absences count П+НН, vac_sick counts Б+К, hours_worked = days_worked × daily_hours from a 09:00–18:00 schedule (9h) (T13-07).
    (6) `test_kz_holidays` — import `get_holidays_set` and `is_holiday_year_missing`; assert `"2025-01-01"` is in the 2025 set, `is_holiday_year_missing(2099)` is True, and `compute_symbol` returns В for a 2025 holiday date on a work day (T13-08).
    (7) `test_timesheet_renders` — seed superadmin user + one dept + one employee (with schedule + dept_id); login via session_transaction (set user_id, role=superadmin, org_id, dept_id); `GET /timesheet?dept_id=<id>&month=2025-06` returns 200 and the response body contains the employee name and the substring "Итого" (T13-01).
    (8) `test_override_scope_403` — seed a dept_admin in dept-A and an employee in dept-B; login as the dept_admin; `POST /api/timesheet/override` with the dept-B employee id + a valid symbol returns 403 (D-05 / horizontal privesc threat).
    (9) `test_timesheet_scope_isolation` — login as dept_admin whose session dept_id is dept-A; `GET /timesheet?dept_id=dept-B&month=2025-06` must NOT leak dept-B data: assert dept-A is the rendered scope (dept_admin param is ignored and forced to session dept_id, or returns 403 on explicit mismatch) (D-08).
    (10) `test_dash04_summary` — seed org_admin + 2 depts + employees with attendance; `GET /org_admin?summary_month=2025-06` returns 200 and body contains "Сводка по отделам" and an attendance-rate value computed as days-with-Я / work-days × 100 (DASH-04).
    (11) `test_override_invalid_symbol_422` — login as dept_admin; `POST /api/timesheet/override` with symbol `"Я"` (not in MANUAL_SYMBOLS) returns 422 (V5 input validation / symbol injection threat).
    Use `with client.session_transaction() as sess:` to set session fields (matching test_org_dept.py). Do NOT use real face/CV operations. Add a module docstring listing covered requirements and noting all tests are xfail until their implementing plan lands.
  </action>
  <verify>
    <automated>cd /var/www/sites/face-almgp33 &amp;&amp; python -m pytest tests/test_timesheet.py -q 2>&amp;1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_timesheet.py -q` collects exactly 11 tests with no import/collection errors and reports them as xfailed (0 failed, 0 passed unexpectedly is acceptable since strict=False)
    - `grep -c "def test_" tests/test_timesheet.py` returns 11
    - File contains all of: `def test_compute_symbol_all_cases`, `def test_symbol_auto_derivation`, `def test_symbol_late`, `def test_symbol_early_and_combined`, `def test_totals_row`, `def test_kz_holidays`, `def test_timesheet_renders`, `def test_override_scope_403`, `def test_timesheet_scope_isolation`, `def test_dash04_summary`, `def test_override_invalid_symbol_422`
    - No test asserts a hardcoded placeholder (e.g. `assert True`) as its sole assertion
  </acceptance_criteria>
  <done>All 11 tests collect and run as xfail; each encodes a real T-13 behavior contract from RESEARCH.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app import | Tests import app.py symbols that do not exist yet; guarded with xfail + hasattr so collection never crashes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | conftest path monkeypatch | mitigate | Guard `TIMESHEET_OVERRIDES_FILE` with `hasattr` so the constant being absent before 03-02 cannot break the existing suite |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH.md Package Legitimacy Audit confirms stdlib only |
</threat_model>

<verification>
- `python -m pytest tests/ -q` exits 0 (no collection errors across the whole suite)
- `python -m pytest tests/test_timesheet.py -q` reports 11 xfailed tests
</verification>

<success_criteria>
- 11 named timesheet tests exist and run as xfail
- conftest guards TIMESHEET_OVERRIDES_FILE
- No existing test regressed
</success_criteria>

<output>
Create `.planning/phases/03-t-13-timesheet-grid/03-01-SUMMARY.md` when done
</output>
