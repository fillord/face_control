---
phase: 09-security-hardening-and-critical-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - models.py
autonomous: true
requirements: [BUG-01, BUG-02, BUG-03, REL-01, REL-02, SEC-04, PERF-01]
must_haves:
  truths:
    - "Late-arrival flags use each employee's own schedule start (+15 min grace), not a hardcoded 09:00"
    - "Department summary shows the real department name, never a raw UUID"
    - "GET /health returns 200 {status:ok,db:connected} when the DB is reachable, 503 when not"
    - "T-13 timesheet has Kazakhstan holiday data for 2027"
    - "Session cookies carry Secure, HttpOnly, and SameSite=Lax flags"
    - "AttendanceRecord is served by a single composite (emp_id, date) index"
  artifacts:
    - path: "app.py"
      provides: "schedule-aware late logic, /health route, KZ_HOLIDAYS[2027], cookie flags, PERF-01 index migration"
    - path: "models.py"
      provides: "AttendanceRecord composite index ix_attendance_emp_date"
  key_links:
    - from: "recognize()"
      to: "_time_threshold(schedule.start, 15)"
      via: "is_late computation"
      pattern: "_time_threshold"
    - from: "get_stats()"
      to: "employees[eid]['schedule']"
      via: "per-employee late threshold"
      pattern: "_time_threshold"
    - from: "compute_dept_summary()"
      to: "Department.query"
      via: "dept_name_map lookup"
      pattern: "dept_name_map"
---

<objective>
Fix the three confirmed bugs and ship the no-new-dependency reliability/config/perf items: schedule-aware late detection (BUG-01, BUG-02), real department names in summaries (BUG-03), a public /health endpoint (REL-01), 2027 KZ holidays (REL-02), session cookie security flags (SEC-04), and the AttendanceRecord composite index (PERF-01).

Purpose: These are the highest-priority correctness and hardening items, all small inline edits to `app.py` and `models.py` with no new runtime dependency. They run first because every later plan also edits `app.py`.
Output: Corrected `app.py` (recognize, get_stats, compute_dept_summary, config block, /health route, KZ_HOLIDAYS, startup index migration) and `models.py` (AttendanceRecord index).
</objective>

<execution_context>
@/var/www/sites/face-almgp33/.claude/gsd-core/workflows/execute-plan.md
@/var/www/sites/face-almgp33/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-security-hardening-and-critical-bug-fixes/09-CONTEXT.md
@CLAUDE.md
@app.py
@models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schedule-aware late detection (BUG-01, BUG-02)</name>
  <read_first>
    - app.py lines 244-256 (`_time_threshold` helper — reuse, do not reimplement)
    - app.py lines 2975-2981 (`is_late` hardcode in `recognize()`)
    - app.py lines 3159-3198 (`employees` dict build + `is_late` hardcode in `get_stats()`)
    - app.py lines 115-138 (`_emp_to_dict` — confirms every emp dict carries a `schedule` sub-dict with `start`)
  </read_first>
  <action>
    In `recognize()` (app.py ~2980) replace `is_late = now > "09:00:00"` with a schedule-aware computation: read `schedule = emp_dict.get("schedule", {})`, take `start = schedule.get("start", "09:00")`, and set `is_late = now > _time_threshold(start, 15)`. Reuse the existing `_time_threshold(base_hhmm, delta_minutes)` helper; when no schedule is present the default "09:00" yields the "09:15:00" grace fallback per D BUG-01. Do NOT change the response shape — `is_late and event == "check_in"` stays.

    In `get_stats()` (app.py ~3197) replace `if check_in > "09:00:00":` with a per-employee threshold. The `employees` dict (built at ~3159 via `_emp_to_dict`) already carries each employee's `schedule`, so compute `start = employees[eid]["schedule"].get("start", "09:00")` and compare `if check_in > _time_threshold(start, 15):`. No extra DB query is needed because `employees[eid]` is already in scope. This implements D BUG-02 using the available in-memory schedule rather than a second query.
  </action>
  <acceptance_criteria>
    - `grep -c '> "09:00:00"' app.py` returns 0 (both hardcoded literals gone).
    - `grep -c '_time_threshold' app.py` increases by at least 2 versus baseline (recognize + get_stats now call it).
    - `cd /var/www/sites/face-almgp33 && venv/bin/python -c "import ast; ast.parse(open('app.py').read())"` exits 0 (syntactic validity).
    - `venv/bin/python -c "import app"` imports without error (with SECRET_KEY set in env).
  </acceptance_criteria>
  <done>Late detection in both recognize() and get_stats() derives the threshold from the employee's schedule start + 15 minutes; no "09:00:00" string literal remains.</done>
</task>

<task type="auto">
  <name>Task 2: Real dept names (BUG-03) + KZ 2027 (REL-02) + cookie flags (SEC-04)</name>
  <read_first>
    - app.py lines 351-411 (`compute_dept_summary` — the `dept_name = dept_id` fallback at ~388)
    - app.py lines 207-229 (`KZ_HOLIDAYS` dict, ends at 2026)
    - app.py lines 29-46 (Flask config block — where to add cookie flags)
    - models.py lines 99-108 (`Department` model — confirms `.name` attribute)
  </read_first>
  <action>
    BUG-03: In `compute_dept_summary()` (app.py ~378-388) build a name map once before the dept loop: `dept_name_map = {d.id: d.name for d in Department.query.filter_by(org_id=org_id).all()}`. Replace the `dept_name = dept_id  # fallback` line with `dept_name = dept_name_map.get(dept_id, dept_id)`. `Department` is already imported (app.py line 17). This implements D BUG-03 via the cached-map variant.

    REL-02: Add a `2027` key to `KZ_HOLIDAYS` (app.py ~228) mirroring the 2026 entry's recurring dates exactly (Jan 1-2, Jan 7, Mar 8, Mar 21-23, May 1, May 7, May 9, Jul 6, Aug 30, Oct 25, Dec 1, Dec 16-17) as ISO date strings `"2027-MM-DD"`, keeping the identical list-of-strings format used by 2024-2026. Mirroring 2026 guarantees `is_holiday_year_missing(2027)` is False and the format stays consistent per D REL-02.

    SEC-04: In the Flask config block (after line 38, near the other `app.config[...]` lines) add `app.config["SESSION_COOKIE_SECURE"] = True`, `app.config["SESSION_COOKIE_HTTPONLY"] = True`, and `app.config["SESSION_COOKIE_SAMESITE"] = "Lax"`. Config-only, no logic change, per D SEC-04 (nginx terminates SSL so Secure is safe).
  </action>
  <acceptance_criteria>
    - `grep -c 'dept_name_map' app.py` is >= 2 (map built + used); `grep -c 'dept_name = dept_id' app.py` returns 0.
    - `cd /var/www/sites/face-almgp33 && venv/bin/python -c "import app; assert app.is_holiday_year_missing(2027) is False; assert '2027-01-01' in app.get_holidays_set(2027)"` exits 0.
    - `grep -c 'SESSION_COOKIE_SECURE\|SESSION_COOKIE_HTTPONLY\|SESSION_COOKIE_SAMESITE' app.py` returns 3.
    - `venv/bin/python -c "import app"` imports cleanly.
  </acceptance_criteria>
  <done>Dept summaries show real names; KZ_HOLIDAYS[2027] present and complete; three session-cookie security flags set in config.</done>
</task>

<task type="auto">
  <name>Task 3: /health endpoint (REL-01) + AttendanceRecord composite index (PERF-01)</name>
  <read_first>
    - app.py lines 1-18 (imports — `from sqlalchemy import text`, `from sqlalchemy import exc as sa_exc` already present)
    - app.py lines 615-618 (`/` kiosk route — pattern for a public, no-auth route)
    - app.py lines 3257-3279 (startup `with app.app_context()` block + existing idempotent ALTER TABLE migration pattern)
    - models.py lines 110-124 (`AttendanceRecord` — column-level `index=True` on emp_id and date)
  </read_first>
  <action>
    REL-01: Add a public `GET /health` route (no `@require_role`) returning JSON. Wrap `db.session.execute(text("SELECT 1"))` in try/except; on success return `jsonify({"status": "ok", "db": "connected"})` with HTTP 200; on exception return `jsonify({"status": "error", "db": "unavailable"})`, 503. `text` is already imported. Place it near the other public routes (after the kiosk route). This must remain public per D REL-01 — do NOT add auth.

    PERF-01 (models.py): Import `Index` from `sqlalchemy` at the top of models.py. On `AttendanceRecord` remove `index=True` from the `emp_id` and `date` `mapped_column(...)` definitions and add a class-level `__table_args__ = (Index("ix_attendance_emp_date", "emp_id", "date"),)`. This makes `db.create_all()` build the composite index for fresh databases.

    PERF-01 (app.py startup migration): In the `with app.app_context():` block (after `db.create_all()`, alongside the existing ALTER TABLE guards), add an idempotent migration that drops the two old column indexes and creates the composite for already-existing databases. Use the same `with db.engine.connect() as conn: conn.execute(text(...)); conn.commit()` pattern wrapped in `try/except sa_exc.OperationalError: pass`. Drop the SQLAlchemy-default-named indexes `ix_attendance_record_emp_id` and `ix_attendance_record_date` (use `DROP INDEX IF EXISTS`), then `CREATE INDEX IF NOT EXISTS ix_attendance_emp_date ON attendance_record (emp_id, date)`. Per D PERF-01 use raw DDL (no Flask-Migrate) following the existing inline-migration convention.
  </action>
  <acceptance_criteria>
    - `grep -c '@app.route("/health")' app.py` returns 1; the handler contains `SELECT 1` and returns 503 on failure (`grep -c '503' app.py` increases).
    - `cd /var/www/sites/face-almgp33 && venv/bin/python -c "import app; c=app.app.test_client(); r=c.get('/health'); assert r.status_code==200 and r.get_json()['db']=='connected'"` exits 0.
    - `grep -c 'ix_attendance_emp_date' models.py` >= 1 and `grep -c 'ix_attendance_emp_date' app.py` >= 1.
    - `grep -c 'Index' models.py` >= 1 (import + usage); `models.py` parses: `venv/bin/python -c "import models"` exits 0.
    - Startup runs clean against the live DB: `venv/bin/python -c "import app"` exits 0 (the migration block executed inside app_context on import).
  </acceptance_criteria>
  <done>/health returns 200/503 correctly; AttendanceRecord uses a single composite index defined in models.py and migrated in for existing DBs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Flask session cookie | Session cookie crosses the network; must resist theft/replay |
| public internet→/health | Unauthenticated endpoint reachable by anyone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | session cookie over HTTP / JS access | mitigate | SEC-04 sets Secure + HttpOnly + SameSite=Lax |
| T-09-02 | Information Disclosure | GET /health | accept | returns only {status, db} — no PII, no internal detail; intentionally public for uptime checks |
| T-09-03 | Denial of Service | /health DB probe | accept | single `SELECT 1`, no auth needed; rate limiting is out of scope for read-only health (login/PIN covered in 09-02) |
</threat_model>

<verification>
- All three tasks: `venv/bin/python -c "import app"` and `import models` succeed with SECRET_KEY set.
- No "09:00:00" literal remains; both late-detection sites call `_time_threshold`.
- `/health` returns 200 when DB up; KZ_HOLIDAYS has 2027; cookie flags present; composite index defined and migrated.
- Post-deploy smoke: `pm2 restart face-recognition` then `curl -s http://127.0.0.1:5051/health` returns `{"status": "ok", "db": "connected"}`.
</verification>

<success_criteria>
BUG-01, BUG-02, BUG-03, REL-01, REL-02, SEC-04, PERF-01 implemented exactly per 09-CONTEXT.md; app and models import cleanly; /health live; no hardcoded 09:00 late logic remains.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 09-01)

| Symbol / Route / Key | Type | Location | Notes |
|----------------------|------|----------|-------|
| `GET /health` | route | app.py | public; {status, db}; 200/503 |
| `KZ_HOLIDAYS[2027]` | data | app.py | full recurring KZ holiday list for 2027 |
| `SESSION_COOKIE_SECURE/HTTPONLY/SAMESITE` | config | app.py | Lax; Secure; HttpOnly |
| `dept_name_map` | local var | app.py compute_dept_summary | UUID→name map |
| `ix_attendance_emp_date` | DB index | models.py + app.py startup | composite (emp_id, date); replaces ix_attendance_record_emp_id/date |
</artifacts_produced>

<output>
Create `.planning/phases/09-security-hardening-and-critical-bug-fixes/09-01-SUMMARY.md` when done.
</output>
